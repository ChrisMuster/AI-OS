# Memory

**Last modified:** 2026-08-10

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-08-10.

- 2026-08-04 - Reconstructed backlog.md after data loss removed most Active items and the Build Only When Needed section. Restored missing items from memory logs, completed archive evidence, MEMORY.md, and session-search transcripts; pruned the oversized guard-coverage entry back to backlog scale; restored Build Only When Needed; added a completed archive entry for the journal declined-list fix; and updated MEMORY.md with the restored counts.
- 2026-08-04 - Added the Shadow Repository Feasibility Spike to backlog.md ahead of the Personal AI OS plan review and refreshed the MEMORY.md backlog index line to 20 Active items.
- 2026-08-04 - Added backlog-backups/ as the local snapshot store used by the new backlog-guard workflow, and documented it in Contents.
- 2026-08-04 - Added `project_newline_test_sites.md`, the first memory written to act as a standing session-startup reminder rather than as a fact to recall on topic. It holds a set of outstanding text-write fixes and the agreed sequence around them, at the user's instruction that they are deliberately not a backlog item. Its MEMORY.md index line is marked as a startup reminder so it is surfaced in the greeting every session until the user confirms the work is done, at which point the file is deleted. Also extended `feedback_agents_line_threshold.md` with the reporting half of that rule: flag the line count in one line without restating the rationale.
- 2026-08-06 - Took `feedback_diagnose_review_misses.md` from five standing checks to eleven, at the user's instruction that a diagnosed miss must become a measure rather than a paragraph in a session report, and that past review sessions be swept for admitted mistakes that never became checks. The six new checks came from rounds 1 to 4 of the guard-coverage re-plan review: a citation that resolves is not a citation that is right, because its target moves when the file is edited above it; never accept a document's account of which of its own instructions have already run; derive an enumerated set independently rather than only checking the members it names; a search that fails reads exactly like a search that found nothing; a verification step is only a check if it could fail, meaning it must both observe its target and fail when the work is absent; and a correction can break the half of a rule that was working. Check 3 also gained the clause that it applies to a plan for a behaviour change and not only to the change. Also extended the review ledger's line-number trap with its inverse: a generated locator is checked against the tool that generated it rather than against raw file lines, after a round 4 reviewer finding was rejected on exactly that basis. The change-vector sweep found two places carrying the old count, the file itself and the `MEMORY.md` index line, and both were updated.
- 2026-08-07 - Extended `feedback_doc_change_vectors.md` to put log entries inside the change vector, at the user's instruction. The triage list exempted historical records including `LOG.md`, and that exemption is now conditional on the record being correct: an entry that asserted something untrue is a false claim in the audit trail rather than a record of anything, so a correction sweeps the logs alongside the prose and lands as a new entry naming and quoting the old one, never as an edit to it. The prompting case was a 2026-08-06 entry claiming a backlog item had been "returned to a neutral pre-fold form" when a snapshot proves a third form: the root `LOG.md` was corrected the same night while the identical sentence in `memory/LOG.md` went unswept for a day. The `MEMORY.md` index line was rewritten to carry the new clause and, measured the same way as the 2026-08-06 entry above, came down from 197 characters to 124 against the 150 guidance, so it now complies where it previously did not.
- 2026-08-07 - Took `feedback_diagnose_review_misses.md` from twelve standing checks to seventeen after round 6 of the guard-coverage re-plan review, and corrected a stale figure inside it. The five new checks are all the authoring pass's own misses: verify a mapping in the direction that detects omission rather than the direction the document offers; read copied content in its destination's context; derive figures embedded in prose, not only those in sections about figures; sweep outward into the files that cite or govern a corrected claim; and take a count from the tool rather than by eye, after the pass reported four grep hits where its own output showed five. The correction is to check 3, which said the guard-coverage plan names neither root index "across seventeen stages": seventeen is the post-re-plan position count, not the present one, and two of those positions are a gate and a re-plan step rather than stages. It was found because the re-plan had already corrected the identical conflation in its own text while the sweep never reached the memory file it cites for the rule, which is check 16 catching the change that created it. The `MEMORY.md` index line was updated from eleven to seventeen in the same pass.
- 2026-08-10 - Took `feedback_diagnose_review_misses.md` from seventeen standing checks to twenty-two after round 7 of the guard-coverage re-plan review. All five are the authoring pass's own misses, and two of them are the same defect seen from opposite sides: round 6 wrote two corrections that described themselves accurately and did neither of the things they claimed, which is now check 19, verify a correction against the property it was meant to establish rather than against its own description. The other four: run a verification command as literally written rather than confirming its flags exist, after a row using GNU grep alternation against a ripgrep tool passed on the untouched tree and so could never detect the work it checks; expand a checklist that a deliverable references by name, after a required bullet changing a different file stayed invisible to every check reading the deliverable; check a document's header and metadata against a pass that rewrites its body, after a `Revised:` line reached no correction, deliverable or verification row; and read a destination file's existing structure before specifying content for it, after rationale for four of seven decisions turned out to be already written in the destination while the plan called all seven pending, so the specified action would have written a second copy beside the first. The `MEMORY.md` index line was updated from seventeen to twenty-two and re-measured at 143 characters against the 150 guidance, having gone to 151 on the first wording.
- 2026-08-10 - Added a row to the artefact table in `guard_coverage_decision_log.md` for `CODEX-GUARD-COVERAGE-REPLAN-PLAN.md`, the Codex baseline taken on 2026-08-10 for the guard-coverage re-plan at the state sent for cross-AI review, and noted the baseline in the paragraph beneath the table. The convention it follows is [[feedback_gitignored_plan_baselines]]: a gitignored plan gets a stable copy of the version the reviewing AI saw, so later corrections can be diffed with `git diff --no-index`, and the copy is deleted rather than archived once its subject is finished. The insert pushed the two history-archive rows down one line, so the four locators the re-plan cites into this table moved from 485/490/491/492 to 485/490/492/493 and the paragraph and keep-current instruction beneath it moved from 494-496 and 498 to 495-498 and 500; all five references in the re-plan were re-derived from the file rather than recounted by eye, which is the standing check the same session added as check 18.
- 2026-08-10 - Added `reference_git_push_needs_sandbox_disabled.md`, recording that `git push` fails under the default Bash sandbox with "Could not resolve host" because the sandbox blocks DNS, and that the fix is to re-run the identical command with the sandbox disabled. Written after the user pointed out that this is rediscovered every time and asked where the rule was: a search of `memory/`, `AGENTS.md`, the wrapper and both settings files found nothing, so the absence was the cause rather than a failure to read. The file states what it must not be diagnosed as (network outage, credential, remote URL, proxy, git config), scopes the confirmed case to push while flagging the same expectation for other network commands, and states explicitly that disabling the sandbox is a workaround for network isolation and never for a guardrail, so it does not make push self-authorising under [[feedback_git_review_first]]. Its `MEMORY.md` index line was measured at 221 characters on the first wording and 163 on the second, and trimmed to 149 against the 150 guidance.
- 2026-08-10 - Corrected `guard_coverage_decision_log.md` rationale for guard coverage decisions 34 and 37 after Codex review found the re-plan would otherwise confirm stale text. Decision 34 now records the parent-plan-only sweep/test model and the runnable command, rather than a five-document sweep and numbered memory hit count; decision 37 now distinguishes predicate reuse from the new allowlist mechanism. Also normalised four existing dash characters in this file to ASCII so strict document verification passes on the touched file.
- 2026-08-10 - Rewrote `reference_git_push_needs_sandbox_disabled.md` from a diagnosis into an instruction, hours after it was written, because the version added that morning was applied reactively on its first live use: the push was announced as likely to fail on the sandbox DNS block and then run into that failure anyway, and the user asked why the doomed attempt is run when the outcome is already recorded. The file said what the error means and how to recover from it, and its recovery sentence assumed a first run had happened, so nothing in it told a reader to skip straight to the sandbox-disabled run. It now leads with running the command with the sandbox disabled from the start, rules out announcing the failure and then waiting for it, extends the same approach to the other networked commands it names rather than only expecting the same failure from them, and records the general point that a memory written as a description of a symptom is consulted once the symptom appears while one written as an instruction is consulted before the command runs. The two environment memories it cross-links, [[reference_bash_cwd_breaks_hooks]] and [[reference_windows_mcp_stdio_setup]], are flagged in the file as unswept for the same symptom-shaped wording. The `MEMORY.md` index line was rewritten action-first and measured at 149 characters against the 150 guidance.
- 2026-08-10 - Swept the two environment memories that the `reference_git_push_needs_sandbox_disabled.md` rewrite flagged, `reference_bash_cwd_breaks_hooks.md` and `reference_windows_mcp_stdio_setup.md`, for the same symptom-shaped wording and rewrote both to lead with the action. The `cd` memory turned out to be factually stale as well as badly shaped: it was written when the rule-hooks PreToolUse hook resolved its script path relative to the shell's working directory, so a stray `cd` blocked every subsequent Bash and Edit call, and the live hook commands in `.claude/settings.json` are now anchored to `$CLAUDE_PROJECT_DIR`, which removes that failure entirely. It now leads with the prevention, which stands on the project's relative-path convention rather than on the hook, records the fix and the date it was verified, keeps the PowerShell recovery for any future relatively-resolved hook, and drops a pointer to a backlog item that no longer exists. The MCP memory was already instruction-shaped in its section headings but had no opening that led with the action, so it gained a six-step "Do this" block covering transport choice, the executable-plus-script shape, which config the extension actually reads, the window reload, and raising `MCP_TIMEOUT` up front rather than after seeing zero tools. Eight pre-existing non-ASCII characters in it, six em dashes and two tick marks, were normalised to ASCII because the touched file failed strict document verification on them; they were located from the checker's line numbers rather than by eye. Both `MEMORY.md` index lines were rewritten action-first and measured at 145 and 147 characters against the 150 guidance, the second replacing a line that had been running at 325. The closing paragraph of `reference_git_push_needs_sandbox_disabled.md` was corrected in the same pass, since it named both files as unswept and that stopped being true here.
