# Memory

**Last modified:** 2026-09-23

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-09-23.

- 2026-09-18 - Archived the round-15 repairs packet to `rule_hooks_review_packet_archive_2026-09-18-round15-repairs.md`, rewrote `rule_hooks_review_packet.md` with the round-16 review result, updated the `MEMORY.md` index line, and added the round-15/16 `find -delete` lesson to `guard_coverage_review_ledger.md`. Round 16 found three A6 blockers in the repaired `_find_delete_is_filtered` model: a grouped broad branch, a negated predicate, and a predicate-looking token inside a prior `-exec` argv all let an unfiltered `-delete` through. The packet now says the held rule-hooks change set is blocked and must not be staged or committed. Later the same session the three findings were repaired and the packet updated in place: they moved to an Addressed section recording that the model was replaced rather than patched a fourth time, and the `MEMORY.md` index line was rewritten with it. The packet also gained a finding the repair produced on its own, from the mutation pass rather than from any review, that the round-15 implementation falsely blocked `find . -name '-o' -delete` by reading the pattern as find's OR operator. Its commit-readiness line now says the next review is required rather than available, since four consecutive rounds have found defects in the previous round's repairs and the repairs were again made by the AI that verified them. Extended rather than given its own entry so the section stays at its 15-entry cap.
- 2026-09-19 - Distilled round 16's durable lesson into `guard_coverage_review_ledger.md`, archived the closed packet to `rule_hooks_review_packet_archive_2026-09-18-round16-repairs.md`, and rewrote `rule_hooks_review_packet.md` with the round-17 review result. Round 17 found two A6 defects in the new `find -delete` expression evaluator: R17-1 is a blocker where `-fprintf` consumes two operands but the evaluator consumes one, allowing an unfiltered delete when the format string looks like a predicate; R17-2 is a false-block class where value-taking predicates and narrowing predicates drifted apart. `MEMORY.md` now points at the round-17 blocked state.
- 2026-09-19 - Recorded the round-17 repairs in `rule_hooks_review_packet.md` and rewrote its `MEMORY.md` index line. Both findings moved to an Addressed section with what changed and what proved it, the rounds table now reads repaired, and the packet gained a verification section written after the checks ran. Two things in it are decisions rather than results. The repair took the structural route at the user's choice, replacing the two drifted lists with one predicate table rather than adding the missing entries to both, his reasoning being that adding entries leaves the mechanism that produced four rounds of this in place. And the packet no longer states its own commit readiness, because he set the rule for how this item ends: a further round is not commissioned by default, he decides when it is finished after asking both AIs whether they think it is committable, and a fifth fault of the same shape in A6 means re-planning A6 rather than repairing it again. The packet also gained a deliberate boundary for the bare `find` tests, which were left out of the table on the grounds that adding one is an allow rather than a block. This entry also covers a repair to this directory's own `LOG.md`, whose last three entries carried CRLF line endings and were normalised with `encoding-guard --fix`, clearing the only encoding WARN in the project. Later the same session, at his instruction, `feedback_review_loop_ends_by_decision.md` was created and indexed, recording that rule as a standing one rather than leaving it in the conversation and the packet: no round is commissioned by default, he decides when an item is finished after asking each AI whether it thinks the work is committable, and three or four rounds spent on the same thing means re-planning that thing rather than repairing it again. The packet was then marked as commissioning round 18 and had one passage rewritten that broke this process's own rule against restating what the Open and Addressed sections already encode, a status line having said which findings were repaired. Extended rather than given its own entry so the section stays at its 15-entry cap.
- 2026-09-20 - Distilled the round-17 A6 lesson into `guard_coverage_review_ledger.md`, archived the closed repair packet to `rule_hooks_review_packet_archive_2026-09-19-round17-repairs.md`, and rewrote `rule_hooks_review_packet.md` with the round-18 review result. Round 18 found two A6 blockers: a value-taking global option can consume the apparent `-name` guard, and syntactic predicates such as `-fstype ntfs` can match the whole traversal while still being treated as narrowing. The packet now records that the held rule-hooks change set is not committable and that A6 should be re-planned before another repair. `MEMORY.md` was updated with that state. The oldest visible Revision History entry was archived to LOG.md to keep the section at its cap.
- 2026-09-20 - Recorded the end of the A6 `find -delete` series. `guard_coverage_decision_log.md` gained the decision and its reasoning: the adjudication was removed rather than repaired a fifth time, because round 18 showed the question it asked has no answer available from the argv, and because `-delete` was the only destructive verb in A6 with an exception. The measurement that made the cost acceptable is recorded there too, being the part a later reader will want to check rather than take on trust: zero fires on real work in 82 days of fire-log, and every `find ... -delete` in the session-search index postdating the check itself. `rule_hooks_review_packet.md` was updated in place rather than archived, no new round having run: R18-1, R18-2 and R18-3 moved to an Addressed section, the round-18 findings and verification were kept under Superseded headings rather than deleted, and the bare-tests boundary was replaced by the accepted false block that supersedes it. Its `MEMORY.md` line now says the removal is unreviewed by a second AI, which is the state that matters for what happens next and is the user's call rather than the packet's. `backlog.md` gained the fire-log contamination item at the user's instruction, sequenced to be picked up once the rule-hooks change set is committed (Active 24 to 25, guard checked against its snapshot).
- 2026-09-20 - Archived the closed round-18 rule-hooks packet to `rule_hooks_review_packet_archive_2026-09-20-round18-closed.md`, rewrote `rule_hooks_review_packet.md` with the round-19 review result, and updated `MEMORY.md`. Round 19 found one open A6 blocker: `env -S` and `env --split-string` can hide `find . -delete` because their option values are executable command input, not inert wrapper configuration. `guard_coverage_review_ledger.md` gained the durable review lesson. The oldest visible Revision History entry was archived to LOG.md to keep the section at its cap.
- 2026-09-20 - Tightened the round-19 `env -S` / `--split-string` lesson in `guard_coverage_review_ledger.md` with the repair controls now handed to Claude: prove both separated and attached split-string spellings, plus an ordinary `env VAR=value command` control. The root repair handover and briefs were updated in the same task; `memory/rule_hooks_review_packet.md` remains the source of truth for the open finding. The oldest visible Revision History entry was archived to LOG.md to keep the section at its cap.
- 2026-09-21 - Recorded the R19-1 repair. `rule_hooks_review_packet.md` moves R19-1 to Addressed with the fix, the two rejected options and the measurement behind the accepted cost, and its `MEMORY.md` index line follows; the packet also now records that the finding was wider than the round reported it, being a bypass of the shared reader rather than an A6 defect. `guard_coverage_review_ledger.md` gained two general traps from the repair rather than two more `env` facts: that an entry in a table of value-taking options makes two claims and only the first was being checked, a row being able to be the wrong KIND rather than missing; and that a store can be the wrong instrument for an absence measurement when the defect under investigation is what keeps it empty, which is why the fire-log was set aside for the session-search index here. The oldest visible Revision History entry was archived to LOG.md to keep the section at its cap.
- 2026-09-21 - Recorded the triage rule the user set off the back of the last two review series, which is a change to how reviewing works rather than a record of one round. Added `feedback_realistic_break_test.md`: finding a break is the first half of a review and triaging it is the second, so three questions follow any break before a fix is designed, and when a shape is neither likely nor harmful the fix is to refuse it rather than to model it. The same rule went into `review_process.md` as a new section, a step in the reviewer's during-review list, two rows in the What Belongs Where table and a line in the handoff checklist, because that document is what a reviewing AI actually reads. The reasoning recorded is the user's: the `find -delete` series cost five rounds, ten blockers, 294 lines and 33 tests before being deleted, and a refusal is cheap precisely because a block is visible and reversible where a wrong model fails silently in the allowing direction. `MEMORY.md` indexed the new memory. The oldest visible Revision History entry was archived to LOG.md to keep the section at its cap.
- 2026-09-21 - Archived the closed round-19 rule-hooks packet to `rule_hooks_review_packet_archive_2026-09-21-round19-repairs.md`, rewrote `rule_hooks_review_packet.md` with the round-20 review result, and updated `MEMORY.md`. Round 20 reviewed the A10 `env -S` / `--split-string` repair and reported no new findings. The packet records the checks run and deliberately does not write a commit-readiness verdict or commission round 21, both belonging to the user. The oldest visible Revision History entry was archived to LOG.md to keep the section at its cap.
- 2026-09-21 - Archived the clean round-20 rule-hooks packet to `rule_hooks_review_packet_archive_2026-09-21-round20-clean.md`, rewrote `rule_hooks_review_packet.md` as the round-21 Claude review commission, and updated `MEMORY.md`. The packet preserves Codex's no-findings result in the archive, asks Claude to re-check the A10 repair and the round-20 reasoning, and keeps stage, commit and push as separate user-gated actions. The oldest visible Revision History entry was archived to LOG.md to keep the section at its cap.
- 2026-09-21 - Closed the rule-hooks review loop. `rule_hooks_review_packet.md` was rewritten as a closed record rather than archived and deleted, at the user's instruction, because it carries the item's round history and the reason the loop ended, which the ledger has no place for; its rounds table now shows round 21 as cancelled by decision rather than as a round that found nothing, since it was never run. `MEMORY.md` points at that closed state. The substantive decision recorded is the user's: round 21 would have had the author of the A10 repair review their own code after Codex had already reviewed it independently and clean, which discharges nothing the writer/reviewer rule had not already discharged, and the finding trend across rounds 14 to 20 had run 5, 3, 3, 2, 3, 1, 0. What was actually outstanding was the close-out verifier, never run across the whole series. `guard_coverage_review_ledger.md` gained one trap from this, and it is the more reusable half: a gate can fail on the state of its own baseline rather than on the work. The verifier failed on three doc-sync WARNs, `gained_rh_entry` requiring a non-empty suffix of the committed Revision History entries to survive where every committed entry in those three directories had been archived across uncommitted sessions; the failure was accepted as known-false and then, re-run after the commit, returned PASS on all three gates, so it never needed accepting. The oldest visible Revision History entry was archived to LOG.md to keep the section at its 15-entry cap, and the archive-plus-append this produces is the shape that warned falsely before the commit; it does not warn now, because the commit reset the baseline the guard compares against, which is the same finding from the other direction.
- 2026-09-23 - Moved the three pre-push items to `backlog-completed-2026.md` once the work was committed at `8f32832`: the fire-log contamination, the A9 adjacent string literals, and the link-check self-links. Active went 25 to 22 against the snapshot taken first, with an explicit allowance of 3 for the deliberate reduction. Each archive entry carries the finding that generalises rather than a summary of the diff, which is what makes the archive worth re-reading: a guarantee checked in one place and stated in general is not a guarantee, a backlog item written before a triage rule may carry a proposal that rule would now overturn, and a measured count can be fifty times the reported one while the harm stays cosmetic. This section is now at its 15-entry cap with nothing archived, since it stood at 14; the next entry here archives one.
- 2026-09-23 - Restructured the Active list at the user's direction after a review of all 22 items, taking it to 19. Two merges, each because the parts shared one file or one question rather than merely one topic: three encoding-guard scanner defects became one item, and the verification-claim hook and the handover scope-limiting check became one, the latter having already recorded that it shared the other's core question. **The user's hypothesis that these belonged to guard coverage was checked against the plan rather than agreed with, and it was wrong**: `GUARD-COVERAGE-PLAN.md` line 73 records encoding-guard as no scope change, and line 77 gives doc-sync-guard two changes that do not include the archive-plus-append defect, so folding either in would have widened a plan that says it is not changing those guards. A `(quick win)` tag was added to four items, with the convention written into the Rules section beside `(build only when needed)`. Three of the four were narrowed by measurement taken in the session rather than by re-reading: the PDF item is a missing `pdftoppm` binary and not a missing capability, the scheduler fault is Codex-specific since the Claude launch was observed alive at PID 43568, and the textstat cleanup is six references across four files. The fourth records that its own widened claim was contradicted on 2026-09-23. The sync architecture item changed shape: Stage D targets the NAS rather than Google Drive, dropping the Mirror-mode and junction mechanisms, with the user ruling knowingly that the absent off-site copy is not a blocker until the site-to-site link exists. Trimmed to clear the guard's 1600-character item limit, which fired on the first attempt at 1721.
