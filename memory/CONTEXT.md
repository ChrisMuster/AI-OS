# Memory

**Last modified:** 2026-09-26

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-09-26.
- 2026-09-23 - Recorded Codex's round-2 re-review of the hook trust check repair. `codex_hook_check_review_packet.md` no longer says the R1-1/R1-2 repairs are unreviewed, its Review Notes record that no R2 findings were found, and the `MEMORY.md` index line now says the user readiness decision is next. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Added `feedback_stay_on_the_item_in_progress.md` and indexed it in `MEMORY.md`, after the user corrected a startup plan that offered other backlog items as alternatives while the two-AI review orchestration item was under way: mid-item, the next step is that item's next step. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - The `MEMORY.md` line for `codex_hook_check_review_packet.md` now reads CLOSED, the change set having been committed at `ee9a8f5` with no round open. `feedback_agents_line_threshold.md` gained the user's decision that the AGENTS.md line-count warning is reported in one line and never waits on his acknowledgement, overriding the AGENTS.md wording that asks for it. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Brought the review-orchestration records up to date for its second test stage. `review_orchestration_decision_log.md` settles the three pre-B2 questions, records the new `AGENTS.md` orchestrated-session clause, the user's choice that the run's first writer pass makes the change, and a derailment walk; it also records a correction, that the plan's "must not add to AGENTS.md" line was an AI's, not the user's. `review_orchestration_review_ledger.md` updates its automated-session trap from expected to measured. `feedback_agents_line_threshold.md` gained the rule that such a plan line is the same fault and is never attributed to the user. Later the same session B2 ran: the decision log gained its report (clean after one round, a Revision History cap both AIs missed and the audit caught, an approver too strict for the writer to verify its work, startup skip held, SDK cost counters), the ledger gained two traps from it, and `textstat_cleanup_review_packet.md`, written by the run, was indexed in `MEMORY.md`. The textstat cleanup item then moved from `backlog.md` to `backlog-completed-2026.md` at the user's instruction (Active 19 to 18, guard checked against its snapshot). The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Recorded the B2 rerun. `review_orchestration_decision_log.md` gained its report: the planted defect was caught by the in-loop checks and fixed in the fix pass, and the run stopped after three rounds on a real finding outside its edit paths. `review_orchestration_review_ledger.md` gained the trap behind that stop, and the "gitignored is not unversioned" trap the user raised earlier. `stdout_utf8_sweep_review_packet.md`, written by the run, was indexed in `MEMORY.md`. The decision log then gained the user's meter readings (Codex 10% to 46% over three reviewer rounds) and a section recording that Stage A must restore independent planning, the front half of the design the live plan had drifted away from. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-24 - `review_orchestration_decision_log.md` gained "Stage A drafted", the reasons for the main choices in the newly drafted Stage A specification, and the derailment walk for drafting it; then "The user's decisions for Stage A", recording the stacked branch, optional SDKs, default models, replay set, round caps and the usage-limit handling he raised. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-24 - Set up the Codex review of the Stage A plan: `review_orchestration_decision_log.md`'s artefact table names the round's brief and packet. `stdout_utf8_sweep_review_packet.md` moved R3-1, the journal script, to Addressed after its hand fix, and its `MEMORY.md` line now reads 0 open; two em dashes the run had copied from audit output into finding titles were replaced with hyphens, and `review_orchestration_review_ledger.md` gained the trap behind them. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-24 - Created `review_orchestration_review_packet.md` for Codex's review of the Stage A plan and indexed it in `MEMORY.md`. The packet records 11 open findings: 10 plan-readiness issues around stop reasons, intent checks, scope/disagreement mechanics, blind synthesis, usage limits, model defaults, replay restore refs and sync-boundary verification, plus one review-process issue about the brief's packet-heading wording. The Codex baseline `CODEX-REVIEW-ORCHESTRATION-PLAN.md` was refreshed to the reviewed plan after the findings were recorded.
- 2026-09-24 - At the user's choice, the stdout UTF-8 sweep moved to `backlog-completed-2026.md` and its undecided enforcement question became part (d) of the encoding-guard scanner internals item (Active 18 to 17, guard checked against its snapshot). Then orchestrated run B3 repaired Codex's Stage A findings from the user's four rulings: `review_orchestration_review_packet.md` now holds 13 addressed and 3 open, the run having stopped on `disagreement` over the replay set; `review_orchestration_decision_log.md` gained the B3 report and its derailment walk; `review_orchestration_review_ledger.md` gained two traps, that an uncommitted reviewed change cannot be replayed and that a declined finding and its re-raise count twice; the packet's `MEMORY.md` line follows. After the user's ruling on the replay set, the three findings left open were repaired by hand and moved to Addressed, the decision log records the meters, the ruling and the replay set chosen under it, and `backlog.md` gained the Revision History miscount item (Active 17 to 18, guard checked). Codex's review-only round R4 then added R4-1 and R4-2 to the packet; the decision log records the round and the re-test of the replay set, and the ledger's replay trap now says to restore everything a brief lists as under review. After the user's ruling on what qualifies a round, both were repaired by hand and moved to Addressed, and the decision log records the ruling and R4's meter reading. Finally, at the user's go-ahead, `review_process.md` gained "Capture Before Every Review Round" (uncommitted code under review saved as `memory/<item>_review_R<round>.diff`, then a personal capture) and a matching Handoff Checklist item. Extended rather than given its own entry so the section stays at its cap.
- 2026-09-24 - After Stage A1 was built, the closed plan-review round was archived: `review_orchestration_review_ledger.md` gained "A repair note goes stale when a later repair moves what it cites", the packet was copied to `review_orchestration_review_packet_archive_2026-09-24.md` under a closed header, and the live packet was reset for the A1 code review and now holds its round R1 findings, all five repaired by hand and addressed; the ledger also gained "Tool output written into a packet can break the packet's own structure", from that round. The uncommitted A1 code was saved as `review_orchestration_review_R1.diff` before the round, and the repaired code as `review_orchestration_review_R2.diff` ready for round R2, and the packet's `MEMORY.md` line follows. A first attempt extended the entry above instead, which doc-sync rightly reported as no entry gained once that entry was committed; this entry replaces it, and the oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-25 - Recorded the A1 code review's round R2. `review_orchestration_review_packet.md` gained R2-1 and R2-2, both on the brief checker's launcher handling, now repaired by hand and addressed (launchers refused outright, at the user's choice over Codex's suggested fix), and its header names both round diffs; `review_orchestration_decision_log.md` gained "A1 code review round R2, and launchers refused outright"; the packet's `MEMORY.md` line follows. The oldest visible entry was archived to LOG.md to keep the section at its cap. For round R3 the repaired code was saved as `review_orchestration_review_R3.diff`, and the decision log's artefact row names it; R3 came back clean, which the packet's header, the decision log and the packet's `MEMORY.md` line now record.
- 2026-09-25 - `review_orchestration_decision_log.md` gained "The user's decisions for A2": the SDKs go into the project `.venv`, a narrow permission rule replaces per-run approval once the engine has a fixed path, A2 is built in three committed chunks, and the doc-sync archive-plus-append quick win is the proof item, with the reasons the other two do not fit. Its artefact table names the scratch archive the B and A1 round scripts were copied to, outside the project. Given its own entry because the previous one is committed; the oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-25 - Before the A2 chunk (a) code review, the closed A1 code review round was archived: `review_orchestration_review_ledger.md` gained "Narrowing a parser to fix one false refusal invites the next bypass", the packet was copied to `review_orchestration_review_packet_archive_2026-09-25.md` under a closed header, and the live packet was reset for the chunk (a) review, whose reviewed code is saved as `review_orchestration_review_A2a_R<round>.diff`. `review_orchestration_decision_log.md` gained "A2 chunk (a) built" and an artefact row for the new diff names; the packet's `MEMORY.md` line follows. The oldest visible entry was archived to LOG.md to keep the section at its cap. After round R1 the packet gained R1-1 and R1-2, both repaired by hand and moved to Addressed, the decision log gained the round's record, and the packet's `MEMORY.md` line follows. The decision log then recorded the user's ruling that paid overage stops a run. Round R2 saved `review_orchestration_review_A2a_R2.diff`, accepted R1-1 and R1-2, and raised R2-1 into the packet; the decision log gained the round's record and the packet's `MEMORY.md` line follows. R2-1 was then repaired by hand and moved to Addressed.
- 2026-09-26 - Round R3 of the A2 chunk (a) code review saved `review_orchestration_review_A2a_R3.diff`, accepted the R2-1 repair and raised R3-1 into `review_orchestration_review_packet.md`; `review_orchestration_decision_log.md` gained the round's record and the packet's `MEMORY.md` line follows. The oldest visible entry was archived to LOG.md to keep the section at its cap. R3-1 was then repaired by hand and moved to Addressed, and the decision log recorded the user's ruling that an unknown reset time is acceptable. Round R4 saved `review_orchestration_review_A2a_R4.diff` and came back clean; the packet header, the decision log and the packet's `MEMORY.md` line record it.