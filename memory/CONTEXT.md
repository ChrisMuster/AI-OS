# Memory

**Last modified:** 2026-08-23

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-08-23.

- 2026-08-12 - Added the Google Drive relocation to `backlog.md` as an Active item and marked it NEXT in the agreed order, at the user's instruction. It moves the project's parent folder out of Drive's Computers backup section into My Drive, so the project is genuinely two-way synced across both of the user's machines rather than backed up from one, which is the property a two-machine workflow needs and the one the Computers section does not provide. The item is deliberately lean, per the rule that caused the 2026-08-04 loss: it states what the work is and points at the relocation plan document for the staged plan, the measured breakage and the verification gate, rather than restating any of it. It is recorded as gating the shadow repository build, because the user chose to relocate first and prove the new location by real use before building on it; the argument for the reverse order, that the shadow would protect the irreplaceable gitignored files during a 17 GB copy, is recorded in the plan as considered and not taken. The agreed next-session order line gained a position 0 rather than renumbering the existing five, so the established order stays readable against earlier references to it. Backlog Active 19 to 20 with no count drop; snapshot 20260812T163103+0100 taken before the edit and the guard check passed after it.
- 2026-08-13 - Rewrote the Google Drive relocation item in `backlog.md` at the user's instruction, the previous session having flagged it as inaccurate but left it alone pending a snapshot and a go-ahead. Two things had changed under it. A rewrite of the relocation plan now sits in front of the move, because the existing plan document was written before the Drive cache failure was diagnosed and its assumptions did not survive that diagnosis; and the scope has gained three pieces that were settled in discussion, moving `.git` and `.venv` outside the synced tree, switching My Drive from Stream to Mirror, and a documented new-machine setup path, the last being needed precisely because the first means neither directory travels with the files any more. The edit is shape-level rather than a status update, which is the distinction the item's own rules draw: what changed is what the work now covers and what has to happen before it, not how it is going. The settled decisions themselves are deliberately not restated in the item and belong in the rewritten plan document, since the rolling handover that currently holds them is overwritten on the next handoff. Backlog Active 20 with no count drop; snapshot 20260813T123201+0100 taken before the edit and the guard check passed after it.
- 2026-08-13 - Split the sync architecture plan under the shared review process, adding `sync_architecture_decision_log.md` and `sync_architecture_review_ledger.md` as the item's companion documents so the live plan could be rewritten as a build spec. The decision log carries the rationale, the two corrections that testing forced, the superseded predecessor decisions and the rejected options; the ledger carries seven reusable traps. The backlog's relocation item was rewritten as the sync architecture item in the same pass, and the Shadow Repository Feasibility Spike was updated to record that its build is Stage A of that plan while it stays a separate item, on the user's decision that it is part one rather than folded scope.
- 2026-08-13 - Added `feedback_archive_find_the_rules.md` after the user pointed out that archiving had gone wrong twice and asked for something that sends Biblio to look for the archiving rules whenever archiving is mentioned. The file re-keys an obligation that already existed: `feedback_check_for_existing_procedure.md` covers it in general, cross-links the archiving rule, and carries an archiving failure as its own first example, but it is keyed to "before writing a script, a command, a procedure or a rule", which does not fire when the job is renaming one file. The new file is keyed to the word "archive" instead, the same fix that worked for the PowerShell here-string, and inventories the five distinct procedures by target so the right one can be named before anything moves: a directory, a CONTEXT.md Revision History over 15 entries, a spent plan or handover, a completed backlog item, and a `CODEX-*` review baseline. It records both failures with their diagnosis, states that a rename or a banner does not discharge the procedure, and logs the gap found today, that no rule covers a project-level plan with no owning workflow. The general file gained a pointer to it.
- 2026-08-13 - Updated `feedback_archive_find_the_rules.md` once the user settled the gap it had recorded as open. Rule 3 now carries the root `archived/` directory as the destination for a plan that governs the project as a whole and has no owning workflow, with the tie-breaker that a plan mostly building one workflow belongs to that workflow even if it touches others. It also gained the user's entry condition, which applies to every archive destination rather than only the new one: a document is archived when the work it specifies is finished or abandoned, never when it merely stops being read, so a live plan, a history archive of a plan still being built from, and a review baseline whose subject is still under review all stay at the project root. A superseded plan counts as finished. The known-gap section was rewritten from open to closed rather than deleted, so the reasoning that produced the new directory stays readable.
- 2026-08-13 - Added `feedback_commit_coauthor_trailer.md` recording the user's decision that AI-written commits carry a `Co-Authored-By` trailer, his reasoning being that he directs the work while the AI does it, so the history should say so. It was written because the rule cannot be inferred from the repository: no commit before this date carries the trailer, so a future session checking `git log` for the house style would conclude the opposite, and the file blocks that inference explicitly. The conflict behind it is recorded too, since the trailer is a standing instruction in the Claude Code environment while the project convention ran the other way, and every earlier session resolved that silently by dropping it. The user's first response was to ask whether the AI had invented the requirement, which is why the file also carries the general lesson that an environment instruction disagreeing with an observed project convention is surfaced for his decision rather than settled quietly.
- 2026-08-13 - Added `feedback_search_gitignored_for_rules.md` after a Grep search for a project rule returned no matches without having searched `memory/` or `USER.md`, because the Grep tool respects `.gitignore` and this project keeps its governing material in gitignored files. The conclusion drawn from that search was correct but unsupported, and only a slower full-tree search run afterwards established it. The file records why this matters beyond the one case: it silently undercuts `feedback_check_for_existing_procedure`, since a clean not-found over an unsearched region makes a missed rule look like diligence, which is the instrument problem `feedback_verify_the_checker` describes. It gives the working method, using `Glob` and `MEMORY.md` to reach the gitignored set, warns against recursive shell searches from the project root because the tree holds roughly 13 GB under `wikis/*/raw/` and one such search timed out the same day, and requires a negative result to state which regions it covered.
- 2026-08-23 - Appended a declined item to `feedback_journal_scan_ask_once.md` during session-startup step 8, per that file's own rule that a decline is recorded in the same session so it persists. The item is a person named in the 22 August journal entry as a family member's former partner; the user's reason is that she is the parent of someone `USER.md` already records, so she needs no record anywhere. The list now holds eleven declined items.
- 2026-08-23 - Added a backlog item to investigate mechanical detection of scope-limiting language in `HANDOVER.md`, on the user's decision after the rule behind it failed a third time. He was explicit that the item is an investigation and not a commitment to build: a mechanical check for this may not be feasible at all, so deciding whether reliable detection is possible is the deliverable rather than a prerequisite to it. That framing is the item's substance, because the shape being detected is semantic rather than lexical and a phrase list would fire on any honest sentence reporting a check result. The item records that it shares its core question with the verification-claim hook item already on the list, both asking whether a semantic rule failure can be caught by a machine, and notes they may be scoped together rather than twice. Active 20 to 21, no count drop and no allowance needed.
- 2026-08-23 - Extended `feedback_cross_ai_review.md` with a third occurrence of the handover-sets-the-scope fault, caught by the user before the Codex review packet was handed over. The clause was "Mechanical checks already run, so Codex need not repeat them", and two softer variants went in alongside it: a stale-word list asserting "the only place these should now appear", and a heading reading "Known gaps no mechanical check covers", which implies everything unlisted is covered. What the entry records is why every existing defence failed, since the rule was written, sat in this file, was quoted in the handover's own steer, and had been read earlier in the same session: at the moment of writing, the clause feels like saving the reviewer time rather than like scope-setting, which is the disguise the rule already names. The fix applied is structural rather than another warning, the packet now opening with a statement that the user sets the scope, framing every reported result as a claim to re-test, and marking each list as a floor or as exhaustive. An escalation question is left open rather than answered, since a rule failing three times earns a mechanism under [[feedback_escalate_rule_to_hook]], but the shape is semantic rather than lexical and needs scoping before anything is built.
- 2026-08-23 - Added a trap to `sync_architecture_review_ledger.md` after discharging the Stage 0 Drive sync gate, because three instruments were tried on one question and only the third could be trusted. A scan for `ERROR` as a word matched nothing, since Drive appends severity as a single letter to the timestamp, and a zero there reads exactly like a clean log; counting `ZE` lines then gave errors nearly equal to info, a count that is correct but useless because Drive logs status polls and shell-IPC chatter at error severity. What worked was aggregating by source function. The trap also carries the general form of the problem, which is the more reusable half: when a finding is the *absence* of errors over a period, the logs for that period must be shown to exist, be continuous, and contain errors of other classes, or the absence is only a log nobody wrote. Extends the existing watcher trap in the same file and relates to [[feedback_verify_the_checker]].
- 2026-08-23 - Recorded the sync architecture correction pass in the item's two companion documents and updated both of its backlog items. The decision log gained the six decisions the user took one at a time after the cross-AI review, with the reasoning for each and the two places the first recommendation was withdrawn under his questioning: force-adding the wiki raw metadata was described as impossible when it is merely inadvisable, and the related spike plan was proposed for a historical banner before reading it in full showed it holds settled design existing nowhere else. It also gained the indexer constraint the review did not raise and the user did, that both the audit and the knowledge-graph indexer prune by plain `git check-ignore`, so the second repository must never become the default git context, together with the pre-build graph baseline of 317 nodes, 1288 edges and 9 broken references. Its artefact-roles table now carries the spike plan as the item's tactical plan and a second review baseline for it. The review ledger gained three traps, the load-bearing one being that a plan absorbed into a larger design rots outward-first, its claims about itself staying correct while its claims about its surroundings all fail at once, which is why a blanket judgement on such a document is wrong in both directions. The ledger now holds eleven traps. Both backlog items had their plan pointers corrected rather than their status narrated.
- 2026-08-23 - Second pass over the same two companion documents, after the superseded basic-python wiki was archived and the knowledge-graph question that ran alongside it was settled. The decision log's decision 3 gained the result of the content diff it had promised, and its indexer baseline was corrected from 317 nodes, 1288 edges and 9 broken references to 318, 1290 and 8; the figure quoted in the entry above was accurate when written and is superseded by this one rather than edited, since the earlier entry records what was true at that point. The correction is written up rather than swapped silently because it is the first exercise of the new baseline rule and it failed instructively: nodes and broken references matched the prediction exactly, while the edge count was wrong by two, both edges created by the Contents entry the archiving procedure itself required in the parent wiki index. The review ledger gained a trap for that, that a task's own documentation edits move the graph as surely as its filesystem changes do, plus an update marking the basic-python instance closed while its trap stands. The ledger now holds twelve traps. The decision log also records a rule collision found while following the archiving procedure: its step 5 calls for a README status-tag update while the wikis exception forbids listing individual wikis there, and the exception wins.
