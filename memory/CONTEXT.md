# Memory

**Last modified:** 2026-09-24

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-09-24.
- 2026-09-21 - Closed the rule-hooks review loop. `rule_hooks_review_packet.md` was rewritten as a closed record rather than archived and deleted, at the user's instruction, because it carries the item's round history and the reason the loop ended, which the ledger has no place for; its rounds table now shows round 21 as cancelled by decision rather than as a round that found nothing, since it was never run. `MEMORY.md` points at that closed state. The substantive decision recorded is the user's: round 21 would have had the author of the A10 repair review their own code after Codex had already reviewed it independently and clean, which discharges nothing the writer/reviewer rule had not already discharged, and the finding trend across rounds 14 to 20 had run 5, 3, 3, 2, 3, 1, 0. What was actually outstanding was the close-out verifier, never run across the whole series. `guard_coverage_review_ledger.md` gained one trap from this, and it is the more reusable half: a gate can fail on the state of its own baseline rather than on the work. The verifier failed on three doc-sync WARNs, `gained_rh_entry` requiring a non-empty suffix of the committed Revision History entries to survive where every committed entry in those three directories had been archived across uncommitted sessions; the failure was accepted as known-false and then, re-run after the commit, returned PASS on all three gates, so it never needed accepting. The oldest visible Revision History entry was archived to LOG.md to keep the section at its 15-entry cap, and the archive-plus-append this produces is the shape that warned falsely before the commit; it does not warn now, because the commit reset the baseline the guard compares against, which is the same finding from the other direction.
- 2026-09-23 - Moved the three pre-push items to `backlog-completed-2026.md` once the work was committed at `8f32832`: the fire-log contamination, the A9 adjacent string literals, and the link-check self-links. Active went 25 to 22 against the snapshot taken first, with an explicit allowance of 3 for the deliberate reduction. Each archive entry carries the finding that generalises rather than a summary of the diff, which is what makes the archive worth re-reading: a guarantee checked in one place and stated in general is not a guarantee, a backlog item written before a triage rule may carry a proposal that rule would now overturn, and a measured count can be fifty times the reported one while the harm stays cosmetic. This section is now at its 15-entry cap with nothing archived, since it stood at 14; the next entry here archives one.
- 2026-09-23 - Restructured the Active list at the user's direction after a review of all 22 items, taking it to 19. Two merges, each because the parts shared one file or one question rather than merely one topic: three encoding-guard scanner defects became one item, and the verification-claim hook and the handover scope-limiting check became one, the latter having already recorded that it shared the other's core question. **The user's hypothesis that these belonged to guard coverage was checked against the plan rather than agreed with, and it was wrong**: `GUARD-COVERAGE-PLAN.md` line 73 records encoding-guard as no scope change, and line 77 gives doc-sync-guard two changes that do not include the archive-plus-append defect, so folding either in would have widened a plan that says it is not changing those guards. A `(quick win)` tag was added to four items, with the convention written into the Rules section beside `(build only when needed)`. Three of the four were narrowed by measurement taken in the session rather than by re-reading: the PDF item is a missing `pdftoppm` binary and not a missing capability, the scheduler fault is Codex-specific since the Claude launch was observed alive at PID 43568, and the textstat cleanup is six references across four files. The fourth records that its own widened claim was contradicted on 2026-09-23. The sync architecture item changed shape: Stage D targets the NAS rather than Google Drive, dropping the Mirror-mode and junction mechanisms, with the user ruling knowingly that the absent off-site copy is not a blocker until the site-to-site link exists. Trimmed to clear the guard's 1600-character item limit, which fired on the first attempt at 1721.
- 2026-09-23 - Started the two-AI review orchestration item's companion documents, as the backlog rule requires once work starts: `review_orchestration_decision_log.md` and `review_orchestration_review_ledger.md`, both indexed in `MEMORY.md`. The ledger was created at once rather than deferred, because the planning session had already produced findings worth keeping, among them a probe that reported all web research broken because it skipped the production entry point's certificate setup. `feedback_review_loop_ends_by_decision.md` gained a scope note at the user's direction, separating manual review rounds, which he still commissions one by one, from rounds inside an orchestrated run, which run automatically; its index line follows. The backlog item now points at the plan and its companions, with Active unchanged at 19 against its snapshot. Later the same session the project Python floor was raised from 3.9 to 3.13 at the user's decision: the "Test the stated Python floor" item moved to `backlog-completed-2026.md` (Active 19 to 18), the decision log's floor section was rewritten for 3.13 with the rejected 3.10 to 3.12 options, `feedback_normalisation_standards.md` no longer steers writes away from `Path.write_text(newline=...)`, and the matching trap in `guard_coverage_review_ledger.md` gained a resolution note. Extended rather than given its own entry so the section stays at its 15-entry cap.
- 2026-09-23 - Created `python_floor_review_packet.md` and indexed it in `MEMORY.md` for Codex's review of the uncommitted Python 3.13 floor change. The packet records two open findings: PF1, the above-floor WARN is hidden by the startup rule that stays silent on WARN-only setup verification; and PF2, `setup.py --check` verifies package imports in `.venv` but not the `.venv` interpreter version, while runtime hand-off scripts re-exec into that environment. Later the same day Claude repaired both, choosing option B for PF2 at the user's direction, and moved them to the packet's Addressed section with what changed, what proved it and the boundaries left in place; the packet's description and its `MEMORY.md` line now say the repairs are unreviewed. Extended rather than given its own entry so the section stays at its 15-entry cap.
- 2026-09-23 - Recorded the pre-B1 derailment walk in `review_orchestration_decision_log.md`, after the Python floor change was committed at `6c12258` and captured privately at `47003bf`. The one new fact is vendor-checked and dated: the Agent SDK's default loads CLAUDE.md and the project hooks together from the same settings source, so an automated session cannot be kept out of session startup without also losing the guardrails, which makes that a B2 design question rather than a B1 setting. Given its own entry because the previous one was already committed, and doc-sync-guard does not count an extension of a committed entry as a new one. The oldest visible entry was archived to LOG.md to keep the section at its cap. Later the same session the user chose Option B for the first test stage: the decision log records why, and why a marker carried only by the orchestrator's sessions was preferred over a flag file; the ledger gained the watermark-ordering trap; and `feedback_explain_before_asking.md` gained a seventh recurrence, plan labels used as the explanation, with its `MEMORY.md` line rewritten to say it applies to every explanation and report. After the first test stage ran, the decision log gained its report (both logins work on the subscriptions, the skip marker worked, Claude's hooks are confirmed, Codex's hooks left no trace when started through the SDK) and the ledger gained two traps about testing a guard.
- 2026-09-23 - Created `codex_hook_check_review_packet.md` and indexed it in `MEMORY.md` for Codex's review of the uncommitted Codex hook trust check. The packet records two open findings: R1-1, a partial `hooks/list` response can pass because `judge_codex_hooks()` never compares Codex's returned project hooks with the hooks defined in `.codex/config.toml`; and R1-2, the old-Python path can crash on the local `tomllib` import before the floor failure is printed. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Recorded Claude's repair of R1-1 and R1-2 in `codex_hook_check_review_packet.md`: both moved to a new Addressed section with what changed, what proved it (including scratch-copy mutations each caught by named tests) and the one boundary, that the old Python is simulated. The original findings are kept under their own heading. The `MEMORY.md` line now says the repairs are unreviewed and go back to Codex. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Recorded Codex's round-2 re-review of the hook trust check repair. `codex_hook_check_review_packet.md` no longer says the R1-1/R1-2 repairs are unreviewed, its Review Notes record that no R2 findings were found, and the `MEMORY.md` index line now says the user readiness decision is next. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Added `feedback_stay_on_the_item_in_progress.md` and indexed it in `MEMORY.md`, after the user corrected a startup plan that offered other backlog items as alternatives while the two-AI review orchestration item was under way: mid-item, the next step is that item's next step. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - The `MEMORY.md` line for `codex_hook_check_review_packet.md` now reads CLOSED, the change set having been committed at `ee9a8f5` with no round open. `feedback_agents_line_threshold.md` gained the user's decision that the AGENTS.md line-count warning is reported in one line and never waits on his acknowledgement, overriding the AGENTS.md wording that asks for it. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Brought the review-orchestration records up to date for its second test stage. `review_orchestration_decision_log.md` settles the three pre-B2 questions, records the new `AGENTS.md` orchestrated-session clause, the user's choice that the run's first writer pass makes the change, and a derailment walk; it also records a correction, that the plan's "must not add to AGENTS.md" line was an AI's, not the user's. `review_orchestration_review_ledger.md` updates its automated-session trap from expected to measured. `feedback_agents_line_threshold.md` gained the rule that such a plan line is the same fault and is never attributed to the user. Later the same session B2 ran: the decision log gained its report (clean after one round, a Revision History cap both AIs missed and the audit caught, an approver too strict for the writer to verify its work, startup skip held, SDK cost counters), the ledger gained two traps from it, and `textstat_cleanup_review_packet.md`, written by the run, was indexed in `MEMORY.md`. The textstat cleanup item then moved from `backlog.md` to `backlog-completed-2026.md` at the user's instruction (Active 19 to 18, guard checked against its snapshot). The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-23 - Recorded the B2 rerun. `review_orchestration_decision_log.md` gained its report: the planted defect was caught by the in-loop checks and fixed in the fix pass, and the run stopped after three rounds on a real finding outside its edit paths. `review_orchestration_review_ledger.md` gained the trap behind that stop, and the "gitignored is not unversioned" trap the user raised earlier. `stdout_utf8_sweep_review_packet.md`, written by the run, was indexed in `MEMORY.md`. The decision log then gained the user's meter readings (Codex 10% to 46% over three reviewer rounds) and a section recording that Stage A must restore independent planning, the front half of the design the live plan had drifted away from. The oldest visible entry was archived to LOG.md to keep the section at its cap.
- 2026-09-24 - `review_orchestration_decision_log.md` gained "Stage A drafted", the reasons for the main choices in the newly drafted Stage A specification, and the derailment walk for drafting it; then "The user's decisions for Stage A", recording the stacked branch, optional SDKs, default models, replay set, round caps and the usage-limit handling he raised. The oldest visible entry was archived to LOG.md to keep the section at its cap.
