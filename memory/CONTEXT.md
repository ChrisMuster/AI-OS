# Memory

**Last modified:** 2026-08-12

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-08-12.

- 2026-08-11 - Recorded the fix pass for that review's five findings. `guard_coverage_decision_log.md` gained decision 44, why stage 14's evidence channel is proved before it is trusted: decision 42 pinned the reader's runtime and left the writer's to whatever the pre-commit hook resolves, so an interpreter without PyYAML would stamp every record degraded, the acceptance rule would exclude every one, and the observation period would never close while looking like a quiet stretch. The section records that the review's reproduction failed on re-running (PyYAML present in both the venv and the system install, `python` resolving to the venv in both shells, every real interpreter returning no findings), that the design gap survives its evidence, and why pinning the hook's interpreter was rejected for scope rather than principle, the stage 3a rejection having been of the hard bootstrap rather than of a soft preference. Three stale counts were fixed by deleting the number and naming the authority rather than by writing a new one: "three treatments" against five rows, and "all three" audit hooks against six, in this file and in the ledger. `guard_coverage_review_ledger.md` gained a correction to its own producer-runtime trap, which had stated the failed reproduction as observed fact, plus a new trap: a finding can be sound in substance and wrong in its evidence, and the evidence is what sets the severity, so re-run a reproduction before accepting a rating. That is the second time in three rounds that a finding's evidence has failed on re-running.
- 2026-08-11 - Extended `feedback_explain_before_asking.md` after the user was asked a question resting on a term he had no way to resolve. The rule already forbade questions phrased in plan sections and decision numbers, and already said to explain from first principles even when the topic came up earlier in the same session, but the failure took a shape the wording did not cover: an option was explained properly mid-session, given a label coined while explaining it, and then a closing question was hung on that label alone. A term the AI invents is exactly as opaque as a decision number, and the existing clause was read as being about topics rather than about labels. The file gained the recurrence, an instruction to read every noun phrase in a question and restate anything defined only earlier or only inside a working document, and a clause recording that the user asked for more explanation rather than fewer questions, so the rule is not read as licence to decide silently. It also gained a cross-link to [[feedback_stepwise_explanations]]. The `MEMORY.md` index line was rewritten to lead with the keep-asking half and measured at 148 characters against the 150 guidance, having come in at exactly 150 on the first wording.
- 2026-08-11 - Added `feedback_dont_narrate_routine_compliance.md` after the user pointed out that the same Revision History archive notice had been given to him repeatedly across sessions. The rule is to perform a documented maintenance step silently rather than announcing it, flagging it as an upcoming cost, or reporting it as news once done, on his point that as long as the step is being done and not missed, that is the system working as designed. The file draws the line at deviation against conformity: a failed check, an unfollowable rule, a judgement call between defensible options, or anything skipped is reported; a documented step running, a limit reached and handled by its documented remedy, or a threshold behaving as specified is not. It also states what the rule does not weaken, since both readings are available and wrong: verification results are still stated in full, and a routine step that cannot be completed, or that reveals something wrong, is a deviation and gets reported. Its `MEMORY.md` index line was measured at 142 characters against the 150 guidance.
- 2026-08-11 - Corrected the stage 4a cost estimate in `guard_coverage_review_ledger.md`, found by a stale-language sweep after the parent plan's stage 4a spec was fixed. The bullet still said the scaffold means a `run_tests.py` plus "a new close-out suite", which is the superseded instruction the plan had just been corrected away from: close-out discovers tests by globbing `tests/test_*.py` and has no suite to add, so assertions written into a local runner would sit outside the verification gate. The reason it mattered rather than being tidy-up is that the plan's stage 4a spec cites this bullet as the authority for the cost estimate, so the wrong wording stayed reachable from the corrected document. Fixed in place with a dated correction note rather than a silent rewrite.
- 2026-08-11 - Extended `feedback_cross_ai_review.md` with the rule that a handover suggests while the user sets the review scope. The prompting case was a repeat: the previous round's steer already said to do a full review rather than one scoped by the authoring AI or by `HANDOVER.md`, and the handover written straight after it still offered its suggestions "rather than leaving you to re-check what is solid", which declares the unlisted work sound and is the one judgement the author cannot make. The update records why that defeats cross-AI review specifically, since a reviewer who accepts the author's boundary inherits the author's blind spots, and gives the detection rule as a shape rather than a phrase: any clause whose effect is "you need not look here" is scope-setting even when worded as helpfulness. Cross-linked to [[feedback_dont_narrate_routine_compliance]]. The offending clause in the live `HANDOVER.md` was rewritten in the same pass, and the instruction added to that document's steer section so it binds the next session. One pre-existing em dash in the file's opening paragraph was normalised to ASCII, since strict verification failed on the touched file.
- 2026-08-11 - Recorded the fix pass for the reviewing AI's four findings on the corrected guard-coverage parent plan. `guard_coverage_decision_log.md` withdrew two claims it had stated as settled. The first is the denominator: decision 42's section called the observation period "30 usable hook fires", and the store counts neither fires nor commits but rows written inside `_precommit_doc_sync()`, which a personal-data block never reaches, so the parent plan's sharper claim that a blocked-then-retried commit writes two rows was wrong by one. The second is the producer runtime: the section closed with "the condition has never been observed here" on the strength of one session's shells, and a session on the same machine the same day resolved `python` to the system install and got the degrade, because resolution follows the launching process environment rather than the machine. Both corrections are appended rather than overwritten, and the interpreter-pinning deferral now rests on scope alone, with the decision recorded as open and the user's to take. `guard_coverage_review_ledger.md` gained a second correction to its producer-runtime trap, recording that its own first correction was the error and keeping all three states because the sequence is the lesson; an amendment to the evidence trap saying a failed re-run downgrades nothing until both shells are known to resolve the same binaries; and an extension to the denominator trap, that collapsing synonyms onto one term hides a wrong denominator rather than exposing it, so the writing code must be read on its early-return paths after any such collapse.
- 2026-08-11 - Extended `feedback_git_review_first.md` with a sequencing clause after the user pointed out that `memory/CONTEXT.md` had been committed three times in one session. Each commit was authorised, so the fault was not permission but order: a memory file was written after each commit, and every memory write adds a Revision History entry to the same tracked file, so the commit was made before the task's work was finished. The clause says to finish all pending edits first and stage once, gives the tell as a commit followed by more edits to the same file in the same task, and states explicitly that this never licenses staging without a go-ahead, because a sequencing rule sitting inside a permission file is exactly the kind of thing a later reader could take as one. The file's frontmatter description also carried an em dash and was rewritten, since strict verification failed on the touched file.
- 2026-08-11 - Recorded the decision to repair PyYAML in the hook-resolved interpreter rather than extending the `.venv` shim for the guard-coverage plan. `guard_coverage_decision_log.md` now distinguishes canonical project-runtime repair from bare interpreter repair, records that unsandboxed `C:\Python313\python.exe` was repaired with PyYAML 6.0.3 while the Codex restricted sandbox still cannot see the user-site install, and closes decision 44 around dependency repair plus same-context producer proof. `guard_coverage_review_ledger.md` gained the matching review trap: `pip install` or "Requirement already satisfied" is not proof unless `import yaml` and the relevant guard command pass in the same execution context that writes or judges evidence.
- 2026-08-11 - Corrected one stale scope claim in `guard_coverage_decision_log.md`, found by a targeted sanity review of the preceding PyYAML fix pass. The 2026-08-03 inventory-probe section opened by saying one of the escalation's premises "did not hold on this machine", which that section's own `Correction, 2026-08-11` paragraph contradicts: resolution follows the launching process environment, so the premise did hold in a session on the same machine the same day. The body heading beneath it had already been corrected to "in that session" and the lead-in sentence was missed, leaving the section self-contradicting at its most-read line. Fixed to "in that session" rather than rewritten, since the correction paragraph is the record of how the scope error was found and stays as it is. The same sweep confirmed what did not need changing: no `--repair` claim survives in the plan or either memory document, the no-shim decision reads consistently across decision 44, the stage 14 body and this file, and no `STAGE14-PRODUCER-OK` token exists in `workflows/doc-sync-guard/LOG.md`, so the stage 14 observation period has not been started.
- 2026-08-11 - Closed the `link-check --link` Revision History entry in `guard_coverage_review_ledger.md` when stage 4a landed, and retired the manual workaround it carried. The entry had stood as an OPEN DEFECT since 2026-08-04 and instructed the reader to check every file a `--link` run named, because the pass is otherwise silent about having edited history; leaving that in place would have kept a live instruction for a defect that no longer exists. The diagnosis below it is kept rather than deleted, because stage 4 and stage 9 both touch the same script and will re-read it, and the entry now also records what stage 4a deliberately did not cover: `apply_link_fix` rewrites a renamed target with a blanket `content.replace()` over the whole file, so `--fix` still has no region concept and can edit a link inside a dated entry.
- 2026-08-11 - Recorded the guard-coverage stage 14 build in the two supporting documents. `guard_coverage_decision_log.md` gained the rationale for a finding the stage recorded rather than fixed: re-deriving the replay figures reproduced the recorded numbers exactly, but classifying the clause A warnings by cause - which no earlier round had done - found that 11 of 41 are a single false-positive class, where a Revision History archived twice on the same date leaves the archive reference line unchanged and a legitimate archive-plus-append is read as an in-place edit. The entry records why it was not fixed in the stage (the plan states clause A is adopted as it stands, the behaviour predates the stage and is only made reachable by it, and the fix is a design question rather than a patch) and what was rejected: shipping clause A at INFO to avoid reporting it, which would have been a scope change made by the builder to suppress an inconvenient finding and would have discarded the clause's 27 genuine findings along with the 11 false ones. `guard_coverage_review_ledger.md` gained the general trap behind it: a warning rate quoted to justify enabling a clause is a cost only if the findings are true, so classify a sample by cause before accepting the number, and treat any frequency word in a Known Issues bullet as an unmeasured claim until something has counted it - this one had read "rare, rule-breaking combinations" since 2026-07-07 and describes the project's own normal archiving pattern.
- 2026-08-12 - Rewrote `feedback_bash_syntax_not_powershell.md` after the user asked why the PowerShell-here-string fault keeps being warned about and then fixed afterwards rather than avoided. The file was already instruction-shaped in its How to apply section but keyed to the wrong trigger: it opened on "when issuing a command through the Bash tool, use POSIX syntax", which surfaces when the topic in mind is shell choice, and the fault occurs when the topic in mind is a commit message and the multi-line string is incidental. It now leads with the trigger that actually fires - this command carries a multi-line string - and the action, with the heredoc as the first thing in the file. It records the third occurrence and the diagnosis behind it: the PowerShell tool's own instructions demonstrate the here-string form for commit messages, so the wrong template has the strongest and most task-specific cue at the moment of the mistake, which is why documenting it harder had failed twice. It also records that the rule is now enforced by hook A8 rather than by recall, that the memory is therefore no longer the last line of defence, and the general lesson that a convention which keeps failing despite being documented belongs in a hook rather than in another paragraph. The MEMORY.md index line was rewritten action-first. Its first wording measured 197 characters against the 150 guidance and was cut to 145; the count in this entry was originally written before the measurement was taken and was wrong, which is the same fault as writing a verification result before running the check.
- 2026-08-12 - `guard_coverage_decision_log.md` gained the rationale for two details the stage 14b spec left open and the user delegated to the build: an archive reference line present but carrying no readable date is treated as no archive recorded and warns, on the reasoning that every other ambiguous shape in the same function biases to a loud warning and a malformed line is the form a forged one would take; and where two reference lines exist the newest date on each side is compared, so an older line appended alongside cannot game the test. The entry states plainly that both are tightenings rather than the silencing 14b was framed around, since the old code counted any change to the line as evidence of an archive, and records that neither shape occurs anywhere in the branch history - measured, not inferred from the net figure - which is why the replay still lands exactly on the predicted 41 to 32. The review ledger needed no change: its entry records a general trap about accepting warning rates without classifying them, which outlives this fix.
- 2026-08-12 - New memory `feedback_claim_after_check.md`, written at the user's instruction after he asked why a verification sentence keeps being written into a LOG entry before the check has run, rather than accepting another correction. The file records the diagnosis rather than only the rule: an entry gets composed as one continuous piece of prose about a finished task, so the verification sentence is transcribed from the list of checks about to run; the process actively pushes that way, because doc-sync-guard requires a changed directory's LOG.md to have gained an entry and close-out hard-fails on a doc-sync WARN, so close-out cannot pass until the entries exist while the entry is meant to report the close-out result; and the rule that should have caught it is keyed to verification while the fault happens during log writing, where the cue is a neighbouring entry's "Verified:" clause waiting to be filled, which is the same shape as the PowerShell here-string diagnosis. The rule itself is two passes, facts then results. A matching clause was added to the AGENTS.md LOG.md rules, and a backlog item was added to investigate hook enforcement on the A8 pattern, since prose has now lost at this fault more than once. Backlog Active 18 to 19.
