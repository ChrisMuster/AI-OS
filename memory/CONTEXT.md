# Memory

**Last modified:** 2026-08-25

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-08-25.

- 2026-08-23 - Extended `feedback_cross_ai_review.md` with a third occurrence of the handover-sets-the-scope fault, caught by the user before the Codex review packet was handed over. The clause was "Mechanical checks already run, so Codex need not repeat them", and two softer variants went in alongside it: a stale-word list asserting "the only place these should now appear", and a heading reading "Known gaps no mechanical check covers", which implies everything unlisted is covered. What the entry records is why every existing defence failed, since the rule was written, sat in this file, was quoted in the handover's own steer, and had been read earlier in the same session: at the moment of writing, the clause feels like saving the reviewer time rather than like scope-setting, which is the disguise the rule already names. The fix applied is structural rather than another warning, the packet now opening with a statement that the user sets the scope, framing every reported result as a claim to re-test, and marking each list as a floor or as exhaustive. An escalation question is left open rather than answered, since a rule failing three times earns a mechanism under [[feedback_escalate_rule_to_hook]], but the shape is semantic rather than lexical and needs scoping before anything is built.
- 2026-08-23 - Added a trap to `sync_architecture_review_ledger.md` after discharging the Stage 0 Drive sync gate, because three instruments were tried on one question and only the third could be trusted. A scan for `ERROR` as a word matched nothing, since Drive appends severity as a single letter to the timestamp, and a zero there reads exactly like a clean log; counting `ZE` lines then gave errors nearly equal to info, a count that is correct but useless because Drive logs status polls and shell-IPC chatter at error severity. What worked was aggregating by source function. The trap also carries the general form of the problem, which is the more reusable half: when a finding is the *absence* of errors over a period, the logs for that period must be shown to exist, be continuous, and contain errors of other classes, or the absence is only a log nobody wrote. Extends the existing watcher trap in the same file and relates to [[feedback_verify_the_checker]].
- 2026-08-23 - Recorded the sync architecture correction pass in the item's two companion documents and updated both of its backlog items. The decision log gained the six decisions the user took one at a time after the cross-AI review, with the reasoning for each and the two places the first recommendation was withdrawn under his questioning: force-adding the wiki raw metadata was described as impossible when it is merely inadvisable, and the related spike plan was proposed for a historical banner before reading it in full showed it holds settled design existing nowhere else. It also gained the indexer constraint the review did not raise and the user did, that both the audit and the knowledge-graph indexer prune by plain `git check-ignore`, so the second repository must never become the default git context, together with the pre-build graph baseline of 317 nodes, 1288 edges and 9 broken references. Its artefact-roles table now carries the spike plan as the item's tactical plan and a second review baseline for it. The review ledger gained three traps, the load-bearing one being that a plan absorbed into a larger design rots outward-first, its claims about itself staying correct while its claims about its surroundings all fail at once, which is why a blanket judgement on such a document is wrong in both directions. The ledger now holds eleven traps. Both backlog items had their plan pointers corrected rather than their status narrated.
- 2026-08-23 - Second pass over the same two companion documents, after the superseded basic-python wiki was archived and the knowledge-graph question that ran alongside it was settled. The decision log's decision 3 gained the result of the content diff it had promised, and its indexer baseline was corrected from 317 nodes, 1288 edges and 9 broken references to 318, 1290 and 8; the figure quoted in the entry above was accurate when written and is superseded by this one rather than edited, since the earlier entry records what was true at that point. The correction is written up rather than swapped silently because it is the first exercise of the new baseline rule and it failed instructively: nodes and broken references matched the prediction exactly, while the edge count was wrong by two, both edges created by the Contents entry the archiving procedure itself required in the parent wiki index. The review ledger gained a trap for that, that a task's own documentation edits move the graph as surely as its filesystem changes do, plus an update marking the basic-python instance closed while its trap stands. The ledger now holds twelve traps. The decision log also records a rule collision found while following the archiving procedure: its step 5 calls for a README status-tag update while the wikis exception forbids listing individual wikis there, and the exception wins.
- 2026-08-24 - Added `reference_codex_biblio_tools_retry.md` after Codex treated the initial absence of `mcp__biblio_tools` as unavailable during startup even though the namespace appeared after tool discovery. The new memory records the pre-emptive action: discover, wait briefly, retry `verify_setup` and `get_timestamp`, and only then report `BIBLIO_TOOLS_NOT_AVAILABLE` if the live tool palette still lacks the namespace.
- 2026-08-24 - Recorded the sync architecture correction pass in three memory files after the user decided all eleven review findings and five sign-off items individually. The decision log gained a section for the pass, carrying the two review findings that were wrong as diagnosed and would have made the documents worse if acted on, a repository naming collision that no reviewer found, the correction of its own false claim that Google Drive could not reach the project's git directories, with the measured figures, and the user's reframing that the A-versus-C classification is a size question rather than a privacy one, since nothing in either category reaches the public repository. Its backlog-backups section was rewritten from an open question to a settled split, recording that the deferral it proposed was never available because git does not un-include history. The review ledger gained four traps, the load-bearing one being that a clean check is not evidence unless it could have failed for the reason it is cited against, which came from a connectivity-only integrity check that skipped the exact failure mode being ruled out. The ledger now holds sixteen traps. `feedback_plans_are_personal.md` had its distillation bullet reworded: it called a roadmap file a tracked reference document while the rule above it gitignores that same filename, and reading the bullet rather than the rule produced a proposal to publish a file that had never been tracked in any branch.
- 2026-08-24 - Second pass over the sync architecture companion documents, after re-deriving the Stage A scope table exposed a contradiction that reading the rules against each other had not found: the classification claims every file in the authored personal folders while the allowlist excluded every media and PDF file, with six files sitting in the gap. The decision log gained a section for the decision, which went against the recommendation offered. It records both halves the user rejected, that a permanence argument about git history belongs to a published repository rather than a private one, and that excluding the files would have left the only irreplaceable material in the project with no history and no backup, since the derived category holds bulk material handled elsewhere plus things that regenerate. It also records the misunderstanding the recommendation created, that exclusion would have meant relocating the files, when they would simply have stayed unprotected where they are, and that the fix was deliberately widened to all four authored personal folders because the exclusion cut across them identically. The accompanying guard is a file-size flag on the pre-seed gate, set from measurement at twenty times the largest file currently in scope, per the user's condition that it must not fire on present content. The review ledger gained two extensions rather than new traps: that a measuring script inherits the authority of a measurement while still being assumptions in code, after the script counted nine tracked placeholder files it should have excluded, and that the broad-rule-meets-narrow-rule class has now resolved once in each direction, so the outcome is not predictable from the shape.
- 2026-08-25 - Recorded the sync architecture repair pass across three memory files, after a blind eight-reviewer round found that almost none of the 2026-08-24 decisions had reached the artefacts that implement them. The diagnosis is one fault repeated: the previous pass rewrote the sentences describing each rule and left the patterns, tables and commands untouched, so "the authored personal folders take every file type" was written in four places while the pattern beneath it still read `*.md` and would have excluded the six files the decision was taken to protect. `sync_architecture_review_ledger.md` gained three traps and now holds nineteen: that rewriting prose is not rewriting the rule, with the five-step sweep that closes it; that a pattern can be correct when written and wrong later without anyone editing it, after archiving a wiki put a fifth `raw/` folder one level deeper than the exclusion reached; and two further instances of the broad-rule-meets-narrow-rule class, taking it to four, all of them found by classifying every ignored path by script in seconds where four rounds of careful reading had found two. Its count trap was extended to record that the script it named as the remedy, `scope_measure.py`, was never in the project and was gone by the next review, so three documents carried an instruction to re-run something unrunnable. `sync_architecture_decision_log.md` had its "disposable backup" reasoning struck through and corrected, since it assumed several machines and there is one, making the Drive copy the only off-machine copy; its authored-text size figure was made non-numeric on the user's instruction that a figure which has to be maintained does not belong in a plan. `feedback_plans_are_personal.md` had the personal-repository claim removed from its distillation bullet, which had been added ahead of the AGENTS.md wording decision and left the file contradicting its own opening sentence; the open question is recorded there for the user rather than settled by a session.
- 2026-08-25 - Added `feedback_rewrite_the_rule_not_the_prose.md`, the general form of the fault the eight-reviewer round exposed, keyed so it fires whenever a decision is applied to a document rather than only on this item. It states that a rule change is finished only when the artefact that executes it has changed, not when the sentences describing it have, and gives the five-step sweep with the re-read at the end that the user added: apply, run the change vector, search for other statements of the same rule, fix those and their vectors, then re-read for what the change broke and revisit the decision if it broke something. The example is load-bearing rather than decorative, since the 2026-08-24 pass rewrote both plans after eleven findings and five sign-off items were decided individually and almost none of the decisions reached the patterns, so the file records the user's framing that rewriting a document means rewriting how it works. It also carries the two supporting habits learned the same day, that a rule collision is machine-detectable and should be classified by script rather than by reading rules against each other, and that a pattern can be correct when written and wrong later because its correctness is a fact about the tree rather than about the pattern.
- 2026-08-25 - Reworded `feedback_plans_are_personal.md` and its `MEMORY.md` line after the user settled the second of the four open items, that design-time documents may be versioned privately but must never reach the public repository. The file said "never staged or committed" and "local-only", which was written when a file could only be in the public repository or nowhere, so the two meant the same thing; the rule was always about publication rather than about the act of committing. His framing is recorded as the operative one rather than the reasoning first offered, that he does not mind these documents being tracked and committed privately and minds them being published. The open question this file carried since earlier the same day is closed rather than deleted, so a reader arriving from the earlier note finds the resolution. AGENTS.md was changed in the same pass, outside `memory/`: rule 6 now reads "never published", states that the private repository is where these documents are meant to live, and records why the old wording misled; rule 4 was scoped to the public repository's history; and the Personal data isolation preamble now defines "tracked" as "tracked by the public repository" once, so every rule in that section inherits it rather than each needing its own qualifier. The build spec's step 13 was updated to record the rewording as done ahead of the build rather than owed after it, which was the ordering defect: the seed commit at step 8 would have breached a rule whose correction sat at step 13.
- 2026-08-25 - Recorded the third of the four open items in `sync_architecture_decision_log.md`: the Drive backup is pushed automatically, closing a contradiction rather than opening a question. Section 5 of the plan said the personal repo pushes fully automatically on a real event while Stage D said the Drive remote is used deliberately from one machine at a time, and with the hub deferred those two sentences described the same push in opposite ways. The Stage D wording was the stale half, surviving from when Drive was to be the transport between two machines, and read literally it made the only off-machine copy of the personal repository depend on somebody remembering to push it. The constraint underneath it was kept but converted from advice into a gate, since the corruption test was two machines pushing concurrently to a bare repo in a Drive folder and there is one machine. The entry also records the more useful half of the exchange, which is that the user had understood the hub to be the thing on Drive and reading the plan had not corrected him: it said two bare repositories where there are three, showed only the hub in its diagram, and never stated plainly that the backup is on Drive while the hub deliberately is not. His reading was reasonable and the document was at fault, so section 4 was rewritten to name all three in a table with their locations and the tested reason for the split. His ability to say push it now at any moment is confirmed as the `shadow push` trigger and recorded as a stated requirement rather than a convenience. The rejected-options table had disposable removed from its Drive row for consistency with the correction made earlier the same day.
- 2026-08-25 - Moved the knowledge-graph baseline in `sync_architecture_decision_log.md` from 318 nodes, 1290 edges and 8 broken references to 319, 1290 and 7, and recorded the second exercise of the baseline rule beside the first. The movement was found by the rule rather than noticed by eye, and it is fully accounted for: creating `workflows/personal-data-guard/config/denylist.txt` earlier the same day added that file as a node and at the same time resolved the broken reference the guard's own CONTEXT.md had always made to it, leaving the edge count unchanged because the edge existed already and was merely unresolved. What makes it worth writing up is that the cause had nothing to do with the work being measured: the file was created to close a personal-data leak and moved the graph as a side effect. Both exercises of the rule so far have caught a movement whose cause was something other than the change under review, which is the argument for keeping it. The earlier 318 figure is dated rather than overwritten, so the entry above still reads as true when it was written.
- 2026-08-25 - Added `feedback_user_overrides_rules.md` and recorded the fourth and last open item in `sync_architecture_decision_log.md`. The new memory states the principle the user gave in his own words while overriding the archiving rule: the project rules bind Biblio and not him, so an instruction that conflicts with one is surfaced once with the rule quoted and then carried out, never weighed for permissibility and never quietly obeyed as though no rule existed. It records that he was explicit about not conceding the rule was wrong, so an override is his decision rather than a precedent and the rule stays in force for every other case and for Biblio always. It is written as the other half of [[feedback_check_rules_before_exempting]], which covers what Biblio may not do on its own authority, where this covers what happens when the authority is his. The decision log gained a section for two decisions from one question: every `raw/` folder behaves identically whether its wiki is live or archived, rejecting the alternative because it would have turned archived into no longer backed up; and `wikis/archived/basic-python/` is to be deleted rather than kept, sequenced behind Stage A so the deletion is recoverable rather than permanent. The evidence was re-derived rather than inherited from the August diff, finding all 32 files have counterparts, 18 byte-identical, and no unique content beyond stale date lines and navigation headings. The general answer also surfaced a gap nobody had raised, that archiving a wiki after Stage B moves a junction while its datastore folder stays put, so the archiving procedure gains two steps.
- 2026-08-25 - Moved the correction narratives out of both sync plans and into `sync_architecture_decision_log.md`, after the user asked whether the plans were growing again for the reason the split was meant to prevent. Measured rather than asserted: the plans had grown 486 lines this session against 147 in the two companions, which is the wrong ratio, and roughly ten and sixteen per cent of each plan was history rather than build instruction. The log gained a table of what each plan used to say and what it says now, so nothing was deleted. The correction to my own argument is recorded because the user challenged it and was right: I had said moving history to the log risks it being missed, and the evidence against that is this very item, where the log held every decision and the decisions still failed to reach the patterns, so location was not the variable and the test is what caught it. The distinction actually used is classification rather than location: a constraint on a future edit is build instruction and stays in the plan phrased as an instruction, while the same fact told as a story belongs in the log. Two blocks were kept in the plans on that basis, the stage letters not being re-lettered and the five stage 6 references pointing at another plan, since deleting either would let a reader revert a correct thing.
