# Memory

**Last modified:** 2026-08-27

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-08-27.

- 2026-08-25 - Reworded `feedback_plans_are_personal.md` and its `MEMORY.md` line after the user settled the second of the four open items, that design-time documents may be versioned privately but must never reach the public repository. The file said "never staged or committed" and "local-only", which was written when a file could only be in the public repository or nowhere, so the two meant the same thing; the rule was always about publication rather than about the act of committing. His framing is recorded as the operative one rather than the reasoning first offered, that he does not mind these documents being tracked and committed privately and minds them being published. The open question this file carried since earlier the same day is closed rather than deleted, so a reader arriving from the earlier note finds the resolution. AGENTS.md was changed in the same pass, outside `memory/`: rule 6 now reads "never published", states that the private repository is where these documents are meant to live, and records why the old wording misled; rule 4 was scoped to the public repository's history; and the Personal data isolation preamble now defines "tracked" as "tracked by the public repository" once, so every rule in that section inherits it rather than each needing its own qualifier. The build spec's step 13 was updated to record the rewording as done ahead of the build rather than owed after it, which was the ordering defect: the seed commit at step 8 would have breached a rule whose correction sat at step 13.
- 2026-08-25 - Recorded the third of the four open items in `sync_architecture_decision_log.md`: the Drive backup is pushed automatically, closing a contradiction rather than opening a question. Section 5 of the plan said the personal repo pushes fully automatically on a real event while Stage D said the Drive remote is used deliberately from one machine at a time, and with the hub deferred those two sentences described the same push in opposite ways. The Stage D wording was the stale half, surviving from when Drive was to be the transport between two machines, and read literally it made the only off-machine copy of the personal repository depend on somebody remembering to push it. The constraint underneath it was kept but converted from advice into a gate, since the corruption test was two machines pushing concurrently to a bare repo in a Drive folder and there is one machine. The entry also records the more useful half of the exchange, which is that the user had understood the hub to be the thing on Drive and reading the plan had not corrected him: it said two bare repositories where there are three, showed only the hub in its diagram, and never stated plainly that the backup is on Drive while the hub deliberately is not. His reading was reasonable and the document was at fault, so section 4 was rewritten to name all three in a table with their locations and the tested reason for the split. His ability to say push it now at any moment is confirmed as the `shadow push` trigger and recorded as a stated requirement rather than a convenience. The rejected-options table had disposable removed from its Drive row for consistency with the correction made earlier the same day.
- 2026-08-25 - Moved the knowledge-graph baseline in `sync_architecture_decision_log.md` from 318 nodes, 1290 edges and 8 broken references to 319, 1290 and 7, and recorded the second exercise of the baseline rule beside the first. The movement was found by the rule rather than noticed by eye, and it is fully accounted for: creating `workflows/personal-data-guard/config/denylist.txt` earlier the same day added that file as a node and at the same time resolved the broken reference the guard's own CONTEXT.md had always made to it, leaving the edge count unchanged because the edge existed already and was merely unresolved. What makes it worth writing up is that the cause had nothing to do with the work being measured: the file was created to close a personal-data leak and moved the graph as a side effect. Both exercises of the rule so far have caught a movement whose cause was something other than the change under review, which is the argument for keeping it. The earlier 318 figure is dated rather than overwritten, so the entry above still reads as true when it was written.
- 2026-08-25 - Added `feedback_user_overrides_rules.md` and recorded the fourth and last open item in `sync_architecture_decision_log.md`. The new memory states the principle the user gave in his own words while overriding the archiving rule: the project rules bind Biblio and not him, so an instruction that conflicts with one is surfaced once with the rule quoted and then carried out, never weighed for permissibility and never quietly obeyed as though no rule existed. It records that he was explicit about not conceding the rule was wrong, so an override is his decision rather than a precedent and the rule stays in force for every other case and for Biblio always. It is written as the other half of [[feedback_check_rules_before_exempting]], which covers what Biblio may not do on its own authority, where this covers what happens when the authority is his. The decision log gained a section for two decisions from one question: every `raw/` folder behaves identically whether its wiki is live or archived, rejecting the alternative because it would have turned archived into no longer backed up; and `wikis/archived/basic-python/` is to be deleted rather than kept, sequenced behind Stage A so the deletion is recoverable rather than permanent. The evidence was re-derived rather than inherited from the August diff, finding all 32 files have counterparts, 18 byte-identical, and no unique content beyond stale date lines and navigation headings. The general answer also surfaced a gap nobody had raised, that archiving a wiki after Stage B moves a junction while its datastore folder stays put, so the archiving procedure gains two steps.
- 2026-08-25 - Moved the correction narratives out of both sync plans and into `sync_architecture_decision_log.md`, after the user asked whether the plans were growing again for the reason the split was meant to prevent. Measured rather than asserted: the plans had grown 486 lines this session against 147 in the two companions, which is the wrong ratio, and roughly ten and sixteen per cent of each plan was history rather than build instruction. The log gained a table of what each plan used to say and what it says now, so nothing was deleted. The correction to my own argument is recorded because the user challenged it and was right: I had said moving history to the log risks it being missed, and the evidence against that is this very item, where the log held every decision and the decisions still failed to reach the patterns, so location was not the variable and the test is what caught it. The distinction actually used is classification rather than location: a constraint on a future edit is build instruction and stays in the plan phrased as an instruction, while the same fact told as a story belongs in the log. Two blocks were kept in the plans on that basis, the stage letters not being re-lettered and the five stage 6 references pointing at another plan, since deleting either would let a reader revert a correct thing.
- 2026-08-27 - Recorded the review-baseline convention explicitly, in `review_process.md` as a six-step baseline cycle and in `feedback_gitignored_plan_baselines.md` as the same cycle for the `CODEX-*` copies, after the user asked why a baseline would be refreshed before a review round and pointed out that doing so would make every diff empty. He was right and the documents were at fault: the shared process said only to refresh "when the current state is the new handoff baseline for the other AI", which is ambiguous enough to be read as "refresh at handoff" and had been, both in a handover instruction and in a session summary that repeated it. The rule is now stated as a numbered cycle whose load-bearing clause is that the baseline is refreshed at the *end* of a review round and never at the start, so drift between a plan and its baseline is the normal state for most of an item's life and is the deliverable rather than a defect. Both memories cross-reference the cycle so neither can drift alone, and the check that had encoded the opposite convention was inverted in the same pass. The decision log for the sync architecture also gained the third exercise of the knowledge-graph baseline rule, which caught a movement caused by the correction pass on the plan itself.
- 2026-08-27 - Rewrote the indexer-baseline section of `sync_architecture_decision_log.md` after the user asked whether the figure going stale had actually been fixed or only patched. It had been patched: the number was updated and the surrounding sentence reworded, which is the prose-not-the-rule failure the review ledger names as its most expensive lesson. The entry now records the fourth exercise, in which a figure written into the plan went stale within two hours because the same session added a test file and a Dependencies line, and states the structural diagnosis rather than the incident: the plan held a standing figure while the stage step list already required a fresh reading, so there were two baselines for one purpose that could only disagree, and the comparison the rule actually needs is the start of a stage against the end of the same stage, minutes apart. A figure stored weeks earlier measures the interval instead. The plan now carries no figure at all and the reading is recorded in the stage's own `LOG.md`. The entry also corrects an overstatement it had inherited: all four exercises were described as the rule catching a real movement, which is true but incomplete, since every one was benign and none found a defect, so the value sits entirely in the within-stage comparison rather than in watching the number between sessions.
- 2026-08-27 - Gave the shared review process a fourth companion artefact, the review packet at `memory/<item>_review_packet.md`, after a review round's findings were destroyed by the handoff mechanism itself. The findings were written into `HANDOVER.md` on 2026-08-26 and the next handover overwrote that file the same afternoon, one working day later, leaving the whole of Tier 3's input recoverable only from a session archive. The diagnosis the user reached is the one that changed the design: he had assumed each handover carried the review material forward cumulatively, which is a reasonable assumption and is what should have happened, so the fault is a container with the wrong lifetime rather than a session that forgot. A handover has a session's lifetime and a review round has a multi-session one. `review_process.md` gained the packet as an artefact role, an After-review step that updates the packet before the handover is written so the packet stays the record, a closing condition that a round ends only when every finding is addressed or dropped, and two corrected rows in its What-Belongs-Where table. Its "Review Packet Checklist" heading was renamed to "Handoff Checklist", since the packet is now a file and two things in the one document that defines both should not share a name; three of its bullets were reworded at the same time, because "checks already run, so the reviewer need not repeat them" and "known gaps no mechanical check covers" are both the handover-sets-the-scope fault that [[feedback_cross_ai_review]] records. The user explicitly rejected the stronger form of the fix that was offered, a gate refusing to produce a handover while findings are open, on the grounds that a handover is exactly what you need in order to cross a session boundary with work outstanding; the obligation is now to record the open count and point at the packet, never to withhold the document. `backlog.md` carries the same rule for consistency. `sync_architecture_review_packet.md` was created as the first instance, reconstructed from the session archive with its three recovery limits stated in the file rather than glossed: the original 1-to-19 numbering is not reproducible and the items are renumbered R1 to R14, the requested diagnosis of why the defects survived several passes is genuinely lost, and the transcript holds narration rather than tool output so no verbatim copy of the handover exists anywhere.
- 2026-08-27 - Added a backlog item for link-check inserting self-links, at the user's instruction after a first reading of his message took "I added it to the backlog" as him having done so when he meant it as an instruction. The defect was found by accident: the link pass was run in `--link` write mode when only its read-only audit was needed, and three of the insertions it made pointed a Contents line at the CONTEXT.md that line already sits in. All five files it touched were reverted, so the item records a behaviour rather than a change left in the tree. Active 21 -> 22, checked by backlog-guard against a snapshot taken before the edit.
- 2026-08-27 - Appended a declined item to `feedback_journal_scan_ask_once.md`, covering a household member's temporary absence surfaced by the session-startup journal scan and declined by the user in the same session, per the rule in that file that a decline is recorded immediately so it never resurfaces. The reason is recorded with the item rather than left implicit, that USER.md already carries the standing arrangement the absence falls under, so the specific dates are a schedule detail. The list now holds eleven declined items.
- 2026-08-27 - Recorded Tier 3 of the sync architecture correction across the review packet and the decision log. Seven of the eight open findings moved from Open to Addressed in `sync_architecture_review_packet.md`, each naming where it was fixed, leaving one that is blocked on a decision rather than on work; the Status line carries that split so the next session reads the state from the packet rather than from a handover. This was the packet's first live exercise, and it held: everything Tier 3 needed was in it and nothing had to be recovered from a session transcript, which is the failure it was built on 2026-08-27 to prevent. The decision log gained a dated correction rather than a rewrite, because the fact it records expired rather than having been wrong. Its sweep of 2026-08-24 established that a hand-authored config file did not exist on this machine; the file was created the following day, and both plans went on asserting its absence for two more days with nobody editing either claim. That is the "right when written, wrong later" shape applied to a fact rather than to a pattern, so the original sentence is left dated and the expiry is noted beneath it.
- 2026-08-27 - Closed the last open finding of the sync architecture review round in `sync_architecture_review_packet.md` and recorded that the round is nonetheless not closed. The distinction is the point of the entry: all fourteen findings are addressed, and the closing verification pass was not run because the session lost shell access part-way through, so the packet now carries a "what Tier 3 still owes" section listing the five checks that must run before anyone reports the round as clean, with the build simulation called out as never having been run against the current text. Recording an unrun check as unrun rather than as expected-to-pass is the whole obligation under [[feedback_claim_after_check]], and the packet is the right place for it because a handover would carry it for one session while this round has outstanding work across several. The finding itself is recorded with its reasoning rather than only its outcome, since the fixture-versus-real-file choice will look arbitrary later: three of the four folders it covers contain no file of the relevant type today, so the tree cannot test the rule for them at all, and a real-tree control cannot tell a broken pattern from a deliberate deletion while gating a commit. The user's follow-up question produced an addition beyond the finding, also recorded there: a check does not need to know intent, because a file that lost protection is still on disk and a deleted one is not.
