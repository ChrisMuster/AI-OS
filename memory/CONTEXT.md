# Memory

**Last modified:** 2026-08-30

## Purpose

Project-scoped persistent memory for Book Dragon. All memory lives here rather than in the per-user Claude cache so it syncs with the project and is available on any machine that opens this folder.

## Contents

- `CONTEXT.md` - this file. Structural documentation; tracked in git.
- `MEMORY.md` - the index. Loaded at every session start. One line per memory, under 150 chars each. Not tracked in git; created on first-run.
- `LOG.md` - append-only journal of all memory changes. Not tracked in git; created on first-run.
- `backlog-backups/` - `memory/backlog-backups/` [[memory/backlog-backups/CONTEXT]] - Local exact-copy snapshots of `memory/backlog.md`, maintained by backlog-guard. Not tracked in git.
- Individual memory files, named `<type>_<short-name>.md`. Types: `feedback`, `reference`, `user`, `project`. Not tracked in git.

## Inputs

None. Memory files are written by Biblio during sessions as relevant facts emerge from conversation.

## Outputs

None. This directory is a store, not a workflow.

## Steps

N/A. This is a store directory, not a workflow. See the "Project memory" rule in `AGENTS.md` [[AGENTS]] for the full read procedure, write rules, and four-step write procedure.

## Dependencies

- `AGENTS.md` [[AGENTS]] (root) - defines the session startup sequence and all rules governing when and how to read and write memory.

## Known Issues

- The old per-user Claude cache at `~/.claude/projects/<sanitized-cwd>/memory/` may still exist on machines where it was previously written to. New writes must land in this directory. If a conflict arises, this project-scoped memory takes precedence.

## Revision History

Earlier history archived to LOG.md on 2026-08-30.

- 2026-08-28 - Replaced `sync_architecture_review_packet.md` with the Codex CLI re-review findings from the 2026-08-28 blind review of the sync architecture plans. The packet now records six open findings for Claude: stale packet text claiming the handoff reader defect is still unfixed; a Stage A acceptance-table row proving a post-seed tracked claim with a pre-seed selection instrument; remaining permission-shaped wording in the shadow plan; a present-tense reference to the old media/PDF exclusion; an R6 exception that is correct for `.obsidian` but too broad as a future invariant; and a process defect in `review_process.md` that let Codex confuse "review complete" with "findings resolved" and initially give the wrong baseline-refresh instruction. The packet states that Codex refreshed both `CODEX-*` baselines after completing the review, so Claude's correction pass leaves them untouched and creates the next diff surface.
- 2026-08-28 - Worked the Codex CLI re-review's six findings and recorded the round as corrected in `sync_architecture_review_packet.md`, which is rewritten rather than appended to because the packet is rolling within its item. Two of the six landed here rather than in the plans. `review_process.md` gained a three-states table naming who acts and what happens to the baseline in each, because its baseline cycle had blurred review completion with finding resolution: one part said the refresh follows the reviewing AI finishing its read, another said the round closes only when every finding is addressed and the refresh belongs to that closing, and a reviewing AI followed the second and withheld a refresh it owed. The two events are now stated as separate, with different owners, and the closing sentence records what it said before. And `feedback_diagnose_review_misses.md` gained check 26, the diagnosis the user asked for: five of the six findings were created or left standing by the correction pass of the previous 48 hours rather than by the original authoring, so the lesson is not a new failure class but a location, that corrections are the one body of text never swept because the fix pass ends where the sweep would start. The check is to re-run the change-vector sweep over the diff the correction pass produced. Every individual finding was a repeat of an existing check, and the table in the packet names which one for each.
- 2026-08-28 - Added a declined journal-to-USER.md startup candidate to `feedback_journal_scan_ask_once.md`, at the user's instruction, so the same relationship mention is not raised again in later sessions. USER.md was not changed.
- 2026-08-28 - Replaced `sync_architecture_review_packet.md` with the Codex CLI review findings for Claude's correction pass. The packet now separates plan-readiness findings from review-process findings, records the two plan wording fixes Codex made, and leaves the review-process defects open for Claude.
- 2026-08-28 - Closed the review-process half of that round, sweeping the change vector rather than patching the two surfaces the round named. `review_process.md` gained two sections and had a third rewritten. The first requires plan-readiness and review-process findings to be split in the packet, the handover and any log entry reporting the round, and states the trap that instruction creates: the packet reader counts a `###` heading as a finding, so splitting inside one `## Open` section would add phantom findings, and the supported split is two `##` sections whose first word is an open-set synonym. The second states that a baseline belongs to the AI whose name it carries and is refreshed by no other, which the cycle had never covered because it was written for a single reviewer; whether a second reviewer should get its own baseline is left as an open question for the user rather than settled by creating a file. The packet-layout section was rewritten after being checked against `workflows/handoff/scripts/gather.py` instead of read for sense: it had promised that headings could be "anything reasonable" and labels could take "any form", where the reader matches a closed set of six open and seven addressed first words, requires an `##` heading, skips any heading containing "question", and accepts one to four uppercase letters immediately followed by digits. That section is the only written statement of the format, so its overclaim could have produced the same zero-open failure it was added to prevent. `feedback_gitignored_plan_baselines.md` had step 6 corrected and its "do not refresh just before handing the plan over" clause disambiguated, and now names both failure directions rather than only the early one. `sync_architecture_review_packet.md` was rewritten in the split form it now mandates, recording six addressed findings and no open ones.
- 2026-08-28 - Added checks 27 and 28 to `feedback_diagnose_review_misses.md`, the diagnosis the user asked for when he pointed out that the P1 and P2 defects should have been fixed by the pass that created them rather than found by the next review. Check 27 is that a mechanism's documentation is written from the implementation and never from the change's purpose: the packet-layout section was the commit subject "Let a review packet be written in any shape" expanded into prose, so it promised a breadth the reader does not have, in the one document that exists to tell an outside reviewer how to write a packet that reads. Check 28 is that a count of corrected sites is a claim about a search's territory, evidenced from commit 3d48caa, whose message says "All five sites now say" and whose five sites are all inside one workflow directory; a sweep for the sentence rather than around the edit found four more, including two in a different item's live plan where the rule was inverted outright rather than merely ambiguous. Both are the same 48 hours that produced check 26, and are recorded as extensions of it rather than as new failure classes.
- 2026-08-28 - Replaced `sync_architecture_review_packet.md` with the Claude Code review round of the two sync architecture plans, run as a full cold read plus two subagents, one simulating the Stage A build against a throwaway fixture and one verifying every factual claim against the tree. 28 open findings, all plan-readiness, split across four `## Open - ...` sections that the packet reader merges into one count. The verdict is that the personal repository is buildable and was proved working in simulation, while `boundary.py` is not buildable from its specification and three of the eight Stage A acceptance rows are wrong with a fourth missing. The packet also records why earlier rounds missed all of it: every previous round checked whether the documents were internally consistent and none checked whether they were executable, and each blocking finding lives in a script that does not exist yet, where a defect cannot be found by reading the instructions for writing it. The user's correction to that framing is recorded as the one to keep, that a document which cannot be built from does not make sense either, so they are one test rather than two. The fix is planned as two passes, the first ending in a buildable state on the live documents and the second cutting them on a copy with the fat versions kept for rollback, and the packet carries the user's scope rule that nothing inside the two plans is out of scope and the whole document is re-checked after any change.
- 2026-08-29 - Recorded a declined journal-to-USER.md startup candidate in `feedback_journal_scan_ask_once.md` and added the age filter the user gave with it. The item was a mid-August journal passage reading as a clarification of a family relationship USER.md already records; his ruling was that it does not belong there. The declined item itself is named only in that gitignored file, never here. The general steer is the more useful half and went into the How-to-apply section rather than only onto the declined list: the scan re-reads the whole current month every session, so an entry three weeks old has already passed the scan roughly twenty times without being raised, and if it was not raised then it is probably not worth raising now. The rule is therefore to surface from the recent end of the month by default, and to raise an older entry only when it is durable context that changes how Biblio works, not merely because it matches a tracked category and is absent from USER.md. USER.md was not changed.
- 2026-08-29 - Removed the derived half of the review packet's status block rather than correcting it again, and wrote the general form into `review_process.md` and the review ledger so it governs the next packet rather than this one. The packet's status block had said "findings recorded, none yet fixed" while three Addressed sections a few hundred lines below recorded twenty-five fixes made that morning. The diagnosis is what changed the fix: nobody had been careless, the same fact was simply written twice with nothing connecting the copies. Moving a finding between sections is the fix pass and feels complete when done, while the restatement is invalidated by that edit and gives nobody a reason to go and touch it, so a second copy of a derived fact has no moment at which it must be updated and can only be caught afterwards by something reading both. A check would compensate for the duplication rather than remove it. Which findings are open and addressed is now stated only in the sections the handoff reader parses, with the gather command named as the way to get the count; the block keeps only what the sections cannot encode, which is the pass the work is in and whether a build is authorised. `review_process.md` gained the standing rule and `sync_architecture_review_ledger.md` the durable trap, filed as the mirror of the existing entry about counts with no reproducible derivation: that one is a figure nobody can re-derive, this one a figure that is derivable and was stored anyway. The same sweep found the `MEMORY.md` index line still describing a Codex round of R1 to R14 that had been replaced, so it now describes what the file is rather than what it currently holds.
- 2026-08-29 - Created `sync_architecture_simulation_baseline.md`, the record of what a builder actually produced from the fat sync plans, so the same exercise against the cut plans can be compared against it rather than the cut being assumed safe. The user's idea, and it targets the stated risk of the largest open finding, that deleting the plans' narrative invites back the regressions the narrative was preventing. Two design points are recorded with it because both change how it is used. The primary instrument is not a diff of the two builds but the list of choices the builder had to make that the documents did not specify, seven of them from the fat plans, because that list is a fact about what the document fails to say and therefore survives a builder who remembers the fat version, where a code diff does not. And the file is sealed and deliberately kept out of the review packet: the user's original sequence had the reviewing AI diff against it while building, which would have had it read the answer key first and reproduce the baseline rather than derive it, so the build now happens blind and the file is opened only afterwards. The packet carries a pointer saying the baseline exists and must not be opened before building, and `MEMORY.md` gained a line marked SEALED for the same reason.
- 2026-08-29 - Swept `sync_architecture_review_packet.md` for accuracy after a session-startup read found it asserting that a step had not been run while a section above it recorded that it had, and fixed six stale statements. The entry is worth keeping for the split rather than the count, because the six are two classes needing opposite fixes. Two were derived facts stored a second time, which the standing rule in `review_process.md` already covers and which had simply not been applied to these instances; both were deleted, one clause and one duplicated statement of which pass the work is in, leaving the copy a reader meets first. The other four were true when written and expired later, which de-duplication cannot fix because there is only one copy: a verdict written in the bare present tense that a later step overturned, a claim that both live plans were byte-identical to the reviewing AI's baselines when they now differ by +402/-69 and +151/-54 lines, a line-count finding stale for the second time in two days, and this file's own record of a checker's INFO count. The convention applied to all four is the project's existing one for a fact that expired rather than was wrong: leave the statement dated and point at what supersedes it, rather than rewriting what the round found. Two took the further step of replacing a stored figure with the command that measures it. The last one is recorded in the packet as the shape of the exercise: the adjudication of the checker's findings quoted the flagged phrases, so the checker matched its own commentary and the count moved with every rewording, which makes it a figure that cannot be stored accurately at all. Verified, appended after running: the handoff gather packet still reads the file as 3 open and 32 addressed, which is the control that none of the edits broke the reader; `doc-verify --check --strict` reports 0 FAIL and 0 WARN with 5 INFO, all five sitting inside findings and adjudicated as correct or as false positives, against the 0 FAIL, 0 WARN and 8 INFO it reported before the sweep; the targeted context audit for this directory, encoding-guard, personal-data-guard, ai-style-guard and doc-sync-guard all report 0 findings.
- 2026-08-29 - Recorded pass 2's first cut in `sync_architecture_decision_log.md` and `sync_architecture_review_ledger.md`, ahead of removing 289 lines from the live sync plan, so the removal is not later mistaken for a loss. The decision log gained two sections. The first records what the hardware section held, which of it was already recorded elsewhere (price provenance, the RAID and replication limits, the reciprocal two-machine shape, the banned "or similar" condition), and the three things that existed only there and are therefore kept: which half of a deferred stage's work survives its eventual rewrite, the list of questions the hardware research never answered, and the fact that the hardware project itself is tracked outside this plan and is not owned by it. It also records that one subsection was deleted as a duplicate rather than as narrative: the plan's build-order section states the order in a table and says in as many words that the order is stated there and nowhere else, while the hardware section carried a second numbered copy of it, which is the derived-fact duplication of [[review_process]] appearing in a plan rather than in a packet. The second section records why the cut nearly went wrong, and the ledger gained the reusable form as a new trap. A finding written the previous day instructed a cut by line range; the document grew by 333 lines above that range before the cut ran, so the range had come to hold four build stages and two whole sections instead of the one section named. Nothing was wrong when written: the upper bound had been the document's total line count. The trap is that this failure is silent in the dangerous direction, because a stale line number still resolves to real and plausible text where a stale path would error, and the offset is invisible from either end since nothing records the length the citation was measured against. A second citation in the same finding set was wrong when written rather than stale, having never said what it was cited for in either the live document or the untouched baseline, and survived three review rounds for the same reason. The check recorded is to address a passage by its section heading, which survives insertion, and to re-locate any inherited range by name before acting on it. Verified, appended after running: the targeted context audit for this directory reports 0 failures, 0 warnings, 0 degraded and 0 info; `doc-verify --check --strict` reports 0 FAIL and 0 WARN for both changed memory documents and for both plans; the workflow's own checks report 36 PASS and 0 FAIL with its two suites running 51 and 23 tests, all passing; encoding-guard, personal-data-guard and doc-sync-guard all report 0 findings.
- 2026-08-29 - Added `feedback_documents_are_maintained_not_grown.md` and indexed it, at the user's instruction after a day in which every stale statement found was found by accident, as a side effect of a check aimed at something else. His point is the one the memory leads with: it is good that they get found, but finding them that way gives no reason to believe the ones nobody has tripped over do not exist. The rule is that a correction replaces the wrong text rather than sitting beside it, so a round that leaves a document longer with nothing removed has added a second account instead of fixing the first. The part worth having is the split, because the two classes need opposite remedies and the wrong one entrenches the fault. A derived fact stored a second time is deleted and replaced with a pointer, and adding a check there compensates for the duplication rather than removing it. A fact that was true when written and has since expired cannot be de-duplicated at all, since there is only one copy and nobody edited it, so it is either dated with a pointer to what supersedes it or replaced by the command that computes it, the command being preferred wherever one exists. Two corollaries are stated with it: a figure a command can compute is never stored, and a passage is addressed by its section heading rather than by a line number, because a heading survives insertion while a line number silently comes to point at different but plausible text, which is worse than a broken reference because nothing errors. The memory is written to extend rather than restate the two existing rules about propagating a change, which cover sweeping for every affected place and changing the artefact rather than the prose; this one covers what to do at each place once found. Verified, appended after running: `doc-verify --check --strict` reports 0 FAIL and 0 WARN for the new file, its single INFO being a description of how far a document grew rather than a reference into it; the targeted context audit for this directory reports 0 failures, 0 warnings, 0 degraded and 0 info; personal-data-guard and encoding-guard report 0 findings.
- 2026-08-30 - Wrote the user's two plan-completion rules into `review_process.md` as a new "When A Plan Is Finished" section, and replaced the weaker wording that already half-stated the second one rather than leaving a second account standing. The rules are that a plan is not finished until a build simulation from it finds nothing, and that an account of what a document used to say never enters the plan unless it is an instruction for a build. The second carries his framing, which is the part that makes it operable: the historical detail and the do-not-change-this notes are aimed at whoever is rewriting the plan, not at whoever is building from it, so once the plan is done and nobody is rewriting it those notes have no reader left. The single test is therefore "does the builder need this to build?", not "is it true" and not "is it valuable", with evidence keeping its instruction in the plan and sending its proof to the ledger. Checking for an existing statement before writing rather than after was what shaped the edit: the file already said the live plan is not the review diary in its Purpose, told a reviewer not to add past narrative anywhere, and routed round-by-round narrative to the archive in its What Belongs Where table, none of which carried the build-instruction exception. The during-review instruction now applies the builder test explicitly, and the table gained the two classes it was missing, history a builder does not need and a note warning a future rewriter off a section, both routed to the review ledger. Writing the new section while leaving three weaker copies standing would have been the exact fault the rules describe. In the same task `sync_architecture_decision_log.md` gained "Why The Personal Repository Is Worth Building", which is the four-argument case moved out of the Stage A spec's recommendation section as that section was cut: it argues for a decision already taken, so it is rationale rather than build instruction, and a check of the log first confirmed the existing sequencing section covers why Stage A comes first but not why it is worth building at all. The same task's later half moved three more things out of the two live plans as the F28 cut reached them: the decision log gained "Why `ROADMAP.md` Is Private", and the review ledger gained two entries, one recording that a number freed by a deletion is retired rather than recycled (the reason the Stage A spec now starts at section 3 rather than being renumbered) and one recording that a widened pattern is never narrowed back when the example that forced it goes away. Two defects were found in the decision log while checking whether the plans' rationale was already recorded there, both the superseded `wikis/*/raw/` spelling stated in the present tense as though it were the live rule, and both fixed by dating the statement and naming what supersedes it rather than by rewriting the record. They matter beyond themselves: the workflow's consistency checker catches exactly that spelling, and it scans only the two plan documents, so the memory companions carry the same class of defect with nothing looking for it. Last, `feedback_diagnose_review_misses.md` gained three checks covering how this session's own misses happened, each written as an extension of a named existing check rather than as a new class: that a lexical sweep for a *kind of sentence* is a floor no widening turns into the set, with the four distinct ways one sweep failed in a single pass; that a reference must be resolved rather than grepped, because a line-based sweep declared an orphan check clean while a broken cross-reference sat split across two lines, and because cutting a section orphans references by descriptive phrase as well as by number; and that a checker's scope is a claim the defect class rarely respects, after a guard built to catch a superseded spelling was found to scan only the two plans while the same spelling sat in a memory companion. The `MEMORY.md` index line for that file was carrying a stored count of its checks, already stale before this edit, and now names the file as the source instead. Verified, appended after running: `doc-verify --check --strict` reports 0 FAIL and 0 WARN on every changed file, its INFO on each adjudicated as pre-existing or as false positives, two of them the checker matching this session's own description of line-break blindness; the workflow's own checks report 36 PASS and 0 FAIL with its two suites running 51 and 23 tests, all passing; encoding-guard, personal-data-guard, ai-style-guard and doc-sync-guard all report 0 findings.
