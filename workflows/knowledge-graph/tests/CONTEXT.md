# Knowledge Graph — Tests

**Last modified:** 2026-06-24

## Purpose
Standalone unit tests for the knowledge-graph indexer. Verifies that the parser tolerates malformed input without raising, that relationships are extracted and resolved correctly, that broken references are detected and classified, that the validation checks fire on the right conditions, that graph traversal (neighbours, impact, shortest path, subtree) is correct and cycle-safe, that the index is written atomically and reloads intact, that the opt-in content layers (memory, wiki, journal, and conversation) add nodes/edges only when their flags are on while leaving the structural graph unchanged, and that the read-only `sessions` cross-reference derives its search terms correctly and degrades to an empty result on any session-search failure.

## Contents
- test_parser.py — `workflows/knowledge-graph/tests/test_parser.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for the tolerant CONTEXT.md parser.
- test_builder.py — `workflows/knowledge-graph/tests/test_builder.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for graph construction against a temporary project tree, including broken-reference detection and atomic write round-trip.
- test_validate.py — `workflows/knowledge-graph/tests/test_validate.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for the validation checks: broken-reference classification (genuine→WARN, root-file prose→INFO), orphan detection, descendant-aware uncontained directories, duplicate titles, parse-warning surfacing, and back-reference gaps.
- test_query.py — `workflows/knowledge-graph/tests/test_query.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for traversal (neighbours, multi-hop reverse-reachability impact, shortest path directed/undirected, subtree), the `from_dicts` round-trip, run.py unknown-id handling, and adjacency-cache invalidation after a post-traversal edge add.
- test_memory.py — `workflows/knowledge-graph/tests/test_memory.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for the opt-in memory layer: the frontmatter reader (top-level and metadata-nested type, missing block), memory nodes appearing only when the flag is on, link resolution by both filename stem and `name:` slug, forward references kept unresolved and classified INFO, memory orphan severity INFO, MEMORY.md membership edges, memory→structural cross-references, and the flag-off structural regression.
- test_wiki.py — `workflows/knowledge-graph/tests/test_wiki.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for the opt-in wiki sub-graph layer: wiki-page nodes appearing only when the flag is on, per-wiki namespacing (two wikis sharing a page filename do not collide and a `[[stem]]` resolves within its own wiki), cross-wiki links kept INFO, intra-wiki forward references INFO, CONTEXT.md/LOG.md/operations-log.md skipped as pages, per-wiki membership edges, wiki→structural cross-references, wiki-page orphan severity INFO, layer composition, and the flag-off structural regression.
- test_journal.py — `workflows/knowledge-graph/tests/test_journal.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for the opt-in journal (light) layer: journal-entry nodes appearing only when the flag is on, one node per month file with the title from the `# Journal — …` heading, CONTEXT.md/LOG.md skipped as entries, intra-journal `[[stem]]` resolution, journal→structural `[[link]]` and backtick references, forward references kept INFO, a linkless entry kept off the orphan list by membership, journal-entry orphan severity INFO, the layer-order helper, three-layer composition, and the flag-off structural regression.
- test_conversation.py — `workflows/knowledge-graph/tests/test_conversation.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for the opt-in conversation layer (both shapes): standalone `conversation` nodes and namespaced `conversation-doc` doc-set pages appearing only when the flag is on, titles from the `# Title` heading, per-doc-set `[[stem]]` resolution with two doc-sets sharing a page filename (cross-namespace links kept INFO), CONTEXT.md/LOG.md and non-Markdown files skipped as pages, conversation→structural `[[link]]` and backtick references, forward references kept INFO, membership from the tracked `conversations` node keeping a linkless conversation off the orphan list, conversation/conversation-doc orphan severity INFO, the layer-order helper, four-layer composition, and the flag-off structural regression.
- test_collect_dirs.py - `workflows/knowledge-graph/tests/test_collect_dirs.py` [[workflows/knowledge-graph/tests/CONTEXT]] - Tests for the optimised gitignore-aware directory walk: `_is_git_worktree` detection (own and ancestor `.git`), the non-Git fast path never spawning git, ignored directories and their children pruned (never descended), skip/hidden exclusion, no-recurse directories recorded but not descended, one batched check per depth level (not per directory), and sorted output.
- test_sessions.py — `workflows/knowledge-graph/tests/test_sessions.py` [[workflows/knowledge-graph/tests/CONTEXT]] — Tests for the read-only `sessions` command: `session_query_terms` derivation (title used, multi-word title quoted-and-ANDed, `--terms` override priority, id-segment fallback when the title is missing or blank, FTS5 operator-character sanitisation), `run_session_search` best-effort behaviour (empty terms short-circuit, valid payload parsed, non-zero exit / unparseable / non-list output / subprocess exception all yielding `[]`, argv filter threading, and a real-subprocess missing-script check), and `cmd_sessions` payload shape, empty-result cleanliness, override threading, and unknown-id exit 2 — all with the searcher monkeypatched, touching no real session index.
- run_tests.py — `workflows/knowledge-graph/tests/run_tests.py` [[workflows/knowledge-graph/tests/CONTEXT]] — One-command runner that discovers and runs every `test_*.py` here; exits non-zero on any failure.

## Inputs
None. Tests build their own temporary fixtures or small hand-built graphs.

## Outputs
- Test results printed to stdout. Non-zero exit code on failure.

## Steps
1. Run the whole suite in one command: `python workflows/knowledge-graph/tests/run_tests.py`
   (or run an individual module directly, e.g. `python workflows/knowledge-graph/tests/test_query.py`).
2. Append LOG.md with a completion or failure entry.

## Dependencies
- `workflows/knowledge-graph/scripts/` [[workflows/knowledge-graph/scripts/CONTEXT]] — The modules under test (parser, builder, graph, validate, report, run, content_layer, memory, wiki, journal, conversation).
- Python 3.9+ standard library only (unittest, tempfile, pathlib).

## Known Issues
- Tests build temporary trees that are not git repositories, so the optimised walk takes its non-Git fast path (no git spawned, nothing pruned by gitignore) and the gitignore branch of the broken-reference classifier falls back to WARN. This is intentional; the gitignore pruning and the gitignore→INFO branch are covered against the real repo and via mocked-worktree unit tests in `test_collect_dirs.py`, not in temp trees.

## Revision History
- 2026-06-19 — Initial creation. Phase 1 tests: test_parser.py and test_builder.py.
- 2026-06-20 — Phase 2: added test_validate.py (validation checks) and test_query.py (traversal, loader round-trip, unknown-id handling). 52 tests total.
- 2026-06-21 — Phase 4: added run_tests.py (one-command runner) and switched Steps to use it; added adjacency-cache invalidation tests to test_query.py (54 tests total).
- 2026-06-22 — Phase 5: added test_memory.py covering the opt-in memory layer (frontmatter reader, flag gating, stem + name-slug link resolution, forward-reference INFO, memory orphan INFO, MEMORY.md membership, cross-layer references, and the flag-off regression). 72 tests total.
- 2026-06-23 — Phase 6: added test_wiki.py covering the opt-in wiki sub-graph layer (flag gating, per-wiki namespacing with a shared page name, cross-wiki links kept INFO, forward-reference INFO, non-page file skipping, membership edges, wiki→structural references, wiki-page orphan INFO, layer composition, and the flag-off regression). 91 tests total.
- 2026-06-23 — Phase 7: added test_journal.py covering the opt-in journal (light) layer (flag gating, one node per month file, title from heading, non-entry file skipping, intra-journal and journal→structural link resolution, backtick cross-references, forward-reference INFO, linkless entry kept off orphans by membership, journal-entry orphan INFO, layer-order helper, three-layer composition, and the flag-off regression). 108 tests total.
- 2026-06-23 — Phase 8: added test_conversation.py covering the opt-in conversation layer's two shapes (standalone `conversation` files and namespaced `conversation-doc` doc-set pages): flag gating, titles from heading, per-doc-set `[[stem]]` resolution with two doc-sets sharing a page name (cross-namespace links kept INFO), CONTEXT.md/LOG.md and non-Markdown skipping, conversation→structural link/backtick references, forward-reference INFO, linkless conversation kept off orphans by membership rooted at the tracked `conversations` node, both orphan severities INFO, layer-order helper, four-layer composition, and the flag-off regression. 130 tests total.
- 2026-06-24 — Session-search cross-reference: added test_sessions.py (20 tests) covering `session_query_terms`, `run_session_search` graceful-skip and argv threading, and `cmd_sessions` payload/exit behaviour. 150 KG tests total.
- 2026-06-24 - Rebuild speed optimisation: added test_collect_dirs.py (8 tests) covering the non-Git fast path, batched per-level gitignore pruning, no-recurse handling, and sorted output. 158 KG tests total; suite wall time dropped from ~410s to ~60s as temp fixtures no longer spawn git.
