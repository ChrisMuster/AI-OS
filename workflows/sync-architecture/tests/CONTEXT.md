# Sync Architecture - Tests

**Last modified:** 2026-09-01

## Purpose

Tests for `scripts/allowlist.py`, the shared selection rule, for the plan-consistency
checks in `scripts/run.py`, and for the two Stage A gates, `scripts/preseed.py` and
`scripts/boundary.py`, and the executable form
of its verification protocol. Every check carries three controls, named as such in the
test names:

- **Positive control** - a legal instance of the subject, which a counting check must
  find and a validating check must pass silently.
- **Rejection control** - an illegal instance, which the check must refuse. It proves
  the refusal path fires and says nothing about whether the definition is right.
- **Negative control** - something that is not an instance of the subject at all, so a
  clean result is known not to be silence from over-matching.

**This suite is deliberately not hermetic.** The doc-verify suite can be, because it
processes text. This module's whole subject is the difference between two git contexts,
so a test that stubbed git would be testing the stub. Every fixture is a real repository
built under `tempfile`: deterministic, no network, nothing written outside the temporary
directory, and the working directory restored even when a test fails.

The load-bearing control is `ShadowSelectionTests.test_positive_control_shadow_context_exposes_it`.
The defect the module was written to remove was a check running in a different git
context from the build it checked, whose symptom was an assertion that could not fail. A
suite run only against a clean tree cannot tell "the two contexts agree" from "I am not
really asking two different questions", so the fixture constructs the one state where a
correct implementation must return two different answers: a file that is both publicly
tracked and matched by an ignore rule.

## Contents

- test_allowlist.py - `workflows/sync-architecture/tests/test_allowlist.py` [[workflows/sync-architecture/tests/CONTEXT]] - 51 tests in eleven classes. `ExtractBlockTests` (counting: pathspecs returned, comments and blanks stripped, and rejection controls for a missing block, an unterminated fence, and an empty block, which is refused rather than returning an empty list because a check resolving zero pathspecs would report an empty selection as clean; negative control that a differently-labelled fence is not the block). `TrackedAndIgnoredTests` (counting: the invariant, whose positive control doubles as a regression control on `--no-index`, without which git refuses to report a tracked path as ignored and the function returns an empty set for a tree in any state). `ShadowSelectionTests` (counting: public context hides the tracked-and-ignored file, shadow context exposes it, and negative controls that selection is neither "everything" nor blind to the pathspecs). `SelectTests` (validating: the corrected set, the overlap reported rather than swallowed, and the mechanism pinned as the subtraction rather than as a property of how the pathspecs resolve). `StageTests` (the writing path: files become tracked, a bare probe repository accepts the add, a multi-batch stage adds every file, dry-run writes nothing, staging is idempotent, a failure returns False *and* reports git's reason, and the public working tree is untouched). `SmokeTests` (the real CLI as a subprocess: the self-test passes, `--list` reports a selection, and `--stage` refuses both a missing `--git-dir` and a missing repository). Three classes were added on 2026-08-27, taking it to 42 tests in nine classes: `TrackedByBothTests` (counting: the post-seed question of whether one path sits in both repositories' indexes, which is not the question `SelectTests` answers, with a rejection control asserting that an unanswerable question returns `None` rather than a clean empty set) and `RawDepthExclusionTests` (validating: that the bulk-material exclusion reaches a `raw/` folder at any depth, on synthetic fixtures rather than on the real tree, with a rejection control asserting the superseded one-level spelling leaks the deeper file, and `AuthoredFolderFileTypeTests` (validating: that all four authored personal folders' include patterns take every file type, the claim being that the patterns are type-blind rather than that the resulting selection holds every file, since a declared exclusion may still carve one out; on synthetic fixtures for two reasons - three of the four contain no non-markdown file today so the tree cannot test the rule for them at all, and a real-tree control cannot distinguish a broken pattern from a deliberate deletion, which matters because this suite gates the seed commit; its rejection control asserts that a markdown-scoped rule misses every one of the file types, and its negative control asserts bulk material under a `raw/` folder is not swept in by the authored-folder patterns).
- test_run.py - `workflows/sync-architecture/tests/test_run.py` [[workflows/sync-architecture/tests/CONTEXT]] - 23 tests in four classes, covering the plan-consistency half of `scripts/run.py`. `ExtractPlannedTests` (counting: paths declared in the fenced planned-artefacts block, comments stripped, and negative controls that a missing, unterminated or differently-labelled block yields an empty set rather than `None`). `UnresolvedScriptsTests` (counting: a script a plan names that neither exists nor is declared, with negative controls for a script that exists, a declared future one, and prose naming no script at all). Its load-bearing control is `test_rejection_control_an_absent_declaration_excuses_nothing`: an absent declaration must mean nothing is exempt, because reading it as a blanket exemption would let deleting the block silently disable the check. `CompareToBaselineTests` (validating: a plan measured against the version last reviewed, asserting that drift is information rather than failure, that a missing baseline is the one real defect, that an absent live plan is skipped rather than judged, and that the added/removed counts come from a real diff rather than a lockstep comparison, which on the real documents read 1457 lines against a true 273).

- test_preseed.py - `workflows/sync-architecture/tests/test_preseed.py` [[workflows/sync-architecture/tests/CONTEXT]] - 15 tests in three classes over the pre-seed gate. `EnvPredicateTests` and `OversizedTests` isolate the two predicates from git, which is coverage the module's own `--self-test` cannot give because there a fixture decides the answer. Their negative controls are the load-bearing ones, since the failure each guards against is a false positive on an ordinary file: `foo.env` and `config/prod.env` must not match, which is the defect a naive `endswith(".env")` introduces, and a file exactly at the threshold must not be flagged, since the rule is "exceeds". The size limit is an argument rather than a constant read directly, so a control can prove the predicate fires without writing 25 MiB to disk, which makes the separate assertion on the shipped threshold necessary rather than decorative. `PreseedSmokeTests` runs the real CLI as a subprocess, and `test_the_self_test_passes` is how close-out reaches the integration controls; they are reached rather than copied, because a second copy of a rule is the thing that drifts.
- test_boundary.py - `workflows/sync-architecture/tests/test_boundary.py` [[workflows/sync-architecture/tests/CONTEXT]] - 9 tests in two classes over the boundary-completeness check. `UnderRawTests` exists because **no run against this project can test that predicate**: every `raw/` directory in the tree sits inside a wiki, so a path-segment test and a prefix test on the wikis directory return the same answer on every path that exists, and a green run distinguishes nothing. Its positive control for a `raw/` folder outside the wikis tree is the load-bearing one, and its negative controls pin the two over-matching defects a substring test would introduce, a component merely starting with `raw` and an extension containing it. `BoundarySmokeTests` reaches the module's `--self-test`, which carries the integration controls for all five failing conditions, and adds two rejection controls: a run with no `--git-dir` and one against a repository that does not exist must both be refused rather than doing part of the check while appearing to do all of it.

## Inputs

- `scripts/allowlist.py`, `scripts/run.py`, `scripts/preseed.py` and `scripts/boundary.py`, each imported directly by file path rather than by package import, so the suite does not depend on how the entry point sets up `sys.path`.
- A working `git`, for `test_allowlist.py`, `test_preseed.py` and `test_boundary.py`. Unlike most suites in this project, git is a genuine dependency rather than an incidental one: the subject under test is git behaviour. `test_run.py` needs no git, because its subject is text.

## Outputs

- Test results on stdout (unittest). Exit code 0 when all pass, non-zero on failure. No files written outside the temporary directory.

## Steps

1. Run `python workflows/sync-architecture/tests/test_allowlist.py` and `python workflows/sync-architecture/tests/test_run.py`.
2. The close-out verifier discovers both suites automatically, under the `sync-architecture` owner, by finding this tests directory and reading the files in it whose names begin with "test_" and end in ".py".
3. Append LOG.md with a completion or failure entry.

## Dependencies

- `workflows/sync-architecture/scripts/allowlist.py`, `workflows/sync-architecture/scripts/run.py`, `workflows/sync-architecture/scripts/preseed.py` and `workflows/sync-architecture/scripts/boundary.py` [[workflows/sync-architecture/scripts/CONTEXT]], the modules under test. Each is written in full rather than shortened after the first, because the knowledge-graph indexer builds a dependency edge from a full path and reads a bare filename as prose, so abbreviating them here silently deletes the edges this section exists to declare.
- `workflows/close-out/` [[workflows/close-out/CONTEXT]], which discovers and runs this suite as one of its three gates.
- `git`, on PATH.

## Known Issues

- The integration controls for both Stage A gates live in each module's own `--self-test` and are reached from here by a smoke test rather than being reimplemented. That is deliberate, so there is one copy of each rule, but it has a cost worth knowing: a failure inside those controls surfaces here as a single failing smoke test naming the subprocess, and the specific control that fired has to be read from that subprocess's own output.
- The suite shells out to `git` for every fixture, so it is slower than a hermetic one, roughly twenty seconds. That is the cost of testing real git behaviour rather than a stub, and it is the right trade for this subject.
- `SmokeTests` runs the CLI against the real project rather than a fixture, so its assertions are deliberately weak: that a selection was reported, not what was in it. Anything stronger would encode the current contents of the working tree into a test.
- The batching test rebinds `allowlist._CHUNK_CHARS` to force multiple batches and restores it in a `finally`. That reaches into a module private, which is accepted here because the alternative is generating enough files to cross the real limit.

## Revision History

- 2026-08-26 - Initial creation, alongside `scripts/allowlist.py`. Added so the close-out verifier actually runs the module's checks: the module shipped with a `--self-test`, but close-out only discovers suites inside a workflow's own tests directory, so a full close-out reported "0 test file(s) across 0 suite(s)" and the self-test would never have run. The suite was mutation-tested on the way in, by removing the subtraction, removing `--no-index`, and removing the stderr reporting, and it catches all three. A fourth mutation caught a false claim rather than a defect: removing a `core.bare=false` argument changed nothing, which showed the argument had been added on a wrong diagnosis and documented as a fix. It was removed and the documentation corrected.
- 2026-08-27 - Added `test_run.py` and two classes to `test_allowlist.py`, taking the directory to 48 tests. Both additions came from a build simulation of Stage A rather than from reading the code. `TrackedByBothTests` covers a new post-seed overlap check that replaced a `comm` pipeline in the plan's own acceptance table, `comm` being present in Git Bash and absent from PowerShell. `RawDepthExclusionTests` closes a dated hole: the depth-independence of the bulk-material exclusion was proved only by the real tree containing one `raw/` folder two levels down, which contributes three files out of more than sixty-two thousand and is queued for deletion immediately after Stage A, so that assertion would have begun passing on a one-level-too-shallow pattern on the day of the deletion. Both were mutation-tested rather than trusted: inverting the set operation and the failure path made all three of the first class's controls fire, and injecting a "no declaration means skip the check" bug made three of `test_run.py`'s controls fire.
- 2026-08-27 - Added `CompareToBaselineTests` to `test_run.py`, taking the directory to 54 tests, after the user restated the review-baseline convention: the baseline holds the version the reviewing AI last reviewed and is refreshed only once a review round has finished, so drift is the normal state and is the deliverable for the next review. The check had asserted the opposite, failing on drift with a message telling the reader to refresh before a review round, which is the one action that destroys the diff. Mutation-tested by restoring the old FAIL-on-drift behaviour, which fires three of the new controls. One of the new tests pins a defect found while writing the replacement: a first version counted differing lines in lockstep, which reports every line after an insertion as changed and read 1457 lines against a true 273, so the count now comes from `difflib` and was cross-checked against `git diff --numstat`.
- 2026-08-27 - Added `AuthoredFolderFileTypeTests`, taking the directory to 59 tests. It closes a control that was broader than the claim it backed: the rule that all four authored personal folders take every file type was proved only by "an authored folder contributes at least one non-markdown file", which a single zip satisfies, so the PDF and four PNGs the classification decision was actually taken over could all have stopped being selected with the suite still green. The fixture is synthetic for two reasons stated in the class, and the first is the one that could not be fixed any other way: three of the four folders contain no non-markdown file at all today, so a real-tree control cannot test the rule for them and would not begin to until the day a CV arrived. The second is that a real-tree control cannot tell a broken pattern from a deliberate deletion, and since this suite gates the seed commit that ambiguity surfaces as a build blocked for no reason. Its rejection control asserts a markdown-scoped rule misses every one of the file types, and its negative control asserts the authored patterns do not sweep in bulk material under a `raw/` folder, without which "the patterns select PDFs" could be satisfied by a rule selecting every PDF anywhere.
- 2026-08-28 - Corrected `AuthoredFolderFileTypeTests`'s docstring, which overstated what the class proves, without changing a test. It read that a fixture "asks only about the rule, which cannot be deleted by accident". A rule can be deleted by accident: the class carries its own copy of the four folder names and never reads the allowlist block, so removing an authored folder from that block outright leaves every test here green. The sentence stated a limit of the fixture as though it were a property of it, which is the more dangerous of the two errors, because a reader consulting the class for assurance would take coverage it does not have. The docstring now names the limit and points at the instrument that does cover it, the second named invariant of the boundary-completeness check, a Stage A deliverable that walks the ignored set on disk and so reaches a folder dropped from the block by starting from the files rather than from the block. No second control was added here deliberately: it would be a weaker copy of an invariant already specified, and a second copy of a rule is what broke this one originally. Found by an external review round; no check reads a docstring for accuracy, which is why this class of defect keeps arriving from review rather than from the suite.
- 2026-08-28 - Documentation-only pass over `test_allowlist.py`, correcting the `AuthoredFolderFileTypeTests` docstring and this file's Contents description after a cross-AI review, with no test added or changed; the directory stays at 59 tests. The class stated its subject as "the four authored personal folders take EVERY file type", which is false of this tree as it stands, because `conversations/.obsidian/` is deliberately Category C. What the fixture proves, and now says it proves, is that those folders' *include patterns* are type-blind, so no file is lost to an extension-scoped rule; removal by a declared exclusion is a separate mechanism with its own invariant elsewhere. The docstring's reference to that invariant was tightened in the same pass: it had described the exception as "except where an exclusion line in the block names it", which any future exclusion satisfies merely by existing, and it now describes the closed declared set that replaced it. The distinction is load-bearing rather than pedantic, because a check is to be built from these words and the loose form would license the accident the invariant exists to catch.
- 2026-08-28 - Corrected the baseline convention in the `CompareToBaselineTests` docstring in `test_run.py`, which still read "refreshed only once a review round has finished" and now names the moment as the reviewing AI recording its findings. The docstring states the convention the class exists to encode, so a reader checking what the tests mean by a correct baseline was being given the ambiguous form. The 2026-08-27 entry above carries the same phrasing and is deliberately left as written: it records what was believed when the tests were added, and this entry is the correction to it. Documentation-only, with no test added, removed or changed.
- 2026-08-29 - Added the controls for the three pieces `boundary.py` needs, and re-measured the two test counts in Contents, which read 29 and 17 against an actual 51 and 23. `LabelledBlockTests` and `PositiveInversionTests` in `test_allowlist.py` cover the new `label` argument and the exclusion inversion; the inversion's rejection control is run against git rather than as string surgery, requiring the written and inverted forms to select different sets, because an inversion that quietly did nothing would still look like a correct string. A further test asserts the live plan carries all five labelled blocks, since a block deleted from the document is a build blocker and must not pass silently. `BoundaryInputTests` in `test_run.py` covers the new consistency assertions, and its load-bearing control is the undeclared exclusion: that check passes on the live plan, and a pass proves nothing until the check has been shown capable of failing, so the control feeds it a real exclusion over a real authored folder and requires the failure, with a negative control confirming an exclusion that reaches no authored folder is not swept up by over-matching.
- 2026-09-01 - Added `test_preseed.py` and `test_boundary.py` alongside the two scripts Stage A creates, taking the directory from 74 tests across two suites to 98 across four. They exist for the reason recorded in the 2026-08-26 entry above: each new script ships a `--self-test`, and close-out discovers suites at this directory's `test_*.py` files only, so a self-test not reached from here would never run under the gate. The integration controls are therefore reached rather than copied, and what each file adds beyond reach is the coverage a fixture cannot give, namely the predicates in isolation. Three mutations were run against the new controls rather than the suites being trusted, and two of the three found something. Making the size flag block instead of report passed every control the pre-seed suite then had, because neither the clean fixture nor the dirty one let the flag decide the answer; a flagged-but-clean fixture was added and the mutation now fires. Turning `under_raw` into a prefix test on the wikis directory fired four controls, which is the case this project's own tree can never exercise, since every `raw/` directory in it sits inside a wiki and both readings agree on every path that exists. Each mutation was confirmed applied before its result was read and the module restored byte-identically afterwards, checked with `diff` rather than assumed.
