# Rule Hooks - Tests

**Last modified:** 2026-09-21

## Purpose
Synthetic-event tests for the rule-hooks evaluator: prove each rule blocks what it should and allows what it should (the false-positive guards), and prove each adapter parses its AI's payload and emits the right block contract. Verification discipline: the build is not done until the suite passes and a live block is confirmed.

## Contents
- test_rule_hooks.py - `workflows/rule-hooks/tests/test_rule_hooks.py` [[workflows/rule-hooks/tests/CONTEXT]] - The full suite (the runner prints the count; it is not repeated here, having been wrong in this sentence twice): per-rule block/allow cases via the in-process Context pipeline, adapter input-parsing tests, subprocess tests asserting the real output contracts (Claude exit 2 + stderr; Codex `hookSpecificOutput` JSON deny), `TestShellParsingContract` (the shared reader tested directly, because a defect there is a defect in five rules at once: newline separation and continuation, line continuations, comments only at word start, quoted operators staying arguments, substitutions parsed as commands, both sides of a redirection, file-descriptor adjacency, grouping depth, and heredoc detection), `TestGroupingReachesEveryShellRule` / `TestWrappersReachEveryRule` / `TestWrapperOptions` (the same bypass tried against each of the five rules separately, rather than trusting that one fix covers all: this is where the omissions actually lived), `TestFabricatedHeredocs` (a `<<` in a comment, in arithmetic, or in a multi-line quote, each of which invented a body that swallowed every following command), `TestCarriedCommands` (a `sh -c` string and an `eval` operand), `TestFindActionsAreExamined`, `TestA4LogNameBoundary`, `TestA3RoundThree` / `TestA3RoundThreeExtras` / `TestA9RoundThree` / `TestA9RoundThreeExtras` / `TestRoundFour` (the per-round finding sets), `TestHookCostIsBounded` and the A9 and A2 timing assertions (a hook that exceeds its five-second budget has allowed the command, so cost is a correctness property), `TestA8PowerShellHereString` (the here-string block: the real failing commit shape and the double-quoted form block and name A8, the block message names the heredoc remedy, and four false-positive guards - the correct POSIX heredoc, a heredoc body containing the syntax as documentation, a same-line `@'hi'` that is a plain Bash argument rather than an opener, and ordinary commands), `TestPrecommitDocSync` (the pre-commit doc-sync advisory: the pure `format_doc_sync_advisory` text, that a personal-data block short-circuits before the advisory, that a clean personal-data check runs the advisory and still allows the commit, and that a doc-sync guard crash is swallowed), `TestPrecommitDocSyncSeverity` (the advisory's severity filter: drift alone prints the advisory - the positive control - while a DEGRADED finding alone prints nothing and a degrade alongside real drift neither prints itself nor masks the drift), `TestRoundFive` (the nine behavioural findings of the 2026-09-05 closing review, each fix beside the control that must not move; its tenth finding had no code in it and is covered by `TestChangeSetIsDeliverable`), `TestRoundSix` (the findings of the review of those repairs, five of whose six code defects were in the repairs themselves), `TestRoundEight` (the findings of the third review of the same day, including the fabricated-heredoc escape test that asked a fixed two-character question about a run of unbounded length), `TestRoundNine` (the 2026-09-11 external review: a command substitution executed inside arithmetic expansion, three A9 spellings - the keyword and encoding-carrying `Path.open` forms it missed and the ruby read it over-blocked - a directory verb's exit status settling an `||` chain, a matcher that emits a count rather than what it matched, and a deprecation warning turning a block into an allow; each beside the control that must not move, and one half of one finding rejected on the merits with its own pinning test), `TestRoundTen` (the review of those repairs: two regressions the round-nine reader fix introduced, an uncapped self-recursion and a quadratic re-read that both end in an allowed command, plus three pre-existing reader defects at the same construct, the A3 `pushd`/`popd` bypass, ripgrep's `-L` not meaning what grep's does, and the three ruby mode spellings the read/write split had lost; its cost test counts the characters walked rather than timing the fire, having replaced a wall-clock version that could not fail), `TestRoundEleven` (the review of round ten's repairs: `$((` decided structurally rather than by its last two characters, in quotes, and at every nesting level; the work budget and the over-reporting fallback past it; `pushd -n` in the spellings where the move would fail, stack positions and too many operands; flag bundles containing an attached value; ruby's `File.new`, `IO.write` and two-encoding mode; and a call receiver whose mode had been read as its path), `TestRoundTwelve` (the review of round eleven's repairs: quoted words past any size and nested siblings counted in characters walked, the stack walk's four quoting rules each measured in bash, the directory builtins' arguments read as bash reads them, attached and separate option values, `--label`, a path built from several arguments, and ruby's parenless calls; three round-eleven tests that could not fail were strengthened in place), `TestRoundThirteen` (the 2026-09-13 external review: each interpreter's options read the way that interpreter reads them - node's `--eval`, `-p` and `-pe`, option bundles such as `python -Sc` and `perl -we`, an option value such as `-W ignore` not mistaken for a script, an option the tables do not know not trusted to end the options, node's `-r` naming a module, words after a script, a `-m` module or python's `-c` belonging to that program, and a heredoc beside inline code being data; `open()` and `Path.open()` with other keywords before `mode=`, including `file=`; perl's parenless, lexical-handle, layered and read/write `open` forms; ruby's parenless Kernel `open`; python string prefixes read as literals while an f-string stays computed; A6 treating an inverted match and delete-only sed and awk filters as removing the statement rather than supplying it; and two limits the user chose to keep, pinned so a later round sees them as decided), `TestNegatedDirectoryVerbs` (the A3 half of the 2026-09-18 external review: a leading `!` inverts the exit status a directory verb hands to the next `&&` or `||`, with the mirror chains, a double negation and the pipeline case that per-segment reading would have missed as its controls), `TestFindDeleteBlocksOnTheVerb` (the `-delete` contract since 2026-09-20, where the verb decides and none of the expression's shape is consulted: six tests in place of the four classes and 33 that pinned the adjudication engine it replaced - an unfiltered delete, a filtered one recording the accepted cost, the two round-18 specimens that were ALLOW before, the rounds 14 to 17 shapes now blocking for one reason instead of four, an operand spelled `-delete` pinning the deliberate over-width, and one control that A6 has not silently become `any find`), `TestHiddenCommandWrappers` (A10, the command hidden inside a wrapper option's value: eleven blocking tests and seven controls, where the blocking set deliberately spans four rules' worth of payload - a destructive command, a dedicated-tool read, a LOG.md redirect - because the bypass was of the reader rather than of any one rule, and where the controls carry the width of a refusal that never reads what it refuses, the armed one being a `-S` that belongs to the wrapped program rather than to env; it also carries the only test here that guards a property instead of a verdict, that `effective_argv` still returns a true suffix of its input, armed by a mutant performing the splice that was the rejected alternative fix), `TestSharedParseIsSafe` (the parse is memoised, so the tests pin that sharing leaks no mutation and that the cache keys on the exact text), `TestHookCostEndToEnd` (a whole hook fire against the real budget, across three input shapes, plus a separate assertion that the command is lexed once rather than once per rule), `TestEveryScriptModuleIsTracked` (whether every local module under `scripts/` is in git's index, which is the one check here that asks whether the code survives a clone rather than whether it behaves; it was called `TestChangeSetIsDeliverable` until 2026-09-12, a name that reads as a commit-readiness gate and promises more than the check performs, since it says nothing about what is staged), `TestPrecommitDocSyncRecording` (the collection path for the doc-sync guard's switched-off CONTEXT-only LOG clause: a fire records both counts, a fire with no findings records zeroes as the denominator, a degraded fire is recorded rather than dropped, a fire the personal-data gate blocks records nothing, the advisory still prints WARN only, the real guard writes no file during a fire over a throwaway repository carrying the case, a failing fire-log write costs a sample rather than a commit, and a source-level check that no test in this file reaches the fire-log write without redirecting `FIRE_LOG`), and `TestCwdIndependence` (the hook launches `run.py` from a project-root-anchored command, so evaluation must survive a drifted working directory: Claude invocations from a temp directory outside the repo still block a `.env` write and still allow a normal write; shipped `.claude/settings.json` hook commands anchor `run.py` to `$CLAUDE_PROJECT_DIR`; shipped `.codex/config.toml` hook commands resolve the git root; and a Codex hook command blocks from a project subdirectory). Destructive cases use safe synthetic strings only; the B3 detection email is assembled at runtime so the literal never appears in the committed source.

## Inputs
- None beyond the workflow scripts under test and the live git repo (the B3 gitignored-vs-committable test uses real `git check-ignore`).

## Outputs
- Test pass/fail results on stdout.

## Steps
1. Run: `python workflows/rule-hooks/tests/test_rule_hooks.py`
2. All tests must pass before the work is ready.

## Dependencies
- `workflows/rule-hooks/scripts/` [[workflows/rule-hooks/scripts/CONTEXT]] - The code under test.
- `workflows/personal-data-guard/` [[workflows/personal-data-guard/CONTEXT]] - Reached via the B3 rule in the personal-data tests.

## Known Issues
- **A control that names a file which exists nowhere proves nothing.** The clearest example was `cd && grep pattern no-such-file.md`, written to pin that the rule does not guess a home directory: it passed however the rule behaved, because the file was absent either way, and the claim it was named for had never been tested. Rewritten to name a file that exists at the project root, it immediately failed. When writing a must-allow control, check that it can actually distinguish the two behaviours, not merely that it passes.
- **The chain operator and the operands are part of the specimen.** Three round-eleven controls could not fail because of what joined or named their commands: `pushd +0 && cat` passes whether `pushd +0` succeeds or reports an unknown status, since both let the read run, and only `||` separates them; a `;` in front of an absolute read is seen whatever the directory verb before it did; and a first operand that does not exist makes a move fail with or without the check under test. Choose the operator and the operands so the two behaviours reach different verdicts, and confirm it with the mutant before trusting the test.
- **A test must not read the constant it guards.** The work-count test for arithmetic depth first bounded its count by the production budget constant plus linear work, so a mutation removing the budget, by raising that constant, raised the bound in step and the quadratic walk passed. It was the one miss in the round-eleven mutation pass. A guard on a limit states the limit itself, as a number measured on correct code, and says so beside it.
- **A control can be disarmed by the very fix it is written beside.** `test_a6_still_allows_a_client_that_is_the_source` used `sqlite3 db .dump | grep -c 'DROP TABLE'`, and the same round's fix makes the sweep discard a `-c` matcher's pattern whether or not the upstream-only logic exists - so the test passed under a mutation that read the whole pipeline, and needed two simultaneous defects to move. Dropping the `-c` restores it. This is a sharper variant of the entry below: the command was not chosen carelessly, it was made vacuous by a change landing in the same commit, which is a reason to re-run the mutation for any control whose command touches the behaviour being changed.
- **Three tests in this suite were found passing for the wrong reason, all by mutation testing rather than by reading.** `grep -- -pattern README.md` blocked even with end-of-options handling removed, because `-pattern` was being read as a bundle of short flags containing an `r`, which made grep look recursive - two defects cancelling out. `sed -i 's/a/b/' notes.md memory/LOG.md` blocked even when only the last operand was recorded, because the protected file happened to be last. And `pushd x && popd && grep p CONTEXT.md` passed whether `popd` returned to the right directory or simply gave up, because both outcomes allow. Each was rewritten so that only the intended behaviour passes.
- The subprocess tests spawn `run.py`, so the suite shells out and takes some seconds rather than being instant. **No figure is given, and that is the correction.** One was quoted here as 2 seconds, then 5, then 8, then a measured range of 9.8 to 11.3 - and a reviewer then observed 10.3 to 19.4 across eleven runs, so the fourth attempt was wrong the same way as the first three. The number depends on what else the machine is doing, which no amount of re-measuring fixes, and a figure in prose cannot be re-measured by the reader. Run the suite if the duration matters while the suite grew past both. A number in prose decays unless it is re-measured, which is the same defect this file records for its own test count.
- **A timing assertion has to be sized so a correct implementation is nowhere near it.** The end-to-end budget test first used 234,000 characters of operator-dense input, which measures anywhere between 1.9 and 3.4 seconds on the development machine and so failed intermittently against its own threshold. An intermittent failure teaches people to rerun the suite, which is worse than not having the test. Two attempts to fix that by tuning the threshold both failed, the second still failing five times in ten runs at up to 6.98 seconds on correct code. The conclusion is that no wall-clock line inside a four-fold load-dependent spread is stable, so the end-to-end budget test now carries no wall-clock assertion at all and measures the mechanism instead: `test_the_whole_command_is_scanned_once` counts the characters the scanner walks per fire, which is a regression measured in work rather than in time.

  **Three per-rule cost tests still assert wall-clock time, and that is not the same instrument problem.** `test_the_path_backslash_scan_is_linear` (0.5s), `test_a_recursive_glob_operand_does_not_walk_the_project` (1.0s) and `test_a9_scanning_is_linear_in_the_size_of_the_code` (2.0s) each guard a quadratic path measured at 4.9 seconds, 5.9 seconds and four minutes respectively, against correct code that returns in milliseconds. The threshold sits roughly two orders of magnitude away from correct behaviour rather than inside the spread, which is what made the end-to-end test flaky, so these are order-of-magnitude canaries rather than performance lines. Read a claim about the timing strategy as being about the end-to-end test unless it names these.
- **Any test that reaches `_precommit_doc_sync` must redirect `run_mod.FIRE_LOG` to a temp path.** That function appends a `doc_sync_recording` row, and the real fire-log is the store the recording-mode observation period counts, so a row written by a test is indistinguishable from a genuine sample reporting no findings. `test_no_test_writes_to_the_real_fire_log` enforces this at the source rather than at runtime, since a runtime check only catches whichever test happens to run.

## Revision History
Earlier history archived to LOG.md on 2026-09-21.

- 2026-09-07 - The review of the same day's work found **two of these new tests unable to fail**, and one of them was cited by three documents as proof of a safety property. `test_shared_parse_does_not_leak_mutation` compared two live calls to the parser; under the very cache it forbids, those two calls return the same list object, so it compared a list to itself and passed with the depths visibly corrupted. It now holds a copy of the expected depths from before the mutation. `test_control_a_hash_inside_a_word_is_not_a_comment` used a command that is allowed under both behaviours, so a mutation making `#` a comment everywhere broke no test in the suite; it now uses a command that blocks. `TestChangeSetIsDeliverable` was narrower than its own name in three ways - it read one file's imports rather than the package's, asked whether a module was untracked rather than whether it was in the index (so a gitignored module reported as deliverable), and passed quietly when git could not answer. Suite 299 to 313, with `TestRoundSix` carrying the round's findings and their controls. 12 of 12 mutations caught after two harness anchors were corrected; the aliasing control among them was itself rewritten, having first been written against a command where nothing was ever appended and so could not fail either - the same defect, committed while writing the test for it.
- 2026-09-07 - The fourth review removed the end-to-end timing assertion altogether, after a third threshold proved flaky too: at the real 5-second budget it was still observed failing at 5.07s and 5.97s on correct code under load. Three thresholds were tried and all three failed the same way, which is the evidence that the wall clock was the wrong instrument rather than the wrong number. **That test's** cost is now measured only in characters walked, which no machine or load can move. The sentence originally said "cost is now measured only in characters walked" with no subject, which the 2026-09-11 review read as a claim about the whole suite and correctly called overstated: three per-rule cost tests still assert wall-clock time, at margins described under Known Issues. The same review found that the replacement test could not detect the regression its own docstring named: it rebuilt an `lru_cache` over the scanner unconditionally, so memoisation was present during the test whether or not production had it, and it passed with the cache removed. It now rebuilds a wrapper only if production has one, and measures both scanners rather than one, after a mutation removing the heredoc splitter's cache was found to break no test at all. Suite to 331, which is the measured figure: the suite stood at 331 when this round opened on 2026-09-11. **This entry said "313 to 331" and the third review's entry below said "313 to 321", which cannot both be starting points and was reported as a finding on 2026-09-11.** The end state is the half that can be checked by running the suite; the intermediate cannot be recovered from any record, so it is left as the third review stated it rather than reconciled by guesswork, and the count is obtained by running the suite rather than read from here.
- 2026-09-07 - The third review found the timing test genuinely flaky - five failures in ten runs on correct code, once at 6.98 seconds - and the numbers published in its docstring to justify its threshold were wrong again. **The conclusion is that the wall-clock approach was the mistake, not the threshold.** A four-fold load-dependent spread has no stable line inside it, so the timing assertion now uses the real 5-second budget, where a failure means the hook genuinely would have timed out, and the regression is caught deterministically by `test_the_whole_command_is_scanned_once`, which counts the characters the scanner walks per fire rather than the seconds it takes. The deliverability check was rebuilt on `ast`: its hand-rolled import reader dropped the second name of `import a, b`, mangled `import a as b` and `import x  # noqa` (a live line in `run.py`), missed `from . import x` entirely, and could not resolve a package - so both adapters, the code that makes the hook fire at all, could have been untracked with the test green. Suite 313 to 321.
- 2026-09-12 - Added `TestRoundNine` (13 tests) for the 2026-09-11 external review, suite 331 to 344. Six of the eight findings are behavioural and each fix ships beside the control that must not move, because five of them widen what a rule catches and two narrow an over-block, which are the two directions that respectively refuse honest work and wave through the thing the rule exists to stop. The first finding is the previous round's repair, for the seventh consecutive round: stopping the reader descending into arithmetic expansion was right, and bash still runs a command substitution written inside one, so `echo $(( $(rm -rf build) + 1 ))` was invisible to all five rules that read the shared parse. **Half of one finding was rejected on the merits and is pinned by a test of its own**, since a later round would otherwise re-report it: reading a matcher's pattern as SQL input is correct for `git log | grep 'DROP TABLE' | sqlite3 db`, where the pattern describes exactly what reaches the client, and wrong only when the matcher was asked for a count, a filename or silence. Two findings were documentation. The timing claim was scoped to the test it was about, three per-rule wall-clock canaries being described under Known Issues rather than implied away; and `TestChangeSetIsDeliverable` was renamed `TestEveryScriptModuleIsTracked`, a name that stops promising a commit-readiness gate it never implemented. The two 2026-09-07 entries above disagree about a starting count and both are left in place, with the measurable half corrected and the unrecoverable half named as unrecoverable; they also sit in the wrong order, the fourth review's entry above the third's, which the audit permits for same-day entries and which each now resolves by naming the other.
- 2026-09-12 - Added `TestRoundTen` (15 tests) for the review of the round-nine repairs, suite 344 to 359, and fixed two of the round-nine tests the same review found wanting. The headline is that **two of the five behavioural findings were regressions introduced by round nine's own fix**, both ending in an allowed command: an uncapped self-recursion (a `RecursionError` is caught by the evaluator and treated as allow) and a quadratic re-read (a hook that exceeds its budget has allowed the command). One control was found unable to fail because the same round's fix had disarmed it, and one behaviour claim was found to have shipped with no control at all; both are recorded under Known Issues. One new test deliberately pins a helper rather than a command, because the fix it names cannot fail through the scanner: the fallback that rescues the same input arrives first, so testing it end to end would have been a control that proves nothing. The mutation testing for this round was done by monkeypatching the old implementations in a throwaway process rather than by editing files, which removes the restore step that had previously left four files with disturbed mtimes.
- 2026-09-12 - Added `TestRoundEleven` (11 tests) for the review of round ten's repairs, suite 359 to 370, and replaced `TestRoundTen`'s wall-clock cost test with a deterministic count of the characters `_matching_paren` walks over a 2,000-level nest. That replacement was the one control the round-eleven mutation pass found unarmed: its bound read the production budget constant, so the mutation removing the budget raised the bound with it; it now states a fixed number measured on correct code. The tests for this round were written after the probes rather than before, because the first version of the over-budget fallback was itself a false negative and it was a probe, not a test, that caught it. Every round-eleven control was then confirmed by monkeypatching the previous implementation, run from a module in the session scratchpad after a long inline probe was rejected by the shell before it ran.
- 2026-09-12 - Added `TestRoundTwelve` (12 tests), suite 370 to 382, strengthened three round-eleven tests a reviewer showed could not fail, and renamed `test_past_the_budget_nothing_that_runs_is_dropped` to `test_deep_nesting_drops_nothing_that_runs` now that the budget it named is gone. The new cost test counts the characters walked over 400 nested siblings and bounds them by its own command's length, never by a production value. One new test was changed from `;` to `&&` while its mutant was being designed, because with `;` it could not tell an unknown status from a failure - which is the shape now recorded under Known Issues.
- 2026-09-13 - Added `TestRoundThirteen` (15 tests) for the round-thirteen external review. Each test was written after a probe had reproduced its finding and was confirmed failing before any code changed, and every interpreter claim in it was measured in Git Bash for python, node and perl, since ruby, php and ripgrep are not installed here. Most of the class is controls, because five of the seven fixes widen what is allowed, and a widening is the direction a document write or a destructive command would take. Planning the mutation pass found four gaps before it ran: python's `-c` ending option reading, an unknown option not being trusted to end it, and `open(file=...)` naming a non-document had no test that could move, and the `sed -f` control could not tell its two behaviours apart, because the statement never appeared beside a real deletion. Two of the class's tests pin limits the user chose to keep, so a later round reads them as decided rather than rediscovering them.
- 2026-09-18 - Added `TestFindDeleteOrdering` (6 tests) and `TestNegatedDirectoryVerbs` (7) for round fourteen's two rule findings, plus one more recorded-limit test, taking the suite from 397 to 411. Both finding classes were measured before anything was written, and in both cases the measurement is the part worth keeping. `find`'s evaluation order was demonstrated on a throwaway tree with `-print` standing in for `-delete`, which proves a destructive ordering without ever running a destructive command; `bash -n` settled a question the code raised rather than the review, that an unescaped `find ... ( ... )` is a syntax error, so blocking that spelling costs nothing. The negation semantics were run as ten specimens in Git Bash with `echo RAN` in place of the read, and bash ran the payload in exactly the cases the evaluator blocks. Most of both classes is controls, since both fixes widen what is blocked and a false block is the more expensive failure for a rule people can route around. The mutation pass was planned before it ran, in both directions per fix: an always-allow and an always-block mutant for each, a presence-rather-than-parity mutant that fires exactly the double-negation test, and a respelled no-op control for each that fires nothing. Mutants were patched onto the module that calls the helper rather than onto `core`, per the method note the ledger carries, since a helper imported by name is resolved in the importing module's globals and a mutant applied to `core` alone silently never runs. The third new test pins the A6 false block the user chose to keep as a recorded limit (R14-4).
- 2026-09-18 - Added `TestFindDeleteBranchesAndActions` (8 tests) for round fifteen's blocker and one more recorded-limit test, taking the suite from 411 to 420. The new class is half controls, in the same ratio and for the same reason as the round-fourteen class above: the fix widens what A6 blocks, and a false block is what gets a hook routed around. Every verdict in it was measured before it was written rather than reasoned from find's precedence table, again with `-print` standing in for `-delete` on a throwaway tree: the comma form and the two-action form each selected all six entries where the ordinary cleanup selected two, a grouped `-o` selected the complement of its filter, and each control selected only what its filter names. The four blocking tests were then confirmed to fail against the round-fourteen implementation and pass against the repair, with the four controls holding under both, which is what separates a test that measures the fix from one that merely agrees with it; that was done by defining the old implementation inside a throwaway probe and patching it over the module attribute, so no file was edited to prove it. The ninth test pins the A3 false block the user chose to keep as a recorded limit (R15-2), and it carries the un-negated form of the same command as its control, because that one is a correct block and a fix for the limit must not take it with it.
- 2026-09-18 - Added `TestFindDeleteExpressionPath` (10 tests) for round sixteen's three blockers, taking the suite from 420 to 430. Half the class is controls, for the third `find` round running and for the same reason: the fix widens what A6 blocks and a false block is what gets a hook routed around. Every verdict was measured in Git Bash before it was written, with `-print` standing in for `-delete`, so proving a destructive command destructive again never required running one. **Two of the controls exist because of how they can fail rather than because of what they assert.** The double-negation control pins that `! ! -name` narrows again, which was measured rather than assumed and which a later simplification reading any `!` as broad would quietly break. And the operand control was rewritten before it landed: the obvious specimen, `find . -name '(' -delete`, cannot fail, because a `(` misread as a group opener still inherits a guard from the `-name` in front of it and the verdict does not move, which is the trap this file already records about a control that proves nothing. It was replaced by `-name '-o'`, where a misreading severs the filter from the action, and that one was armed by the mutation pass. The pass ran the round-15 implementation and eight targeted mutants against the class with no file edited, patching each over the module attribute: each mutant fired exactly the tests it should and the no-op rebuild fired none. The round-15 run also returned a finding of its own, a false block on that operand shape that no review had reported.
- 2026-09-19 - Added `TestFindPredicateTable` (9 tests) for round seventeen's two findings, taking the suite from 430 to 439. Five of the nine are controls, because this repair moves the verdict in BOTH directions at once: it starts blocking a shape that was allowed and starts allowing eleven that were blocked, so the controls carry the two failures a one-sided reading would introduce. The mutation pass ran the implementation this replaced and six targeted mutants against the class, each patched over a module attribute with no file edited: 6 of the 9 methods fail against the previous implementation and the other 3 pass under both, being controls; each mutant fired exactly the tests it should; the no-op rebuild fired none; and the restored tree returned to clean. **One expectation in the probe was wrong where the code was right**, and finding out which was which needed a measurement rather than a judgement: `find . -name -delete -delete` was predicted to block, on the reading that an action word cannot be an operand, and real `find` printed only the file actually named `-delete`, so the first word is the pattern and the second is a properly narrowed action. It is now a control asserting exactly that.
- 2026-09-20 - **The suite shrank for the first time: 439 to 411.** Four classes and 33 tests went, `TestFindDeleteOrdering`, `TestFindDeleteBranchesAndActions`, `TestFindDeleteExpressionPath` and `TestFindPredicateTable`, because the behaviour they pinned no longer exists: A6 stopped adjudicating whether a `find -delete` is narrowed and now blocks the verb. `TestFindDeleteBlocksOnTheVerb` replaces them with six. **The ratio is the point rather than the count.** Those 33 tests were the cost of an exception no other destructive verb in A6 has, and each round of the series added more of them to a model that was answering an unanswerable question; a contract that consults nothing needs almost no tests to pin. One test in `TestFindActionsAreExamined` went with them, a control asserting a filtered delete is ordinary cleanup, its verdict having inverted; the class keeps its `-exec` half. **One test needed rework rather than removal, and it is the interesting one.** `TestA3RoundThree.test_find_given_an_action_is_not_a_read` proved A3 waives find-with-an-action by running `find . -name '*.pyc' -delete` through the pipeline and asserting nothing blocked. A6 now blocks that command and runs before A3 in the registry, so the assertion would have been satisfied by A6 short-circuiting and would have stopped testing A3 entirely while still passing - the "control that proves nothing" trap this file already records, arriving by a change somewhere else. It calls A3 directly now. The mutation pass ran four mutants with no file edited: a permissive one suppressing the `-delete` verdict fired 15 blocking assertions and no control; an over-broad one blocking every `find` fired exactly the 4 controls and no blocking assertion; a no-op rebuild fired none; and a fourth, which empties A3's `_FIND_ACTIONS` rather than touching A6, was needed to arm the reworked test at all, since the first two patch a rule that short-circuits before the one it names.
- 2026-09-21 - Added `TestHiddenCommandWrappers` (18 tests) for round nineteen's R19-1, taking the suite from 411 to 429. Seven are controls, in the usual ratio and for the usual reason, with one difference worth naming: this rule refuses a shape without reading what it carries, so the controls are the only thing holding the refusal to its intended width. The armed one is a `-S` belonging to the wrapped program (`env ls -S`), measured in Git Bash first, which a reading that scanned past env's own option region would falsely block. Every spelling was measured before it was written, with `printf` standing in for the payload so nothing destructive ran, and **one measurement corrected the writer rather than the code**: the `-iS` bundle appeared not to execute, and `env -i printf` fails identically with no `-S` at all, because `-i` empties the environment and PATH with it, so an absolute path was needed to see that the bundle does split and execute. The mutation pass ran eight mutants with no file edited: the pre-repair state fired all 11 blocking tests and no control, an always-fires mutant fired exactly the 6 reachable controls and no blocking test, an unbounded option region fired the wrapped-program control, short-only and long-only fired their own spellings, an exact-token reading fired exactly the bundle and attached forms, and the no-op rebuild fired nothing. **The eighth exists because one test was otherwise unarmed**: `test_effective_argv_still_returns_a_suffix_of_its_input` guards a property no A10 mutant can reach, so a mutant performing the rejected splice fix was written to fire it, and fires it alone.
