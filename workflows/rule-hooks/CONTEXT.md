# Rule Hooks

**Last modified:** 2026-09-23

## Purpose
Deterministic rule enforcement: moves Book Dragon's load-bearing always/never rules out of prose and into hooks that fire when an AI acts, so a violation is blocked at the moment it would happen rather than relied on by discipline. Phase 1 wires the two daily-use AIs (Claude Code and Codex) plus an AI-agnostic git pre-commit net that protects every AI and manual commits. The benchmark principle: "hooks guarantee execution; prompts do not."

## Contents
- scripts/ - `workflows/rule-hooks/scripts/` [[workflows/rule-hooks/scripts/CONTEXT]] - The `run.py` entry point, the shared `core.py` contract (which since 2026-09-05 includes a hand-written shell reader in place of `shlex`), and the `rules/` and `adapters/` packages. The blocking rules are A3 (a shell file-read where a dedicated tool applies), A4 (LOG.md shell writes), A6 (dangerous shell), A7 (`.env` writes), A8 (a PowerShell here-string in a Bash command), A9 (inline interpreter code writing a markdown file or `.env`), A10 (a command hidden inside a wrapper option's value, `env -S` and `env --split-string`) and B3 (personal data); A2 is the one remaining log-only trial. A10 runs first, because it fires on a command the reader cannot see at all, and every rule after it would allow that command for want of anything to read.
- tests/ - `workflows/rule-hooks/tests/` [[workflows/rule-hooks/tests/CONTEXT]] - Synthetic-event tests for every rule and both adapters, plus subprocess exit-code, config wiring, and cwd-drift contract tests.
- git-hooks/ - `workflows/rule-hooks/git-hooks/` [[workflows/rule-hooks/git-hooks/CONTEXT]] - The universal git pre-commit shim, activated via `core.hooksPath`.
- fire-log.jsonl (gitignored) - runtime, machine-specific append-only record of trial-rule fires, all blocks, and one `doc_sync_recording` row per pre-commit doc-sync fire; never committed. Its path is overridable by the `BOOK_DRAGON_FIRE_LOG` environment variable, which exists so the test suite can redirect the writes of a `run.py` launched as a subprocess; it is not configuration, belongs in no `.env`, and is unset on every real hook fire. The `.gitignore` line is a glob over `fire-log*.jsonl` so a dated archive of the store is covered by the same rule, the rows recording commands that carry real filesystem paths.
- fire-log-archive-YYYY-MM-DD.jsonl (gitignored) - a retired store, kept for reference and not read as evidence. One exists, dated 2026-09-21: see Known Issues for why its rows cannot be attributed.
- archived/ - `workflows/rule-hooks/archived/` [[workflows/rule-hooks/archived/CONTEXT]] - Holds this workflow's finished design-time documents (`*-PLAN.md`, `*-BRIEF.md`); gitignored and local-only. Individual files are not named here, matching that directory's own CONTEXT.md, since they are personal content.

## Inputs
- A firing tool event on stdin (PreToolUse JSON) from an AI's native hook, selected by `--ai <id>`.
- The project root, resolved at runtime from the AI's channel (`CLAUDE_PROJECT_DIR` for Claude; stdin `cwd` for Codex), falling back to `git rev-parse` then script-relative, adjudicated by a read-only AGENTS.md+.git signature test on disagreement. Codex hook launch commands also resolve the git root before starting `run.py`, because they have no project-root environment variable and may fire from a drifted session cwd.
- For B3 (personal data): the personal-data-guard workflow, reused as the single detection source.

## Outputs
- A block decision rendered in the firing AI's contract (Claude: exit 2 + stderr; Codex: `hookSpecificOutput.permissionDecision="deny"` JSON + exit 0), carrying the three-part block-and-explain message (Blocked / Why / To do it manually).
- The git pre-commit gate blocks a commit that would add personal data to tracked files (exit 1) and prints the same block-and-explain; it then prints a warn-not-block doc-sync CONTEXT/LOG drift advisory (exit 0, commit proceeds) and silently appends one `doc_sync_recording` fire-log record for that fire.
- The SessionStart reminder text (`--reinject`) re-injecting the permission gate and self-correction rules.
- Trial-rule warns and all blocks appended to the gitignored fire-log.

## Steps
1. An AI's PreToolUse hook launches `workflows/rule-hooks/scripts/run.py --ai <id>` with the event on stdin, anchored through the AI's project-root mechanism (Claude: `$CLAUDE_PROJECT_DIR`; Codex: `git rev-parse --show-toplevel` in `.codex/config.toml`).
2. `run.py` resolves the project root and routes the event through that AI's adapter into a normalised `Context`.
3. The dispatcher runs the rules for the event category (shell / write); the first block wins, warns are collected.
4. On a block, the adapter emits the AI's block contract; on a warn or allow, the action proceeds (warns are fire-logged).
5. Any hook or rule failure degrades to allow + fire-log, so a tooling bug never freezes the agent.
6. Separately, `--precommit` runs the commit-time gates (the personal-data hard block, then the doc-sync drift advisory), and `--reinject` prints the SessionStart reminder.
7. Append LOG.md with a completion or failure entry. (Per-fire activity is recorded in the gitignored fire-log, never LOG.md.)

## Dependencies
- `workflows/personal-data-guard/` [[workflows/personal-data-guard/CONTEXT]] - Reused as the B3 detection source by both the per-write rule and the git pre-commit gate (one detection source, two triggers). If its marker functions change, the B3 rule follows.
- `workflows/doc-sync-guard/` [[workflows/doc-sync-guard/CONTEXT]] - Reused (in `--staged` mode) as the CONTEXT/LOG drift detection source for the pre-commit warn-not-block advisory. Loaded lazily by the `--precommit` gate; a guard crash is swallowed so it never disrupts a commit.
- `AGENTS.md` [[AGENTS]] (root) - The source of most of the rules being enforced (A4, A6, A7, B3, blocking A3, and the trial A2) and the project-root signature marker.
- `CLAUDE.md` [[CLAUDE]] (root) - The source of rule A8. The Bash-versus-PowerShell tool convention is a Claude-specific wrapper rule rather than an AGENTS.md one, because AGENTS.md keeps tool references abstract on purpose. The rule itself is enforced for any AI whose shell events reach the dispatcher, since a PowerShell here-string is wrong in a POSIX shell regardless of which AI typed it.
- `.claude/settings.json` and `.codex/config.toml` - Per-AI hook wiring that invokes `run.py` through project-root-anchored commands.
- `workflows/settings-check/` [[workflows/settings-check/CONTEXT]] - Validates the new hook commands are allowlisted so they never prompt.
- `workflows/biblio-tools/scripts/verify.py` [[workflows/biblio-tools/scripts/CONTEXT]] - Checks the git pre-commit hook is active, and asks the installed Codex at every session start whether it will run this workflow's Codex hooks.
- Python 3.13+ standard library only; the `git` CLI for root resolution and the personal-data gate.

## Known Issues
- The Codex live smoke test initially failed on 2026-07-01 because the adapter used the unsupported `continue:false` PreToolUse output and did not read apply_patch bodies from `tool_input.command`. After correction, Codex live blocking is confirmed for A7 on shell redirect, apply_patch, and cp writes to `.env.hooktest`.
- The Claude PreToolUse live block is confirmed live: both A7 paths (the Edit/Write file path and the Bash `> .env` redirect) and the git pre-commit block. It requires the `.claude/settings.json` hooks applied and a new session started, since Claude loads hooks at startup.
- **A hook that does not answer has allowed the command.** The five-second timeout means cost is a correctness property rather than a comfort, and four separate paths crossed it before being fixed: a quadratic pattern in A2, an unbounded glob expansion in A3, quadratic write patterns in A9 reaching four minutes on a large inline script, and the shared reader being re-run once per rule, which took a whole fire to 6.46 seconds while every per-rule cost test passed. Any future rule is subject to the same test: it is not enough to be right, it has to be right inside the budget on adversarial input.
- **The threat model is still a cooperative AI reaching for the wrong tool, and that is what makes the coverage boundary defensible.** Four review rounds pushed hard on evasion - quoted operators, substitutions, wrappers, `sh -c` strings, fabricated heredocs - and every one of them was fixed, so the rules read a command through a reader that models those shell rules rather than matching text. It is not a shell, and the places where it deliberately diverges are recorded in `scripts/CONTEXT.md` [[workflows/rule-hooks/scripts/CONTEXT]]. That is a real improvement, but it does not make this a sandbox: an AI with a shell cannot be contained at the verb-parsing layer, and the list of write shapes each rule recognises remains a list rather than a rule. The backstops are unchanged and are what the guarantee actually rests on: the fully-covered Edit/Write tool path, `.env` being gitignored, and the git pre-commit gate.
- **Shell-write coverage boundary (deliberate, not a gap).** A4/A7 detect shell writes to a protected file via output redirections (`>`, `>>`, `>|`, `&>`, `&>>`), `cp`/`mv` destinations (including the `-t` / `--target-directory` forms), `tee`, and `sed -i` - all through the shared `core.shell_write_targets`. This is intentionally NOT exhaustive: rarer verbs (`dd of=`, `truncate`, `rsync`, `install`, `ed`/`ex`) are out of scope by design. **Arbitrary-code interpreters were wholly out of scope until 2026-09-02, and are now partly covered by A9.** The original reasoning was that their writes "cannot be statically parsed", and that still holds for a *saved* script: nothing in `python build.py` says what it writes. It does not hold where the code is inline, in a heredoc body or after `-c`, because the code is then sitting in the command text. That gap was not theoretical - an inline heredoc appending to three `CONTEXT.md` files is what put a personal name into a publicly tracked file that day. A9 covers the inline case for markdown targets only; the saved-script case remains out of scope for the reason originally given. The threat model is a cooperative AI reaching for the wrong tool, not an adversary - an AI with a shell cannot be fully contained at the verb-parsing layer, and chasing completeness only breeds false-positive fatigue on legitimate tooling. The fully-covered Edit/Write tool path, `.env` being gitignored, and the git pre-commit gate are the backstop for anything the shell parser does not catch.
- **The pre-commit doc-sync advisory surfaces drift only, and that is separate from what it records.** Since 2026-08-11 the same step also reads the guard's `INFO` and `DEGRADED` counts into a fire-log record, so the doc-sync guard's switched-off CONTEXT-only LOG clause can be measured against real commits. The print filter below was not relaxed to do it: the advisory still announces WARN and nothing else, and recording mode adds a silent record rather than commit-time output about a clause that is switched off. Both statements are true at once, so a reader who meets only one of them will draw the wrong conclusion about the other.
- **The pre-commit doc-sync advisory surfaces drift only.** `_precommit_doc_sync` filters the guard's findings to WARN, so a DEGRADED finding (a component of the guard that could not run, today its output-inventory probe on an interpreter without PyYAML) prints nothing at commit time. That is the intended boundary while nothing consumes the inventory answers: the advisory exists to name directories needing a documentation update, and a missing package is not one. It stops being safe once the guard acts on the inventory, because a machine without PyYAML would then skip every gitignored directory at commit time with no signal, so the guard-coverage scope extension carries a locked decision requiring this advisory to be made degrade-aware at that point, presenting the degrade on its own line rather than folding it into the drift list. The producer half of the boundary is recorded in `workflows/doc-sync-guard/scripts/CONTEXT.md` [[workflows/doc-sync-guard/scripts/CONTEXT]].
- **A10 refuses a shape rather than reading it, and a harmless `env -S` is refused with the rest.** `env -S "python3 --version"` is blocked, and that is the accepted cost rather than a defect. Reading the option's value instead would mean reimplementing GNU env's own string splitting, whose quoting, backslash escapes, `${VAR}` substitution and leading-`#` comment rule each differ from the shell's, and any difference between our version and the real one would be a new bypass of the shape being closed. The cost was measured before it was accepted: across 26,985 indexed transcript messages spanning 2026-05-29 to 2026-09-20, every occurrence of `env -S` or `env --split-string` is dated to the single day the bypass was reported and is the review that reported it or the documents it produced, no script in the tree uses either spelling, and `env` is not used as a command wrapper in real work at all. **The fire-log was deliberately not consulted**, and the reason generalises: A6 had never fired on `env -S`, that being the bug, so a zero there measures the gap rather than the usage. A measured false block on ordinary work is the signal to revisit this, as it is for `find -delete`.
- **The fire-log written before 2026-09-21 is not evidence, and the store retired that day is kept rather than trusted.** The suite had been contaminating it since the store was created: a test launching `run.py` as a subprocess re-resolves the path in its own memory, so the in-process redirect the tests used could never reach it. Measured at 15,827 rows, of which 2,096 were the A6 fixture `rm -rf x` and 1,092 the A7 fixture `cp secrets.txt .env`. **The rows cannot be filtered clean**, because warn rows carried no `command` field until that date, so 4,507 of A3's 4,533 rows are unattributable by any method. Two consequences are recorded rather than left implied: A3's promotion evidence of "75 fires from a single session" can never be verified, and is left standing on the separate personal-name incident that motivated it rather than revisited (see `scripts/rules/shell_style.py`); and any future promotion of a trial rule must take its evidence from the post-2026-09-21 store, whose warn rows carry the command. The containment and the checks that hold it are described in `tests/CONTEXT.md` [[workflows/rule-hooks/tests/CONTEXT]].
- **A9 refuses a document name Python builds but the command never spells out, and a read spelled that way is refused with the rest.** `open('notes.' 'md', 'w')`, the same split with a comment or continuation between the parts, and a single literal such as `'notes\x2emd'` all name a markdown file that exists nowhere in the command text. Since 2026-09-22 the question is asked of Python's own parser, which is the reader that runs the code, so no spelling differs between what the hook reads and what executes; a hand-written reading of Python was what let nine such spellings through, one of them the review finding PP1. It stays a refusal rather than an assembly, and it runs before the write shapes, so `print(open('notes\x2emd').read())` is blocked as the accepted cost. The cost was measured before it was accepted: read as though it were inline code, no tracked Python file spells a document name this way, and across 27,278 indexed transcript messages spanning 2026-05-18 to 2026-09-21 every occurrence of the adjacent-literal shape is the review that reported it. Code the parser rejects, ruby included, keeps the older regex refusal of a whitespace join, which also refuses a split literal naming a non-document. A name assembled while the code runs (`'notes.' + 'md'`, a variable) is outside what any command-reading hook can see and stays with the computed-target fallback. A measured false block on ordinary work is the signal to revisit this, as it is for `find -delete` and `env -S`.
- **Editing a Codex hook switches it off until the user approves it again, and Codex says nothing.** Codex stores a fingerprint of each approved hook in the user's `~/.codex/config.toml` and runs a hook only while the hook as written still matches it; a changed hook reads as "modified" and is skipped. That is how every Codex hook here stopped on 2026-07-31, when commit `b8f7f5d` rewrote their commands, and stayed off until 2026-09-23: the fire-log's real Codex rows stop on that date, and a Codex session then ran `cat README.md` unblocked. Three details of the mechanism each cost time to find. Approval is recorded against the project path **as spelled**, so `C:\...` and the `c:\...` spelling VS Code often uses need separate approvals of identical fingerprints. Codex's "trust all" also approves plugin hooks, so project hooks are approved one at a time in `/hooks` (the hooks browser, where a short terminal can hide the Stop event). And an AI writing the approval itself is refused by Claude Code's own safety classifier, correctly: the approval is the user's act. The recurrence guard is the "Codex hook trust" check in `verify.py`, which asks Codex through its app-server at every session start and fails, surfaced, if any project hook would not run; the remedy is in `AGENT-SETUP.md` [[AGENT-SETUP]]. **Any change to the hook commands in `.codex/config.toml` must be followed by the user re-approving them.**
- **The shell reader does not see inside a `powershell.exe -Command '...'` wrapper, and nothing currently hands it one.** `run.py --ai codex` blocks `cat README.md` but allows the same command wrapped that way. It is not reached: Codex executes shell commands inside that wrapper on Windows, but its PreToolUse hook receives the bare command, measured on 2026-09-23 through the Codex SDK (0.156.1) and in every recorded Codex hook event, none of which contains the wrapper. It would become a gap only if a hook began receiving the wrapped form, which is the signal to revisit it.
- A2 remains a trial rule: it fires to the fire-log only and never blocks. It scans the raw command rather than the parsed one, because the backslash it looks for is consumed by any reader that resolves quoting, so it is intentionally coarse pending fire-log tuning. A3 left that state on 2026-09-02 and now blocks; it keeps one warn, the byte-level allowance, which is warned rather than silently allowed so the fire-log shows whether it is being leaned on.
- Phase 1 ships Claude + Codex only. Cursor, Gemini/Antigravity, Windsurf/Devin, Copilot, Cline, and OpenCode are Phase 2+, covered meanwhile by the git pre-commit + audit + prose fallback. A1 and A5 are also Phase 2.

## Revision History
Earlier history archived to LOG.md on 2026-09-23.
- 2026-09-07 - Two further review rounds against the repairs above, and the pattern held both times: of the twenty findings they returned between them, most were in the repairs rather than in the original code. The second round found a blocker introduced by the first - a shared arithmetic helper, extracted that morning precisely to stop one shell fact living in two places, was off by one and switched off all six shell rules inside a quoted substitution. The third round found the pipeline guard applied to `pushd` and not `popd`, which is the same fix-one-of-two-parallel-cases shape as the directory stack the first round fixed, in the same file, days apart. It also found the timing test flaky on correct code and the deliverability check unable to see the adapters, both of which have been rebuilt to measure work rather than time and to read imports off the parse tree. **What all of this says about the work is worth more than the individual fixes:** five consecutive rounds have each found defects in the previous round's fixes, so a round returning findings is the expected result and a clean round is the thing that would need explaining. The change set is not finished.
- 2026-09-12 - Two more rounds, and they are the clearest statement yet of the pattern above. An external review on 2026-09-11 returned eight findings against the repairs; those were fixed, and a review of THOSE fixes returned nine more, two of which were regressions created by the fix that had just landed. That makes eight consecutive rounds in which the newest code was the least correct code, and the two regressions are the sharpest instance in the series: teaching the reader to look inside arithmetic expansion was done by handing the body back to the command lexer, which calls the collector back, so the recursion had no cap where every other recursion in that module has one and the work was quadratic - and both faults end in an allowed command, because the evaluator treats a crashing rule as an allow and a hook that misses its budget has already permitted the command. The repair was to stop reusing the lexer and walk the body once, which closed a third defect for free. Outside the reader, the round closed a general bypass of A3 (`pushd "$(pwd)/x" && popd &&` in front of any blocked read, because an unresolvable `pushd` left the modelled directory stack empty and the following `popd` was read as a failure that killed the chain), a per-program flag assumption in A6 (ripgrep's `-L` is `--follow` and prints matching lines, where grep's hides them), and three ruby write spellings a narrowing had lost. Two findings were about the evidence rather than the code: a control that could not fail because the same round's fix had disarmed it, and a behaviour claim that shipped with no control at all. Suite 331 to 359. **The standing conclusion is unchanged and now better evidenced: this change set is not ready on the strength of a passing suite, and the measure worth watching is not the finding count but whether a round finds defects in the previous round's repairs. Round ten did.**
- 2026-09-12 - Round eleven, a review of round ten's repairs, returned twelve findings, one of them an accepted trade rather than a defect, and nearly all of them were in code written the round before: the ninth consecutive round in which the newest code was the least correct. Three were bypasses of all six shell rules at the arithmetic construct, sharing one cause - whether a `$((` is arithmetic was decided by its last two characters where bash decides by position - and the fix for them made the walk quadratic in depth once more, which was then bounded by a work budget whose first fallback was itself a false negative, caught by probing before it was tested. Seven were in the rules, most of them one fact taught in one spelling and not its siblings: `pushd -n` handled where the directory move succeeds and not where it would fail, a per-program flag table that knew each letter but not which carry a value, ruby's `File.open` covered and `File.new` not. One was about the evidence, a wall-clock test that could not fail, replaced by a count of characters walked - and that replacement's first version was then found by the round's own mutation pass reading its bound from the constant it guarded, so the mutation that removed the budget raised the bound with it. The standing conclusion holds. The change set is not ready, and the next measure is still whether round twelve finds defects in round eleven's repairs.
- 2026-09-12 - Round twelve returned fifteen findings, most of them in round eleven's repairs: the tenth consecutive round in which the newest code was the least correct. Two were in round eleven's arithmetic budget and fallback, which had already been wrong once, so that design was replaced by a stack walk rather than patched a third time. Thirteen were in the rules, and most were one idea written three times - option letters read character by character - which is now one reader in `core` and one bash-faithful parser for the directory builtins. One was a regression in round eleven's own A9 fix, and three were tests that could not fail, each because its chain operator or its operands could not tell the two behaviours apart. **The findings are now mostly spellings a cooperative AI is unlikely ever to type** (`pushd -nn`, `popd +1 +0`, `grep -e -c -e`), which is worth weighing against the documented threat model before deciding whether a thirteenth round is the best use of review effort.
- 2026-09-13 - Round thirteen, an external review of round twelve's repairs, returned nine findings: seven were repaired and two kept as recorded limits at the user's decision. Six of the seven were one question answered with one shared list, namely which words on an interpreter's command line are code. A9 read `-c`, `-e` and `-r` the same way for all six interpreters in every position, so node's `--eval` and `-p` were missed, a flag belonging to a saved script was read as code, and a warning setting such as `-W ignore` was taken for a script name, which let a heredoc that python really runs go unread. Options are now read per interpreter, the way each reads its own command line, with the behaviour measured for python, node and perl. A6 stopped reading an inverted match, and sed and awk filters that only delete, as the source of the statement they remove. Five more defects in the same families were found while repairing and fixed with the round. Planning the mutants found three new behaviours with no test able to fail and one control unable to tell its two behaviours apart, each fixed before the mutation run. The change set has still not had a clean round, and round fourteen goes to an outside reviewer.
- 2026-09-18 - Round fourteen, the outside review, returned one blocker and three majors, and its two rule findings were the same mistake in two places: a question answered by membership where the answer depends on ORDER. A6 accepted any `find` filter anywhere in the argv as narrowing a `-delete`, so `find . -delete -name '*.pyc'` was allowed while deleting the whole tree, `find` evaluating left to right; and A3 read a negated directory verb's exit status as its uninverted one, so `! cd workflows || cat ../README.md` was allowed on the reasoning that a successful `cd` kills the `||` branch, which bash does not do once the `!` has inverted the status. The third major was in the handoff review-packet reader and is recorded in that workflow. Both rule fixes were measured rather than reasoned about, and the `find` one without ever running a destructive command: `-print` stood in for `-delete` on a throwaway tree, which demonstrates the ordering exactly. A fourth finding, an A6 false block, was kept as a recorded limit at the user's decision and pinned by a test; a fifth, an A9 boundary around adjacent Python string literals, was taken out of this change set at his decision and made an Active backlog item, so a small self-contained fix is made on its own rather than as one more thread in a set touching several rules. Suite 397 to 411. **The repairs were made by the same AI that then verified them**, so the change set has not been re-reviewed since they landed; a further round is available rather than required.
- 2026-09-18 - Round fifteen took that available round, and it was the right call: the outside review found the round-fourteen A6 repair incomplete, which makes three consecutive rounds in which a repair contained the next round's finding. **The pattern is no longer about `find`.** Round fourteen's fix asked the right question of the wrong scope, reading only the first `-delete` and treating only `-o` as ending a filter's reach, so a GNU comma operator and a second action in another branch both deleted the whole tree unexamined. It was repaired by reframing rather than by adding the two spellings - what narrows an action is the run of predicates ANDed in front of it, and the question is which operators end that run - and the reframing closed a third case nobody reported, an `-o` inside a group. Both reported shapes were measured with `-print` standing in for `-delete` before anything was written, and the new blocking tests were confirmed to fail against the round-fourteen implementation and pass against the repair, which is the check that separates a test measuring a fix from one agreeing with it. Suite 411 to 420. Of the round's other two findings, the A3 false block was kept as a recorded limit at the user's decision and pinned by a test, and the third was a documentation overstatement in the handoff review process, corrected in `memory/review_process.md`. **These repairs were again made by the same AI that verified them**, so the change set has not been re-reviewed since they landed.
- 2026-09-18 - Round sixteen, the outside review of round fifteen's repairs, returned three blockers and all three were in the A6 `find -delete` check again: the fourth consecutive round in which a repair contained the next round's finding, and the third in a row in the same check. **That is what decided the fix.** Rounds fourteen, fifteen and sixteen had each asked the right question, what narrows this action, and each answered it by searching a range of the argv for a filter-shaped token - a test a token can pass while narrowing nothing. A group whose other branch is unconditional selects every file; a negated predicate selects the complement of the name it carries; a `-name` inside a previous `-exec`'s argv is that command's word and never find's predicate. So the token search was replaced by an evaluation of the expression, at the user's decision to stop patching a design that had been wrong three times, and the replacement closed two further defects nobody had reported: a negated group that round fifteen allowed, and an operand named `-o` that round fifteen falsely blocked, the latter surfacing in the mutation pass rather than in any review. Suite 420 to 430, with the round-15 implementation and eight targeted mutants each firing exactly the tests they should and a no-op rebuild firing none. **These repairs were once more made by the same AI that verified them, and this is now a repeated design failure in one check rather than a single miss, so a further review round is required rather than available before the change set is considered for staging.**
- 2026-09-20 - Rounds seventeen and eighteen closed the `find -delete` series by ending it rather than by repairing it again. Seventeen found the two predicate lists had drifted apart and merged them into one table; eighteen found two more unsafe allows in the merged version, and the second of them, `find . -fstype ntfs -delete`, is the one that mattered: `-fstype` is a genuine test that selects every file on a single-filesystem tree, which means the question the check had asked since round fourteen - does a predicate narrow this action - has no answer available from the argv. The check was measuring a syntactic property of the predicate while the safety it claimed depends on the operand supplied. **So the adjudication was removed instead of repaired**, at the user's decision under the stopping rule he set on 2026-09-19: `find` carrying `-delete` now blocks on the verb, exactly as `rm -rf` and `git clean -fd` do. That consistency is the argument - `-delete` was the only destructive verb in A6 with an exception, and the exception cost five consecutive review rounds, ten blockers, 294 lines of implementation and 33 tests. The cost of removing it was measured rather than assumed: in 82 days of fire-log the check had never fired on real work, and every `find ... -delete` anywhere in the session-search index postdates the check itself, so the command it refuses is one nothing in this project has ever run. A real false block on ordinary work is now the signal to revisit it. Suite 439 to 411, the first time it has shrunk. Decision and reasoning in [[guard_coverage_decision_log]].
- 2026-09-21 - Added rule A10 (`scripts/rules/hidden_command.py`) for round nineteen's R19-1, and it is the first finding in this series that was never really about A6. `env -S "find . -delete"` and `env --split-string="find . -delete"` were allowed because `-S` sat in the table of env options whose values are wrapper configuration, and GNU env instead splits that value into words and executes it. **The consequence measured worse than the report**: all four spellings left `effective_argv` returning an EMPTY argv, so before the repair the same wrapper hid a destructive command, a dedicated-tool read, a LOG.md redirect and an inline document write alike, because a rule handed no argv has nothing to object to. It is a bypass of the reader the rules sit on rather than a hole in one of them, so the block is a rule of its own, running first, rather than a clause inside A6. **The shape is refused rather than parsed**, at the user's decision and by the same argument that ended the `find -delete` adjudication the day before: reading the value means reimplementing GNU env's own splitting, and any difference between our version and the real one is a fresh bypass of the shape being closed. A fifth spelling the review had not named is covered, the attached `-S"cmd"`, and so is the `-iS` bundle, which needed a second measurement to read correctly: it first appeared not to execute, and `env -i printf` fails identically with no `-S` at all, because `-i` empties the environment and PATH with it. Suite 411 to 429.
- 2026-09-21 - Contained the suite's writes to the fire-log and retired the contaminated store, which is the fire-log contamination backlog item and the first work here not driven by a review round. **The store this workflow treats as its evidence base had been corrupted since it was created**, and the mechanism is the kind that hides: a test launching `run.py` as a subprocess starts a separate program that re-resolves the path itself, so the in-process redirect the tests relied on could not reach it, and the test written to guarantee no test writes to the real store covered one of `run.py`'s four writers while its name promised all of them. Measured before the fix at 15,827 rows, 2,096 of them one A6 fixture. Three changes and one retirement: `run.py` reads `BOOK_DRAGON_FIRE_LOG` at import so the suite can redirect a child; the warn row gained `command`, which makes this workflow's own claim about measuring the A3 byte-level allowance true for the first time; containment moved to module level in the test file with a source check and a `tearDownModule` runtime check over it; and the old store was archived to `fire-log-archive-2026-09-21.jsonl` behind a widened `.gitignore` glob rather than filtered, because warn rows carrying no command cannot be attributed. **A3's promotion evidence is recorded as permanently unverifiable rather than quietly relied on**, and A3 is left blocking on the strength of the separate incident that motivated it. `.gitignore` and doc-sync-guard's output inventory were changed together, the inventory row widening from a concrete path to a patterned one with an example, so the archive is provably ignored before anything writes another. **Later the same session, A9 gained the refusal for R14-5**, the adjacent-string-literal finding deferred from round fourteen so a small self-contained fix could be made on its own once the change set it would otherwise have been tangled into was committed. That condition was met this morning. The shape of the fix was the user's decision under the triage rule, and it went against the backlog item's own proposal: the item suggested joining the literals, which predates the rule, and joining means matching Python's own lexer where every difference is a fresh bypass, so the shape is refused instead. All five spellings that bypassed A9 now block, including a three-way split and a split across a newline that the item never named. Suite 429 to 437. The accepted cost, a split literal naming a non-document refused with the rest, is under Known Issues with the measurement behind it.
- 2026-09-22 - Closed PP1, the one finding Codex's review of the three pre-push items returned: A9's adjacent-literal refusal recognised only whitespace as the join, and Python also ignores a comment between the parts. **The fix did not add the comment to the list.** Measuring the family first found nine spellings through rather than one, two of them single literals that hide the dot behind an escape, and a wider separator list would have closed four. The spellings were never the real problem; a hand-written reading of Python was, so A9 now asks Python's own parser which document names a program builds without spelling them out, which closes the family by construction. It is still a refusal, it now refuses a read spelled the same way as a decided cost, and the regex stays as the backstop for code the parser rejects. The first version took 5.08 seconds on a script of 2,000 document-named strings, past the hook's budget, and was made linear before anything shipped. Suite 437 to 446. The Known Issues entry was rewritten to the current behaviour rather than extended. Detail in `scripts/rules/CONTEXT.md` [[workflows/rule-hooks/scripts/rules/CONTEXT]].
- 2026-09-23 - Dependencies line says Python 3.13+, following the project floor raised from 3.9 to 3.13; the scripts CONTEXT file follows. No behaviour change.
- 2026-09-23 - Found and recovered an eight-week outage of every Codex hook, during the review-orchestration feasibility tests. Commit `b8f7f5d` on 2026-07-31 changed the hook commands, which invalidated the user's stored Codex approvals, and Codex silently stops running a hook in that state; nothing here was checking whether Codex would run them, only whether they were written correctly. The user re-approved the four project hooks under both drive-letter spellings, a live Codex session was then blocked by A3 with the fire-log recording it, and `verify.py` gained a session-start check that asks Codex directly. Two Known Issues entries added: the approval mechanism and its three traps, and the PowerShell-wrapper blind spot in the shell reader, which is not reached today. No code in this workflow changed.
