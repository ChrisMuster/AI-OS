# Tests

**Last modified:** 2026-08-05

## Purpose
Standalone unit tests for the audit workflow's close-out hooks and structural helpers. Verifies that the pure `graph_findings`, `encoding_findings`, `personal_findings`, `ai_style_findings`, `doc_sync_findings`, and `skill_hardening_findings` merge helpers keep actionable severities and label findings correctly; that `run_graph_validation`, `run_encoding_check`, `run_personal_data_check`, `run_ai_style_check`, `run_doc_sync_check`, and `run_skill_hardening_check` degrade gracefully (never raising, returning a first-class DEGRADED finding that carries a remediation) when their CLI is missing, crashes, or returns unparseable output; that doc-sync scan-skipped WARN findings are surfaced rather than treated as clean; that the `degraded()` helper and `format_report` count and surface DEGRADED distinctly from a pass; that immediate subdirectory filtering suppresses gitignored child directories; and that the breadth-first `collect_dirs` walk preserves its pruning semantics (skip/hidden/gitignore/no-recurse) and the non-Git fast path.

## Contents
- test_audit_graph_hook.py — `workflows/audit/tests/test_audit_graph_hook.py` [[workflows/audit/tests/CONTEXT]] — Tests for the knowledge-graph merge helper (severity filtering, field remap, label, message format, empty/missing-key cases) and the graceful-skip paths of the validator invocation (missing CLI, unparseable output, subprocess exception, and a valid-payload merge).
- test_audit_encoding_hook.py - `workflows/audit/tests/test_audit_encoding_hook.py` [[workflows/audit/tests/CONTEXT]] - Tests for the encoding-guard merge helper (`encoding_findings`: WARN and FAIL mapped, INFO dropped, and the guard's own DEGRADED passed through unwrapped rather than carrying the repair hint) and the graceful-skip paths of `run_encoding_check` (missing CLI, unparseable output, subprocess exception, and a valid-payload merge).
- test_audit_personal_data_hook.py - `workflows/audit/tests/test_audit_personal_data_hook.py` [[workflows/audit/tests/CONTEXT]] - Tests for the personal-data-guard merge helper (`personal_findings`) and the graceful-skip paths of `run_personal_data_check` (missing CLI, unparseable output, subprocess exception, and a valid-payload merge).
- test_audit_ai_style_hook.py - `workflows/audit/tests/test_audit_ai_style_hook.py` [[workflows/audit/tests/CONTEXT]] - Tests for the ai-style-guard merge helper (`ai_style_findings`, WARN-only) and the graceful-skip paths of `run_ai_style_check` (missing CLI, unparseable output, subprocess exception, and a valid-payload merge).
- test_audit_doc_sync_hook.py - `workflows/audit/tests/test_audit_doc_sync_hook.py` [[workflows/audit/tests/CONTEXT]] - Tests for the doc-sync-guard merge helper (`doc_sync_findings`: WARN mapping including scan-skipped payloads, DEGRADED re-wrapped with the repair hint and naming the inventory probe rather than the whole check, the two severities keeping their own labels when they arrive together, and unknown severities dropped) and the graceful-skip paths of `run_doc_sync_check` (missing CLI, unparseable output, subprocess exception, and a valid-payload merge).
- test_audit_skill_hardening_hook.py - `workflows/audit/tests/test_audit_skill_hardening_hook.py` [[workflows/audit/tests/CONTEXT]] - Tests for the skill-hardening-guard merge helper (`skill_hardening_findings`, WARN-only) and the graceful-skip paths of `run_skill_hardening_check` (missing CLI, unparseable output, subprocess exception, and a valid-payload merge).
- test_audit_degraded.py - `workflows/audit/tests/test_audit_degraded.py` [[workflows/audit/tests/CONTEXT]] - Tests the DEGRADED status: the `degraded()` helper (severity, repairable messages naming setup.py, non-repairable messages flagging a missing workflow, the default `what` naming the whole check, and a component-scoped `what` naming only that component) and `format_report` rendering (count line, its own section, not counted as a failure or a clean pass). Also holds the retired-vocabulary guard: no `def`/`class` name in this suite may still say `single_info`, the pre-2026-07-04 name for the degrade path, with a positive control proving the scan still matches the real defect.
- test_audit_subdir_filter.py - `workflows/audit/tests/test_audit_subdir_filter.py` [[workflows/audit/tests/CONTEXT]] - Tests that immediate subdirectory discovery filters gitignored child directories while keeping normal children; that `audit_directory` reuses a passed-in subdirs list without spawning git and falls back to `get_immediate_subdirs` when none is supplied; and that the parent-indexed children map matches `get_immediate_subdirs` per directory.
- test_collect_dirs.py - `workflows/audit/tests/test_collect_dirs.py` [[workflows/audit/tests/CONTEXT]] - Tests the breadth-first batched `collect_dirs` walk: `_is_git_worktree` detection, the non-Git fast path (git never spawned), gitignore pruning of an ignored dir and its children, skip/hidden exclusion, no-recurse dirs recorded but not descended, one batched check per level, and sorted output.
- test_audit_revision_order.py - `workflows/audit/tests/test_audit_revision_order.py` [[workflows/audit/tests/CONTEXT]] - Tests the Revision History ordering check in `check_context_metadata`: an ascending history passes (the negative control), newest-at-the-top is flagged naming the offending pair, same-day entries are allowed, several inversions produce one warning carrying a count, the archive reference line is not compared as an entry, dated bullets in a later section are not pulled in, and a missing section is not an ordering failure. Plus a repository-wide assertion that every governed CONTEXT.md is currently in order, so the first new violation fails here rather than accumulating, and a scope test pinning that assertion to the audit's own `collect_dirs()` walker so it can never widen onto hidden or gitignored files.
- run_tests.py — `workflows/audit/tests/run_tests.py` [[workflows/audit/tests/CONTEXT]] — One-command runner that discovers and runs every `test_*.py` here; exits non-zero on any failure.

## Inputs
None for the hook and helper tests: they build their own payloads and patch subprocess or gitignore boundaries, and never spawn the real validator. The one exception is the repository-wide ordering assertion in `test_audit_revision_order.py`, which reads the `CONTEXT.md` of every directory the audit governs (the `collect_dirs()` set, not a raw filesystem walk).

## Outputs
- Test results printed to stdout. Non-zero exit code on failure.

## Steps
1. Run the whole suite in one command: `python workflows/audit/tests/run_tests.py`
   (or run the module directly: `python workflows/audit/tests/test_audit_graph_hook.py`).
2. Append LOG.md with a completion or failure entry.

## Dependencies
- `workflows/audit/scripts/run.py` [[workflows/audit/scripts/CONTEXT]] - The module under test (`graph_findings`/`run_graph_validation`, `encoding_findings`/`run_encoding_check`, `personal_findings`/`run_personal_data_check`, `ai_style_findings`/`run_ai_style_check`, `doc_sync_findings`/`run_doc_sync_check`, `skill_hardening_findings`/`run_skill_hardening_check`, `get_immediate_subdirs`, and `collect_dirs`/`_is_git_worktree`/`_git_check_ignored_batch`).
- Python 3.9+ standard library only (unittest, unittest.mock, pathlib).

## Known Issues
- The tests patch the `subprocess.run` boundary rather than spawning the real knowledge-graph validator, so the live `validate --json` contract is covered by the close-out audit run, not here. This keeps the suite fast and hermetic.
- `test_audit_revision_order.py`'s repository-wide assertion is the one test that reads the project rather than a fixture, so it fails when any `CONTEXT.md` gains an out-of-order entry. That is the intended signal (the same shape as the doc-sync guard's inventory assertions), not a flake, and it means the suite result depends on the working tree. Its scope is the audit's own governed directory set (`run.collect_dirs()`), so hidden tool directories and gitignored personal content (wikis, saved conversations) are excluded: policing files the audit never checks would let purely local content fail the audit's suite. `test_scope_never_reaches_outside_the_audit_walker` pins that boundary.

## Revision History
Earlier history archived to LOG.md on 2026-08-05.
- 2026-06-26 - Added test_collect_dirs.py (8 tests) covering the breadth-first batched `collect_dirs` walk: worktree detection, non-Git fast path, gitignore/skip/hidden/no-recurse pruning, one batched check per level, and sorted output.
- 2026-06-26 - Extended test_audit_subdir_filter.py (+3 tests): `audit_directory` reuses an explicit subdirs list without spawning git, falls back to `get_immediate_subdirs` when none is passed, and the parent-indexed children map equals `get_immediate_subdirs` per directory.
- 2026-07-04 - Added test_audit_degraded.py for the new DEGRADED status; updated the four hook tests to assert a DEGRADED finding (was INFO) on the missing-CLI / unparseable / exception paths.
- 2026-07-06 - Added test_audit_doc_sync_hook.py covering the doc-sync-guard merge helper (`doc_sync_findings`, WARN-only) and the graceful-skip paths of `run_doc_sync_check`.
- 2026-07-07 - Updated doc-sync hook coverage for the guard's scan-skipped contract: a git-discovery skip now arrives as a WARN payload and is mapped into the audit report rather than being dropped as INFO.
- 2026-07-09 - Added test_audit_skill_hardening_hook.py (9 tests) covering the skill-hardening-guard merge helper (`skill_hardening_findings`, WARN-only) and the graceful-skip paths of `run_skill_hardening_check`. Umbrella Bucket-1 child #6.
- 2026-07-09 - Review-fix: extended test_audit_skill_hardening_hook.py to 11 tests - asserted DEGRADED now passes through the merge helper (INFO still dropped), and added a `TestGuardAuditContract` class that round-trips the real guard's `findings_json` output through `skill_hardening_findings` to lock the producer/consumer label + shape contract. Code-review of child #6.
- 2026-08-03 - Doc-sync DEGRADED passthrough coverage (81 tests). Extended test_audit_doc_sync_hook.py with three tests: a DEGRADED payload is re-wrapped rather than dropped and carries the repair hint while naming the inventory probe rather than the whole check; a DEGRADED and a WARN arriving together keep their own severities; and an unknown severity is dropped. Extended test_audit_degraded.py with two tests for the new `what` parameter (default names the whole check; a component-scoped value names only that component). Codex code-review of guard-coverage stage 3a.
- 2026-08-04 - Added test_audit_revision_order.py (8 tests, 81 to 89) for the new Revision History ordering check: seven fixture cases including the negative control (an ascending history stays silent) and the positive control drawn from the real defect (newest written at the top), plus a repository-wide assertion that every CONTEXT.md is in order today.
- 2026-08-04 - Review-fix, 89 to 92 tests. The repository-wide ordering assertion walked the tree with a raw `rglob("CONTEXT.md")`, a wider set than the audit governs (131 files against 110): it reached into `.codex/` and gitignored wikis and saved conversations, so purely local content could fail the audit's own suite. It now uses the audit's `collect_dirs()` walker through a new `governed_context_files()` helper, with `test_scope_never_reaches_outside_the_audit_walker` pinning the boundary. Twelve test methods still named `*_returns_single_info` while asserting DEGRADED, so the suite output lied to whoever read it; all twelve renamed, and `RetiredInfoVocabularyTests` in test_audit_degraded.py now fails on any reintroduction (verified by reintroducing one). The four merge-helper tuple assertions were left narrow on purpose, with a comment on each explaining that the helper filters DEGRADED out by design and the wrapper owns that path. Codex adversarial review of guard-coverage stage 3a.
- 2026-08-04 - Review-fix, 92 to 93 tests. The `governed_context_files()` docstring added in the entry above quoted the two set sizes as a fixed pair, and both had already moved by the next day. The numbers are gone rather than refreshed: a count of a set that grows with every new directory is wrong by the next commit and is read as current truth until someone re-measures it. The claim they were supporting - that a raw `rglob` is a strictly wider set than the audit governs - is now asserted instead, in `test_rglob_is_a_strictly_wider_set`, which re-derives both sets on every run, so the docstring cannot drift from the behaviour again. The dated entry above keeps its original figures: it is a record of what was measured that day, not a live claim.
- 2026-08-04 - The two fixture writes in `test_audit_subdir_filter.py` converted from `Path.write_text(..., encoding="utf-8")` to `write_bytes`, so they stop writing CRLF fixtures on Windows and stop breaking the newline half of the AGENTS.md text-I/O rule. `write_bytes` rather than `Path.write_text(newline=...)`, which is a 3.10 API against the stated 3.9 floor. Part of the pass clearing the last 50 sites project-wide; `encoding` became a close-out blocking label in the same change, so the next one fails a gate instead of being reported and ignored. Test count unchanged at 93; no assertion touched.
- 2026-08-05 - Encoding hook DEGRADED passthrough, 93 to 95 tests. `test_output_findings_are_valid_audit_tuples` carried a four-line comment asserting that `encoding_findings` filters DEGRADED out *by design* and that the wrapper owns that path - one of the four deliberately-narrow tuple assertions recorded in the 2026-08-04 entry above. That reasoning stopped being true when encoding-guard gained a per-file DEGRADED for a `.py` it could not parse, so the comment was defending a contract the code no longer had, and the test would have failed the widening it should have been describing. It now asserts the real merged set (WARN, FAIL, DEGRADED, with INFO still dropped), and two new tests pin the behaviour the comment used to deny: DEGRADED is passed through, and passed through *unwrapped*, since it names a skipped file rather than a runtime `degraded()`'s repair hint could fix. The other three narrow assertions are untouched: their helpers really do still filter DEGRADED. The module docstring now states the two DEGRADED paths (helper passthrough for one unreadable file, wrapper-built for a guard that could not run) so the distinction is stated once rather than re-derived per test.
