# review-orchestration: the two AI SDKs the engine drives.
# Optional, and deliberately not in the root requirements.txt: nothing else in the
# project depends on them. Install into the project .venv with:
#   .venv/Scripts/python -m pip install -r workflows/review-orchestration/requirements.txt
# (on macOS / Linux: .venv/bin/python). Pinned exactly, because both SDKs change
# their interfaces between releases and the engine is tested against these.
claude-agent-sdk==0.2.159
openai-codex==0.157.0
