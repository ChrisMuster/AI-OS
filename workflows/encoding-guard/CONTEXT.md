# Encoding Guard

**Last modified:** 2026-08-05

## Purpose
Keeps the project's text encoding clean and consistent. It is the single source of truth for encoding hygiene: a deterministic, script-driven check that scans the project for files that are not valid UTF-8, for mojibake (double-encoded or stray Windows-1252 punctuation), for unexpected byte-order marks, and for CR line endings where the project requires LF, plus a repair mode that normalises the damaged files back to clean UTF-8 with LF line endings and plain ASCII punctuation. Alongside the file scan it runs code-pattern checks over Python sources for the two ways a script reintroduces the problem it just repaired: a text-mode call with no explicit `encoding=`, and a text-mode **write** with no explicit `newline=`. The two are checked independently, so pinning one never masks the other. The write shapes covered are builtin `open(...)`, `Path.open(...)`, and `.write_text(...)`; naming them matters because a shape the scanner does not match is reported clean rather than reported missing. Calls are found on the parse tree rather than by matching call-shaped text, so a definition sharing one of those names is not read as a call, and a `.py` file the scanner cannot parse is reported DEGRADED - the check did not run on it - rather than scanned as raw text. Inside those shapes, both arguments are detected as parsed keyword arguments rather than as text appearing somewhere in the call, so a string value, a comment, or a longer keyword such as `file_encoding=` cannot stand in for the real thing, and spacing cannot hide it. Both are then checked for their value as well as their presence, since AGENTS.md states both clauses as values: `encoding=` must name UTF-8 in one of its spellings, and `newline=` must be `"\n"` or the `""` that `csv` requires. A value the scanner cannot read is left silent rather than guessed at. Scraped and imported third-party data (Reddit collections, wiki `raw/` imports) is deliberately exempt so verbatim source data is never rewritten. The full audit consumes the check as an additive, advisory hook, so an encoding regression surfaces at close-out alongside the structural and knowledge-graph findings.

## Contents
- scripts/ - `workflows/encoding-guard/scripts/` [[workflows/encoding-guard/scripts/CONTEXT]] - The `run.py` entry point (check and fix modes) plus shared detection/repair helpers.
- tests/ - `workflows/encoding-guard/tests/` [[workflows/encoding-guard/tests/CONTEXT]] - Unit tests for the detection signatures, the repair maps, and idempotency, plus a one-command runner.

## Inputs
None required. The check walks the project tree relative to the project root. Optional flags select check vs fix mode, JSON output, and dry-run.

## Outputs
- A findings report printed to stdout (human text, or `--json` for programmatic consumers such as the audit hook).
- In fix mode: repaired files written in place as UTF-8 with LF line endings. A file whose only fault is its line endings gets the newline normalisation alone, never the punctuation folding a corrupted file receives. With `--preserve-mtime`, repaired files keep their original modification times. Read-only check mode writes nothing.
- LOG.md entries (workflow and root) on a real fix run. The read-only check does not log.

## Steps
1. Scan the project for encoding problems (read-only):
   `python workflows/encoding-guard/scripts/run.py --check [--json]`
2. Preview repairs without writing anything:
   `python workflows/encoding-guard/scripts/run.py --fix --dry-run`
3. Repair the flagged files in place:
   `python workflows/encoding-guard/scripts/run.py --fix [--preserve-mtime]`
   Use `--preserve-mtime` for a bulk line-ending pass, so repaired `LOG.md` files keep the timestamps doc-sync-guard reads as evidence.
4. Append LOG.md with a completion or failure entry.

## Dependencies
- `AGENTS.md` [[AGENTS]] (root) - Defines the encoding and text-I/O rules and the em-dash-avoidance rule this workflow enforces.
- `workflows/audit/` [[workflows/audit/CONTEXT]] - The full audit shells out to this workflow's `--check --json` and merges its WARN, FAIL, and DEGRADED findings under an `encoding` label.
- Python 3.9+ standard library only.

## Known Issues
- The check classifies legitimate Unicode (for example correctly encoded accented characters or em-dashes) as clean; it targets encoding corruption, not style. The em-dash-avoidance rule is a writing convention for authored content, not an automated failure here.
- Display-layer artifact, not corruption: a valid UTF-8 em-dash (bytes E2 80 94) renders as a mojibake-like sequence ("a-tilde, euro, double-quote") in any terminal or editor still using the Windows cp1252 code page. The file is clean and the guard correctly reports it clean; what looks like mojibake is the viewer decoding good UTF-8 with the wrong code page. This is the expected explanation when the guard passes but the screen looks wrong; it is not a scanner blind spot.
- Repair handles the common Windows-1252 punctuation cases (dashes, smart quotes, ellipsis, non-breaking space). A file with other invalid bytes that cannot be mapped confidently is reported but left untouched rather than guessed at.
- The line-ending check is only as good as its caller: it reads bytes and decodes them, because opening a file in text mode strips the CR on the way in and would make the check permanently silent. Any future refactor that reaches for `read_text` breaks it without failing a test that reads the file the same wrong way; the pipeline test asserts against the real read path for that reason.
- The directory exemption is coarser than the rule it implements. AGENTS.md exempts scraped and imported third-party data, but the exemption is applied per directory, so an authored `CONTEXT.md` or `LOG.md` living inside a `raw/` folder is unscanned too. Several exist and are CRLF today. They are documentation rather than verbatim source data, so the rule would cover them; the implementation does not.
- Exemption is by directory: `raw/` import folders and `collections/`/`archive/` data are pruned from the walk. Authored hidden config dirs (`.codex/`, `.github/`, `.windsurf/`, etc.) are scanned; only system/tooling dot-dirs on the `SKIP_DIRS` denylist are pruned. A scraped data file stored outside the exempt conventions would be scanned like authored content.
- The newline check reports zero findings as of 2026-08-04, over the set it can see: every `.py` file the walk reaches (170 files), for the three write shapes named in Purpose. That number was reconciled against an independent AST pass sharing no code with the guard, which agrees. A regression now fails close-out rather than accumulating: `encoding` is one of the three `BLOCKING_LABELS` in the close-out verifier [[workflows/close-out/CONTEXT]], so an `encoding` WARN hard-fails the gate. The label's INFO findings (an `open()` with no explicit `encoding=`) stay advisory, and a DEGRADED finding is non-blocking. DEGRADED under this label now means either of two things and the message says which: the guard could not run at all (reported by the audit's wrapper, and carrying a repair hint), or one `.py` file could not be read as Python while the rest of the check ran (reported by the guard itself). Re-measured 2026-08-05 after call discovery moved onto the parse tree: still zero findings and zero degraded across the same 170 files, and the same independent AST pass still agrees.
- A write shape the scanner does not match is reported clean, not reported missing, so the coverage list is the real boundary of the check. This is not hypothetical: `.open(...)` was absent from the call pattern until 2026-08-04 and a CRLF-writing append survived a whole clearance pass because of it. Adding a shape means both clauses and a positive control per clause; the mode's argument position differs per shape (`_OPEN_CALLS`), and reading the wrong position is silent rather than loud.
- `.open` is not owned by `pathlib`: `webbrowser.open`, `os.open`, `tarfile.open`, `zipfile.ZipFile(...).open` and others share the name. The scanner separates them two ways, because neither is enough alone. The receiver's leftmost plain name is checked first against a list of modules whose `.open` is not `Path.open`, which catches `webbrowser.open(...)`, `tarfile.open(...)` and the chained `zipfile.ZipFile(...).open(...)` by name. Whatever survives is judged on mode shape (a short literal drawn from `rwxab+t`), which leaves an unrecognised receiver's `.open("some/resource")` alone rather than reading a write mode out of the characters in that string. Each covers what the other cannot: mode shape cannot tell `tarfile.open("w")`, which opens a file *named* `w`, from `p.open("w")`, and a list of module names cannot help with a receiver nobody has heard of. `io`, `codecs` and `gzip` are on that list too and are a genuine false negative rather than a namesake, since they take `(file, mode)` like the builtin and a real text write through one of them is unchecked. The residual false positive is a bare-name receiver - `thing.open("w")` is indistinguishable from `p.open("w")` without type inference - and is accepted deliberately, since the alternative is dropping the dotted write check that this clause exists for. Also deliberate is the false negative in the other direction: a genuine `Path.open` whose mode is computed at runtime is not flagged.
- A `.py` file the scanner cannot parse or tokenise is reported DEGRADED and its code checks are skipped. That is a coverage gap stated out loud rather than a pass: the file was not checked. It replaces a worse behaviour, in which the blanking pass returned the source untouched on failure and every string and comment in the file was then read as code, so an unparseable file produced blocking WARNs out of its own prose. DEGRADED is non-blocking at close-out (only a WARN blocks for this label), so an unreadable file cannot fail a gate; the audit passes it through under the same `encoding` label rather than dropping it, since an unchecked file must not look identical to a clean one.
- Both argument checks are value checks as of 2026-08-05, but their allowed sets differ and neither covers a value it cannot read. `encoding=` accepts only spellings of UTF-8, folded the way Python's own codec lookup folds them (case-insensitively, with `-` and `_` treated alike), so `utf-8`, `UTF-8`, `utf8`, `utf_8`, `u8` and `utf` all pass. `newline=` accepts `"\n"` and also `""`, because `csv` requires the empty string; that is why the newline set has two members rather than the one value AGENTS.md names in prose. Anything readable and outside those sets is a WARN, `None` included, since `None` names the platform default explicitly rather than omitting it.
- The value checks read a literal and compare what it evaluates to, not how it was typed, so `"\x0a"` passes as a line feed and `r"\n"` fails as the backslash-and-'n' it really is. A variable, an expression, an f-string, or a non-string literal is unreadable and is left silent, because deciding it would mean resolving values across the call site, which is a different kind of check from the single-call inspection this guard does. This is the same false-negative trade made for a computed mode and a splatted call, and it means a project that moved its encodings into a named constant would be unchecked without the guard saying so.
- A call whose arguments are splatted (`open(p, "w", **opts)`, `open(*parts)`) is not checked at all, and is silent rather than reported. Neither argument's absence can be proved there: `**opts` may carry either keyword, and `*parts` may supply either one positionally, since both `open` and `write_text` accept `encoding` by position too. This is the same false-negative trade the guard makes for a computed mode, and the tree contains no such call today (confirmed by an independent AST pass over all 170 Python files), but a future one would pass unexamined. Making these readable means resolving values across the call site, which is a different kind of check from the single-call inspection this guard does.

## Revision History
- 2026-06-24 - Initial creation. Check/fix script, audit-hook integration, and tests.
- 2026-06-25 - Fixed the hidden-directory blind spot: the walk now scans authored dot-dir config (`.codex/`, `.github/`, `.windsurf/`, `.clinerules/`, `.continue/`, etc.) instead of skipping every dot-dir, pruning only system/tooling dot-dirs via `SKIP_DIRS`. Added a directory-walk coverage test. Documented the cp1252 display-layer artifact as the explanation for "guard clean but terminal shows mojibake".
- 2026-08-04 - Closed the workflow's largest documented-but-unenforced gap: `--check` now reports CR line endings as a WARN. AGENTS.md states that all project text is UTF-8 with LF and that this workflow enforces all of it, but LF normalisation existed only inside `--fix`, so the LF half was never checked - a CRLF regression was silently repaired if anyone ran `--fix` and completely invisible if they ran `--check`, including the audit's `encoding` hook. Found with a live positive control rather than by reading code: 138 files in the working tree were CRLF at the moment `--check` reported no problems. Three connected changes shipped with it. The repair path was split, so a file whose only fault is its endings is normalised and nothing else, instead of being routed through the full repair that also folds legitimate em dashes to ASCII - the difference between fixing 138 files' newlines and rewriting 138 files' content. A `--preserve-mtime` flag was added for bulk passes, because doc-sync-guard reads LOG.md mtime as its evidence that a directory logged its change, and normalising 71 LOG.md files without it would have made every directory look freshly logged and masked real drift. And the root cause was fixed rather than only the symptom: all 48 text-write sites across the project now pass `newline="\n"` explicitly, since Windows text mode translates every `\n` to `\r\n` and would have reproduced the entire defect within a day. Tests 26 -> 43; the tree is now clean under both this check and an independent CR scan sharing no code with it.
- 2026-08-04 - Correction to the entry above, and the fix for what it got wrong. That entry claimed all 48 text-write sites across the project now pass `newline="\n"` explicitly. The 48 were real and were fixed, but they were the production scripts only: 67 further text writes sat in workflow test files, and nothing could see them because `check_python_code` returned as soon as it found `encoding=` in a call, so a write that pinned the encoding and omitted the newline was reported clean. The claim that the root cause was closed was therefore false when written, and the guard AGENTS.md names as the enforcer of the newline rule was not enforcing it. The two clauses are now checked independently and a text-mode write missing `newline=` is a WARN. Found by Codex's review of stage 3A, reproduced here with the same positive control. 17 of the 67 sites (those in the actively-edited files) are fixed in this pass, using `write_bytes` rather than `Path.write_text(newline=...)`, which is a 3.10 API against a stated 3.9 floor; the remaining 50 are recorded in Known Issues. Tests 43 -> 51, the new ones built as five positive controls drawn from the rule in AGENTS.md rather than from the tree, plus negative controls for reads, binary writes, `csv`-style `newline=""`, a computed mode, and prose.
- 2026-08-04 - The remaining 50 sites cleared, and the reason they were able to sit there closed. All 50 were one uniform shape (`.write_text(EXPR, encoding="utf-8")`, a single positional argument) across 21 test files in 12 workflows, so they were converted to `write_bytes` by a script that imported this guard's own parser, making the set rewritten exactly the set the guard reports rather than a second definition that could drift. `--fix` could not do this: it repairs file bytes, not source call sites, which is the check/fix asymmetry still on the backlog. The durable half is the gate, not the fix. These were real rule violations that the guard reported correctly and that sat inside a `RESULT: PASS` close-out for a day, because `encoding` findings were advisory everywhere: detection was never the gap. `encoding` is now the third blocking label in the close-out verifier [[workflows/close-out/CONTEXT]], so the next one fails a gate. Verified with a live positive control as well as the mocked unit tests: a real violation planted in the tree fails the real gate, and removing it restores the pass.
- 2026-08-04 - The scanner taught the `.open(...)` shape, which is the gap that made the entry above overclaim. `_CALL_RE` matched builtin `open`, `.read_text` and `.write_text`, so `p.open("a", encoding="utf-8")` was never examined by either clause, and one such append in `workflows/doc-sync-guard/tests/` was writing CRLF while `--check` reported the whole tree clean and three documents said the class of defect could no longer accumulate. Found by Codex's review of the uncommitted work, using an independent AST pass rather than a reading of the scanner. What the fix required beyond adding the name: the mode sits in a different argument position for the two forms (`Path.open(mode)` first, because the path is the receiver; builtin `open(path, mode)` second), and reading the wrong one reads the path as the mode and reports nothing at all. Mode reading was also tightened from "any string literal" to "a short literal drawn from `rwxab+t`", because `.open` is shared with `webbrowser.open`, `os.open`, and `tarfile.open`, and read as a mode the URL `http://x` is a write on the `x` in it. Verified live at both ends: the guard now reports the single real violation it previously could not see, that violation hard-fails the real close-out gate (exit 1), and after fixing the call site the guard and an independent AST pass agree the tree is clean. Tests 51 to 58.
- 2026-08-04 - Argument *presence* is now parsed rather than pattern-matched, which is the third of this round's "the call shape was recognised but the answer was still wrong" defects. `encoding=`, `newline=`, `mode=`, and `text=True` were each detected by asking whether that text appeared anywhere in the raw call, which is wrong in both directions inside the write shapes the guard claims to cover: `write_text("newline=", encoding="utf-8")` and a trailing `# newline=` comment both read as compliant, `file_encoding=` satisfied the test for `encoding=`, and the spaces in `newline = "\n"` defeated it. The mode was read the same loose way, and that one was the worst, because a mode read as absent is read as Python's default - a text *read* - which skips the newline clause entirely: `open(p, mode = "w", encoding="utf-8")`, a plain unguarded text write, was reported clean. The fix parses each call once from two aligned views of the source, names from the strings-and-comments-blanked copy and values from a comments-only-blanked copy, so a name can never be read out of a string body or a comment while a mode literal stays readable. A splatted call is now skipped as unreadable rather than answered, and recorded in Known Issues as a real limit rather than counted as coverage. Found by Codex's review. Verified in both directions, since half of this defect was false positives and a test suite of missing-argument controls would not have caught them: 8 new controls (tests 58 to 66), a live positive control planted in the tree that the real close-out gate rejected with exit 1 and that passed again once removed, and an independent AST pass over all 170 Python files agreeing with the guard exactly - 0 text writes missing `newline=`, no real encoding gap, and the same 2 unreadable `webbrowser.open(url)` calls.
- 2026-08-05 - Both argument checks became value checks, closing the last defect of this class: the guard answered the presence question correctly, but the rule it enforces is a value question. `encoding=None`, `encoding="cp1252"`, and `subprocess.run(..., text=True, encoding="cp1252")` all reported clean while AGENTS.md says every read and write passes `encoding="utf-8"`, so the workflow named as the enforcer was enforcing something weaker than the rule as written. Found by Codex's review of the uncommitted work. The two clauses were pinned separately rather than through one shared helper, because their allowed sets genuinely differ, and that difference is what let the previous round justify presence-only for `newline=` while leaving `encoding=` with no equivalent justification anywhere: `encoding=` accepts only spellings of UTF-8, `newline=` accepts `"\n"` and also the `""` that `csv` needs. `newline=None` and a CR-bearing newline are WARN now too, since presence-only accepted the exact default the clause exists to stop. The cost of tightening was measured before the change rather than assumed: every `encoding=` and `newline=` in the tree is already a compliant literal, so this adds a gate with no cleanup tail and no false positives, and `--check` still reports zero findings. A value the scanner cannot read stays silent rather than guessed at, recorded in Known Issues as a real limit. Tests 66 to 79, split by direction because a suite built only from violations passes on a checker that reports every value. The values themselves are read with `ast.literal_eval` rather than by comparing source text, which was a defect found while writing the handover for this work rather than by a test: comparing spellings would have made the legitimate `newline="\x0a"` a false positive while excusing `newline=r"\n"`, a value `open()` itself rejects, as merely unreadable. Judging what a literal evaluates to gets both right and needs no special case for string prefixes.
- 2026-08-05 - Call discovery moved onto the parse tree, which is the first defect in this sequence that produced *false positives* on a blocking label rather than false negatives. Every round above is about the scanner failing to see something; this one is about it seeing something that was never there. A regex matched call-shaped text, and a definition is call-shaped text, so `def open(self, mode="w")` was reported as a text-mode write missing `newline=` - a WARN, and `encoding` had become a close-out hard fail in this same body of work. A class with a method named `open` and no text I/O in it would have blocked the gate. Found by Codex's review, which named the definition case and the mode-shaped non-`Path` `.open` case. The sweep that followed found two more of the same class that the review had not: `class open(Base)`, and, worse, that a `.py` file which failed to tokenise was scanned with strings and comments *unblanked*, so prose in an unparseable file became findings. Nothing in the tree triggered any of it (all 170 Python files parse), so this was latent rather than live, which is exactly why it needed a control rather than an inspection. Three changes: discovery walks `ast.Call` nodes, so a definition is not a call by construction; an unparseable or untokenisable file returns one DEGRADED finding and no code findings; and `.open` gained a receiver test alongside the mode-shape test, since mode shape alone cannot tell `tarfile.open("w")` from `p.open("w")`. The bare-name receiver is deliberately left as a residual false positive rather than "fixed" by dropping the check, because separating a `Path` from any other object needs type inference; it is pinned by a test so it stays a stated decision. Verified three ways rather than by the suite alone, since a suite is what the previous three rounds each passed: a discovery-equivalence pass over all 170 Python files (the only calls the fix drops are the two known `webbrowser.open(url)` namesakes, which reported nothing before or after), live controls against the real close-out gate in both directions (a planted violation blocks; a planted `def open`, a planted `tarfile.open("w")` and a planted unparseable file do not), and 16 new tests, 79 to 95. The argument parser was deliberately not touched: the backlog item for moving the code checks onto `ast` narrows to that half rather than closing, and a full rewrite on top of an already-large uncommitted change would have traded a known defect for an unknown one.
